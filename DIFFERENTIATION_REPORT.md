# تقرير التمايز — Blank Prime مقابل Strong8K
**تدقيق هندسي مستقل · 2026-07-27**
_الجزء الأول: نسبة الاختلاف وأدلتها. الجزء الثاني: ما يستحق النقل هندسياً من التطبيق المرجعي._

> **قيد مطلق التزمتُ به:** مصدر التطبيق المرجعي `C:\Users\user\Strong8K-App\Strong8K\iOS\` **للقراءة فقط**.
> لم يُكتب ولم يُعدَّل ولم يُحذف فيه أي ملف. الملف الوحيد الذي كُتب في هذه الجلسة هو هذا التقرير.

---

## 0. الخلاصة التنفيذية (اقرأ هذا إن لم تقرأ غيره)

| السؤال | الجواب |
|---|---|
| **نسبة الاختلاف في ما ينظر إليه مراجع Apple** | **≈ 45 %** |
| **الحكم** | **حدّي — أقرب إلى الخطر منه إلى الأمان** |
| **أخطر دليل منفرد** | `SplashView` — **أوّل شاشة يراها المستخدم** — فرقها الحرفي **صفر سطر** · وشاشة المشغّل متطابقة **92.3 %** وتحمل **6 وسوم `#package`** من مشروع Strong8K |
| **أسرع مكسب** | إعادة كتابة النصوص العربية: **389** نصاً حرفياً مشتركاً بين التطبيقين |
| **الهندسة الداخلية** | **69.6 %** من الأسطر المهمّة متطابقة — وهذا **مقصود ومقبول**، ولا يُحاسَب عليه |

**السطر الواحد:** مراجعٌ يتصفّح الرئيسية وتسجيل الدخول والإعدادات وصفحات التفاصيل سيقول «تطبيقان مختلفان».
مراجعٌ يفتح **المشغّل**، أو يقرأ **النصوص**، أو يدخل **الوضع التجريبي** (وهو ما يفعله المراجع الذي لا يملك اشتراك IPTV) سيرى **نفس التطبيق**.

---

## 1. المنهجية — كيف قِسنا بدل أن نخمّن

ثلاثة قياسات آلية على كامل الشجرتين المصدريتين (25 ملف Swift عندنا مقابل 19 عند المرجع):

1. **تطابق الأسطر المهمّة** — كل سطر ≥ 35 حرفاً، غير تعليق وغير تافه، يُبحث عنه حرفياً في كامل المرجع.
2. **الكتل المتطابقة المتّصلة** (`difflib.SequenceMatcher` بعد تجريد التعليقات) — كتل ≥ 10 أسطر متتالية متطابقة. هذا هو مقياس «الاستنساخ البنيوي» الحقيقي، لأنه لا يتأثر بتشابه صيغ SwiftUI الشائعة.
3. **تقاطع النصوص الحرفية** — كل نص بين علامتَي اقتباس يحوي حرفاً عربياً، ومفاتيح `L()` كلها.

بالإضافة إلى قراءة يدوية للمكوّنات المشبوهة، ومقارنة `Assets.xcassets` و`Info.plist` و`project.pbxproj` و`Podfile` بالبصمة (md5).

---

## 2. نتائج القياس الخام

### 2.1 نسبة الأسطر المتطابقة حرفياً مع المرجع

| الملف | متطابق / المجموع | النسبة | الطبيعة |
|---|---|---|---|
| `ThumbHash.swift` | 105 / 105 | **100 %** | هندسة (نقل حرفي مقصود، MIT) |
| `VLCPlayer.swift` | 202 / 204 | 99.0 % | هندسة |
| `Downloads.swift` | 329 / 335 | 98.2 % | هندسة + واجهة |
| `Services.swift` | 216 / 222 | 97.3 % | هندسة |
| `PlayerView.swift` | 465 / 489 | **95.1 %** | **واجهة مرئية** ⚠ |
| `PlayerEngine.swift` | 249 / 264 | 94.3 % | هندسة |
| `ActivationView.swift` | 120 / 129 | **93.0 %** | **واجهة مرئية** ⚠ |
| `CatalogDB.swift` | 179 / 196 | 91.3 % | هندسة |
| `Models.swift` | 92 / 104 | 88.5 % | هندسة |
| `Core.swift` | 1054 / 1202 | 87.7 % | هندسة + النصوص |
| `BlankTVApp.swift` | 57 / 65 | 87.7 % | بنية التطبيق |
| `AuthViews.swift` | 159 / 237 | 67.1 % | واجهة |
| `ContentViews.swift` | 769 / 1319 | 58.3 % | واجهة |
| `HomeView.swift` | 294 / 541 | 54.3 % | واجهة |
| `SettingsView.swift` | 280 / 563 | 49.7 % | واجهة |
| `DesignSystem.swift` | 270 / 687 | 39.3 % | نظام التصميم |
| `GatewayView.swift` | 40 / 209 | **19.1 %** | واجهة — الأفضل |
| `RailEngine` · `StreamRouter` · `EngineStats` · `EngineDecisionCache` | 0 | **0 %** | ملكية خالصة |
| **المجموع** | **4959 / 7128** | **69.6 %** | |

### 2.2 الكتل المتّصلة المتطابقة (الاستنساخ البنيوي)

| الملف | أسطر مستنسخة / مجموع الملف | عدد الكتل | أطول كتلة |
|---|---|---|---|
| `PlayerView.swift` | 1258 / 1363 = **92.3 %** | 16 | **316 سطراً** |
| `Downloads.swift` | 796 / 833 = 95.6 % | 8 | 195 |
| `ActivationView.swift` | 382 / 439 = **87.0 %** | 11 | 103 |
| `Core.swift` | 1985 / 2439 = 81.4 % | 34 | 271 |
| `AuthViews.swift` | 362 / 670 = 54.0 % | 7 | 97 |
| `SettingsView.swift` | 556 / 1374 = **40.5 %** | 10 | **175** |
| `ContentViews.swift` | 1245 / 3556 = **35.0 %** | 48 | 138 |
| `HomeView.swift` | 573 / 1699 = **33.7 %** | 20 | 82 |
| `DesignSystem.swift` | 781 / 2456 = **31.8 %** | 23 | 112 |
| `GatewayView.swift` · `RailEngine` · `StreamRouter` · `EngineStats` · `EngineDecisionCache` | **0** | 0 | 0 |

**ملفات الواجهة مجتمعة: 5157 من 12254 سطراً = 42.1 % كود واجهة منسوخ حرفياً.**
منها **2459 سطراً تُنتج بكسلات فعلية على الشاشة** (تصنيف آلي: الكتلة «مرئية» إن كان ≥ 25 % من أسطرها معدِّلات SwiftUI بصرية).

### 2.3 النصوص

| المقياس | المرجع | عندنا | مشترك |
|---|---|---|---|
| نصوص عربية حرفية فريدة | 416 | 540 | **389 = 72 % من نصوصنا** |
| مفاتيح `L()` | 361 | 356 | **301 = 84.6 % من مفاتيحنا** |
| نصوص خاصة بنا فقط | — | **151** | — |

### 2.4 جرد المكوّنات

- **74 من 107** بنى `struct …: View` عندنا تحمل **نفس اسم ونفس دور** بنية في المرجع = **69.2 %**.
- في `ContentViews.swift` تحديداً (سطح التصفّح كله): **28 من 29** بنية عرض تشترك في الاسم والترتيب مع المرجع. الجديدة الوحيدة هي `UnifiedReorderView`.
- **33 بنية عرض ملكية خالصة**: كل مكوّنات البوّابة (`GatewayView`, `GatewayPosterWall`, `GatewayMarqueeRow`, `GatewayModeSwitcher`)، وإعدادات V2 (`SettingsProV2`, `SetScaffold`, `Set*Page`)، ومفردات صفحة التفاصيل (`S8KPlinth`, `S8KPlayCapsule`, `S8KSatellite`, `S8KDetailHeading`, `S8KExpandableText`, `S8KDetailsSheet`)، والهياكل العظمية، و`HeroCarouselView`, `RankRail`, `ExpandedNavBar`, `S8KMetrics*`.

---

## 3. التقييم بالأبعاد

### البُعد 1 — الهوية البصرية: **52 %**

**ما اختلف فعلاً (دليل):**

| العنصر | Strong8K | Blank Prime | الحكم |
|---|---|---|---|
| الخلفية الأساسية | `#0A0A0A` أسود شبه خالص — `DesignSystem.swift:14` | `#001A0B` أخضر داكن جداً — `DesignSystem.swift:708` | **مختلف تماماً** |
| السطح / البطاقة / المرتفع | `#0E0E10` · `#151518` · `#1C1C20` — `:15–17` | `#04220F` · `#072B14` · `#0B361C` — `:709–711` | مختلف |
| اللون المميّز العالي | `#FFD700` ذهبي — `DesignSystem.swift:88` (`BrandTheme.gold`) | `#CBFF06` ليموني — `DesignSystem.swift:712` | **مختلف تماماً** |
| المميّز المتوسط | `#C8860A` — `:88` | `#00BC72` تركوازي — `:713` | مختلف |
| أيقونة التطبيق | تلفاز ذهبي «8K» على أسود | «بلانك ستور» بخط أسود على ليموني | **مختلف 100 %** |
| نصف الأقطار | 6/10/14/18/24/32 — `DesignSystem.swift:268–276` | 4/7/10/13/16/22 — `DesignSystem.swift:850–858` | مختلف (~30 % أضيق) |

**ما لم يختلف (وهذا ما يخفض الدرجة):**

| العنصر | الدليل | الخطورة |
|---|---|---|
| **سلّم الطباعة متطابق 100 %** | 13 رمزاً بنفس المقاس ونفس الوزن: 34/black · 28/heavy · 22/bold · 18/bold · 15/semibold · 15/regular · 14 · 13 · 12 · 11 · 10 · 9 · mono12 — `ref DesignSystem.swift:239–253` مقابل `ours DesignSystem.swift:815–832` | **عالية** — الإيقاع الطباعي هو أول ما يُقرأ بصرياً |
| **سلّم المسافات متطابق 100 %** | 4/8/12/16/20/24/32/48 — `ref :256–265` مقابل `ours :835–844` | عالية |
| **درجات النص المحايد متطابقة** | `white.opacity(0.60/0.35/0.20)` — `ref :31–33` مقابل `ours :773–775` | متوسطة |
| **ألوان الحالة متطابقة حرفياً** | `#FF3B30 · #34C759 · #007AFF · #FF9500` — `ref :36–39` مقابل `ours :778–781` (ألوان نظام، أقل خطورة) | منخفضة |
| **وصفة التدرّج متطابقة** | `S8KGradient.gold` بنفس مواضع التوقّف `0.0 / 0.22 / 0.62 / 1.0` — `ref :56–68` مقابل `ours :807–819`؛ و`goldFlat` = تدرّج ثنائي أفقي هو زر الإجراء الأساسي في التطبيقين | **عالية** |
| **شعار الكلمة بنفس التركيب** | `[كلمة محايدة semibold rounded متباعدة] + [كلمة بلون مميّز heavy بمقاس ×1.02 tracking 0.04] + نفس الظل` — `ref DesignSystem.swift` (`STRONG` + `8K`) مقابل `ours` (`Blank` + `Prime`) | **عالية جداً** — هذا هو ختم العلامة في الشريط العلوي وفي البوّابة |
| **`AccentColor.colorset` متطابق بالبايت** | `srgb(0.784, 0.525, 0.039)` = **ذهب Strong8K `#C88609`** — نفس المحتوى تماماً في الملفين | **حرجة** (انظر §6) |
| **`SplashView` — أوّل شاشة يراها المستخدم — فرقها *صفر سطر*** | 68 سطراً في الملفين، `diff` = 0 — `ours AuthViews.swift` مقابل `ref AuthViews.swift` | **حرجة** — «شاشة الإطلاق» بند صريح في الهوية البصرية |
| نفس رموز SF للتبويبات الخمسة | `house · dot.radiowaves.left.and.right · film · tv · gearshape` — `ref DesignSystem.swift:1154–1161` مقابل `ours DesignSystem.swift:1897–1904` | متوسطة |

### البُعد 2 — العمارة التنقّلية وتركيب الصفحات: **50 %**

**اختلافات حقيقية:**
- **شريط التبويبات**: المرجع = شريط زجاجي عريض ثابت بخمسة تبويبات وكبسولة ذهبية متحرّكة (`ref DesignSystem.swift` — `AppTabBar` عند `ref DesignSystem.swift:1174`). عندنا = شريط عائم قابل للانكماش إلى «قرص» في الزاوية + حقل بحث يتشكّل من الحبّة نفسها (`ours DesignSystem.swift:1995` — `AppTabBar` + `ExpandedNavBar` + `collapsedPuck` + `searchField`). **اختلاف كبير ومرئي.**
- **شاشة الدخول**: `GatewayView.swift` (697 سطراً، **0 % استنساخ**) — جدار ملصقات متحرّك + بطاقة دخول سفلية + حبّة لغة ذهبية + مبدّل كبسولي. لا مقابل له في المرجع.
- **الإعدادات**: عندنا مركز تنقّل + صفحات فرعية (`SettingsProV2`, `SetConnectionPage`, `SetPlayerPage`, `SetAppPage`, `SetAboutPage`, `SetScaffold`) بدل قائمة أكورديون واحدة في المرجع.
- **صفحات التفاصيل**: أُعيد بناؤها فعلاً — تحقّقتُ من ذلك بنفسي: `MovieDetailView` (`ours ContentViews.swift:2527`) لا يستخدم `NavigationStack`، بل لوحة فنية مثبّتة (`canvasHeight` عند `:2543`) + منصّة معتمة (`S8KPlinth` عند `:2560`) + كبسولة تشغيل بحجم المحتوى (`S8KPlayCapsule` عند `:2713`) + أقمار دائرية (`S8KSatellite` عند `:2609/2722/2728`). **و`MetaSection` محذوف فعلاً** — لم يبقَ منه إلا تعليق عند `ContentViews.swift:2745`، بينما لا يزال حياً في المرجع عند `ref ContentViews.swift:2868` ويُستدعى في `:2800/2817/2830`.

**تشابهات باقية:**
- **نفس التبويبات الخمسة بنفس الأسماء ونفس الأيقونات**؛ الفرق هو الترتيب فقط (`home` نُقل إلى المنتصف) — `ours DesignSystem.swift:1885` مقابل `ref DesignSystem.swift:1142`.
- **نفس جرد الشاشات**: 74/107 بنية عرض تتشارك الاسم؛ وفي سطح التصفّح 28/29.
- **الرئيسية**: شريط الإعلان ولافتة الإدارة **متطابقان بايتاً ببايت بما فيهما التعليقات** — `ours HomeView.swift:1037–1080` مقابل `ref HomeView.swift:308–352`.

### البُعد 3 — التشابه على مستوى المكوّن: **40 %**

**استنساخات لا تزال حيّة وتُرسم على الشاشة:**

| المكوّن | عندنا | في المرجع | الحجم |
|---|---|---|---|
| **`ContentCard`** — بطاقة الملصق العمودية | `DesignSystem.swift:1777` | `DesignSystem.swift:1028` | **متطابقة عملياً**: نفس 118×166، نفس `S8KRadius.md`، نفس حدّ `s8kBorder`، نفس الشارة (`caption3`، أسود، `goldFlat`، `S8KRadius.xs`، `padding 7`)، نفس `spacing: 7`، نفس `caption1.semibold` / `caption2` — وحتى **نفس التعليقات** (`// Image container` · `// Badge` · `// Progress bar` · `// Title` · `// Subtitle`). الفارق الوحيد: شريط التقدّم استُخرج إلى `S8KProgressBar`. **وهي مستعملة حيّة في أربعة مواضع في الرئيسية** (`HomeView.swift:1164, 1169, 1381, 1402`) — أي في نفس السياق تماماً كالمرجع (`ref HomeView.swift:753, 775`) |
| **شاشة المشغّل** | `PlayerView.swift` — كتل 316 و180 و140 و95 و88 سطراً | `PlayerView.swift:563, 331, 1224, 882, 1111` | **92.3 %** — مناطق الإيماءات، HUD الصوت/السطوع، مؤقّت النوم، الترجمات، السرعة، المسار الصوتي، إخفاء عناصر التحكّم، تعزيز ×2 بالضغط المطوّل… كلها نفس الشيفرة |
| **`PlaylistsView`** | `SettingsView.swift:707` | `SettingsView.swift:735` | 116 سطراً متتالياً |
| **`ParentalControlView`** (شاشة رمز الاسترداد والإدخال) | `SettingsView.swift:1200` | `SettingsView.swift:1179` | **175 سطراً متتالياً** |
| **`ActivationRequiredView` / `UpdateRequiredView`** | `ActivationView.swift:337 / 144` | `ActivationView.swift:306 / 124` | 103 + 49 سطراً |
| **رأس صفحة البحث** (العنوان + إغلاق + الحقل + شرائح النطاق) | `ContentViews.swift:3308` | `ContentViews.swift:3435` | 90 سطراً |
| **`LoadingView`** | `DesignSystem.swift:1702` | `DesignSystem.swift:951` | 87 سطراً |
| **`CategoryReorderView`** | `ContentViews.swift:1143, 1216` | `ContentViews.swift:1201, 1292` | 67 + 72 سطراً |
| **`ContentTabBar`** (الكل/المفضلة/الأجدد/السجل) | `ContentViews.swift:1325` | `ContentViews.swift:1404` | 65 سطراً |
| **`CategoryPickerSheet`** | `ContentViews.swift:1406` | `ContentViews.swift:1487` | 75 سطراً |
| **لافتة خطأ الرئيسية** | `HomeView.swift:853` | `HomeView.swift:206` | 82 سطراً |
| **`AlertsView`** | `HomeView.swift:1527` | `HomeView.swift:897` | 43 سطراً |
| **بطاقات اختيار طريقة الاتصال في `LoginView`** | `AuthViews.swift:354` | `AuthViews.swift:408` | 97 سطراً |
| **شارة التشغيل في `ChannelRow`** | `ContentViews.swift:613–617` | `ContentViews.swift:~468` | دائرة 32×32 بتدرّج + `play.fill` 12pt bold + ظل — نفس الوصفة |

**مكوّنات فرقها الحرفي = صفر سطر (قِسناها بـ `diff` مباشرة):**

| المكوّن | الملف عندنا | الأسطر | فرق `diff` |
|---|---|---|---|
| `ThumbHash.swift` كامل الملف | `ThumbHash.swift` | 658 | **0** (بصمة MD5 واحدة: `7a93676a5e461bc6a39de3e54a861021`) — *لكنّه نقلٌ حرفي لخوارزمية ThumbHash العامة برخصة MIT، وتطابقه **مبرَّر تقنياً** (رأس ملفنا نفسه يقول إنه «يجب أن يبقى متطابقاً بايتاً ببايت كي يظلّ صحيحاً»). لا تُعامله كدليل انشقاق — عامله كمكتبة طرف ثالث* |
| `SplashView` + `S8KLogoMark` | `AuthViews.swift` | 68 | **0** |
| `PrivacyView` | `AuthViews.swift` | 46 | **0** |
| `TermsView` | `AuthViews.swift` | 40 | **0** |
| كتلة حقول `LoginView` | `AuthViews.swift:188–255` | 68 | **0** |
| `S8KButtonStyle` | `DesignSystem.swift` | 8 | **0** |
| `OutlineButton` | `DesignSystem.swift` | 21 | **0** |
| `GoldDivider` | `DesignSystem.swift` | 9 | **0** |
| `ErrorView` | `DesignSystem.swift` | 23 | **0** |
| `EmptyState` | `DesignSystem.swift` | 17 | **0** |
| `S8KGlassGroup` | `DesignSystem.swift` | — | **0** |
| `CategoryRow` | `ContentViews.swift:1329` | 49 | **0** |
| `SearchField` | `ContentViews.swift:1424` | 26 | **0** |
| `FolderCard` | `ContentViews.swift:1451` | 27 | **0** |
| `FlexWrap` | `ContentViews.swift:3518` | 10 | **0** |
| `ContentCard` | `DesignSystem.swift:1777` | 63 | **9** |

كما أن **25 مكوّناً مشتركاً في `DesignSystem.swift` يظهر بنفس تسلسل المرجع بالضبط**، مزاحاً ~750 سطراً لأن شيفرة جديدة أُضيفت قبله. و`ActivationView.swift` يختلف بثلاثة أنواع تحرير ميكانيكية فقط: غلاف `ScrollView`، و`Capsule()` ← `RoundedRectangle(S8KRadius.md)` في أربعة مواضع، ونصّ واحد (`Text("STRONG 8K")` عند `ref:305` ← `Text("Blank Prime")` عندنا `:336`) — بل إننا أبقينا `.padding(.top, 70)` كما هي وأضفنا إليها تعليقاً فقط.

**اتجاه النسخ مُثبَت من الشيفرة نفسها:** `Downloads.swift` عندنا **لقطة أقدم** من المرجع لا تنفيذ مستقلّ — كل فرق فيه هو إصلاح أضافه المرجع **لاحقاً** ونحن نفتقده (`handoffActiveTurboToBackground` عند `ref:225–242` غائب؛ ومغلّف `StoreEnvelope`/`FailableItem` عند `ref:487–530` غائب، ولا نزال على السطر الواحد الذي كان قبل الإصلاح عند `:464–470`).

**ما تأكّد أنه اختفى فعلاً (تحقّق شخصي):**
- ✅ `MetaSection` — محذوف من شجرتنا كلياً.
- ✅ لوحة `strongGold` (`#0A0A0A` + `#FFD700`/`#C8860A`) — محذوفة من `DesignSystem.swift` (بقي تعليق توثيقي عند `:734`).
- ✅ صفحتا التفاصيل — أُعيد بناؤهما بمفردات ملكية (انظر البُعد 2).

### البُعد 4 — النصوص والعبارات: **28 %** ← الأسوأ

**389 نصاً عربياً حرفياً مشتركاً**. ليست كلمات عامة، بل فقرات كاملة. أمثلة (كلها ظاهرة للمستخدم):

| النص | عندنا | في المرجع |
|---|---|---|
| «تطبيق مشغّل فقط — لا يوفّر ولا يستضيف أي قنوات أو محتوى…» | `Core.swift:579` | `Core.swift:539` |
| «اضغط الأقسام بالترتيب الذي تريده — الأول يأخذ الرقم 1، ثم 2، وهكذا…» | `Core.swift:93` | `Core.swift:77` |
| «نحمي بيانات حسابك بتشفير كلمات المرور وتأمين الاتصال بخوادمنا…» | `Core.swift:471` | `Core.swift:449` |
| «نجمع فقط بيانات تسجيل الدخول، معرّف الجهاز، وإحصاءات استخدام أساسية بموافقتك.» | `Core.swift:465` | `Core.swift:443` |
| «أرسل معرّف جهازك أعلاه إلى موزّعك لتفعيل التطبيق، ثم اضغط «تحقّق مرة أخرى».» | `Core.swift:509` | `Core.swift:487` |
| «تلقائي = عتادي للبث المباشر والأفلام، VLC لبقية الصيغ» | `Core.swift:291` | `Core.swift:268` |
| «رابط قائمة التشغيل غير صالح — يجب أن يبدأ بـ http» | `Services.swift:173` | `Services.swift:173` — **نفس رقم السطر** |
| «أدخل رابط السيرفر (مثال: http://server.com:8080)» | `Services.swift:103` | `Services.swift:103` — **نفس رقم السطر** |
| «للتواصل مع أجهزة AirPlay على نفس الشبكة» | `Info.plist` | `Info.plist` — **الملف كله متطابق بالبايت** |

سياسة الخصوصية، والشروط، وإخلاء المسؤولية، ونصوص التفعيل، ورسائل الأخطاء، ووصف كل مفتاح في الإعدادات — كلها **منقولة نصاً**. هذا أقوى دليل «انشقاق» يمكن أن يقع عليه مراجع، لأنه لا يحتاج إلى كود: يكفي أن يفتح التطبيقين جنباً إلى جنب على شاشة الإعدادات.

### البُعد 5 — الهندسة الداخلية: **≈ 15 %** — *ولا يُحاسَب عليها*

`69.6 %` من الأسطر المهمّة متطابقة؛ الشبكة، ومحلّل Xtream/M3U، ومحرّكات التشغيل، والتخزين المؤقّت، و`ThumbHash` (منقول حرفياً بترخيص MIT)، و`CatalogDB`، و`Downloads`، و`Models` — كلها مشتركة.

> **صراحةً: هذا لا يُحاسَب عليه ولا يجب تغييره.** Apple لا تفكّك الثنائيات ولا تقارن الشيفرة المصدرية بين تطبيقَي مطوّر واحد؛ البند 4.3 يُطبّق على **ما يُرى**. مشاركة المحرّك بين تطبيقين لمالك واحد ممارسة صناعية عادية تماماً. **انقل الهندسة، ولا تنقل الشكل** — والهندسة هنا منقولة كما ينبغي.

---

## 4. الرقم الواحد

| البُعد | الدرجة | الوزن | المساهمة |
|---|---|---|---|
| 1 — الهوية البصرية | 52 % | 30 % | 15.6 |
| 2 — العمارة التنقّلية وتركيب الصفحات | 50 % | 30 % | 15.0 |
| 3 — التشابه على مستوى المكوّن | 40 % | 25 % | 10.0 |
| 4 — النصوص والعبارات | 28 % | 15 % | 4.2 |
| **المجموع الموزون** | | | **≈ 44.8 %** |

## **نسبة التمايز = ≈ 45 %**

*(الأوزان: البصر والعمارة هما ما يراه المراجع في أول 30 ثانية، فأخذا 60 %؛ المكوّنات 25 % لأنها ما يظهر عند التصفّح الفعلي؛ النصوص 15 % لأنها لا تُلاحَظ إلا عند التدقيق — لكن إن لوحظت كانت قاطعة. البُعد 5 خارج الحساب عمداً.)*

**سلّم القراءة:** 0 % = نسخة طبق الأصل · 100 % = تطبيقان لا علاقة بينهما.
`45 %` تعني: **تطبيقان يتشاركان الهيكل العظمي نفسه، وقد بُدِّل جلدهما جزئياً — لا كلياً.**

---

## 5. الحكم

# **حدّي — Borderline**

**هل سيقول مراجع Apple «نفس التطبيق بجلد مختلف»؟ — يعتمد على مسار المراجعة، وهذا في حدّ ذاته خطر غير مقبول:**

- **إن تصفّح الرئيسية + التصفّح + التفاصيل + الإعدادات + الدخول فقط** → سيقول **لا**. البوّابة والإعدادات V2 وصفحتا التفاصيل وشريط التبويبات والهوية اللونية كلها ملكية حقيقية، والعمل الأخير عليها كان صحيحاً.
- **إن فتح فيديو** → شاشة المشغّل نفسها بنسبة 92.3 %: نفس مناطق الإيماءة، نفس HUD السطوع والصوت، نفس صفائح الترجمة والسرعة والمسار الصوتي ومؤقّت النوم، بنفس المقاسات. المشغّل هو **الشاشة الأطول عمراً في تطبيق IPTV**، وسيفتحه المراجع حتماً.
- **إن لم يملك اشتراك IPTV** — وهو الحال الغالب — **فسيدخل الوضع التجريبي**، فيرى نفس الأفلام الأربعة من Blender بنفس الأوصاف العربية حرفياً، ونفس القنوات التجريبية.
- **إن قرأ أي شاشة نصية** (الخصوصية، الشروط، التفعيل، رسائل الخطأ) → نصوص متطابقة.

الخلاصة: **المخاطرة الحالية ليست في الشكل العام، بل في أربعة جيوب كثيفة الاستنساخ**، ثلاثة منها تقع على المسار الذي يسلكه المراجع تحديداً. إغلاق هذه الجيوب يرفع الرقم من ≈45 % إلى ≈68 % دون المساس بأي تصميم معتمد.

---

## 6. النتائج الحرجة — كل أصل أو نص مشترك حرفياً

| # | الاكتشاف | الموقع | لماذا هو حرج |
|---|---|---|---|
| **C1** | **`AccentColor.colorset` متطابق بالبايت** — `srgb(0.784, 0.525, 0.039)` = `#C88609` = **ذهب Strong8K بعينه** | `blankstor/BlankTV/Assets.xcassets/AccentColor.colorset/Contents.json` مقابل `Strong8K/iOS/Strong8K/Assets.xcassets/AccentColor.colorset/Contents.json` | هذا هو `tintColor` النظامي: يلوّن أزرار النظام وصفائح المشاركة ومنتقي الوسائط. أي أن **لون العلامة الأخرى لا يزال يُرسم داخل Blank Prime**. وهو يناقض قرار حذف `strongGold` (commit `7bb4b6e`) — الحذف تمّ في Swift ونُسي في كتالوج الأصول |
| **C2** | **`Info.plist` متطابق بالبايت** بما فيه النص العربي `«للتواصل مع أجهزة AirPlay على نفس الشبكة»` | `BlankTV/Info.plist` مقابل `Strong8K/Info.plist` | نصّ **يظهر في تنبيه إذن نظامي** يراه المستخدم والمراجع |
| **C3** | **389 نصاً عربياً حرفياً مشتركاً** (72 % من نصوصنا) + **301 مفتاح `L()`** (84.6 %) | `Core.swift` (قاموس `L()` تقريباً `:80–590`)، `Services.swift:30, 103, 173` | أقوى دليل انشقاق متاح، ولا يحتاج المراجع إلى أي أداة لرؤيته |
| **C4** | **المحتوى التجريبي متطابق** — نفس أفلام Blender الأربعة بنفس الأوصاف العربية، نفس القنوات، نفس المسلسل التجريبي | `Core.swift:2359–2430` (`enum DemoContent`) مقابل `ref Core.swift:3496–3570` | **هذا بالضبط ما يراه مراجع بلا اشتراك IPTV** |
| **C5** | **نطاق الخادم نفسه مكتوب في الشيفرة**: `https://strong8k.app/api/v1` | `Core.swift:595–596` (و`APIClient` يستعمله فعلاً في `Services.swift:43, 304, 321, 344, 401`) | اسم العلامة الأخرى داخل ثنائي Blank Prime، وفي حركة الشبكة |
| **C6** | **`ContentCard` متطابقة عملياً وحيّة** | `DesignSystem.swift:1777`، مستعملة في `HomeView.swift:1164/1169/1381/1402` | العنصر البصري الأكثر تكراراً في التطبيق كله |
| **C7** | **سلّم الطباعة وسلّم المسافات متطابقان 100 %** | `DesignSystem.swift:815–844` مقابل `ref :239–265` | الإيقاع البصري للتطبيقين واحد حتى لو اختلفت الألوان |
| **C8** | **شعار الكلمة بنفس التركيب الهندسي** | `ours DesignSystem.swift:1653` مقابل `ref DesignSystem.swift:908` | ختم العلامة نفسه، بكلمتين مختلفتين فقط |
| **C9** | **وسوم تذاكر عمل داخلية من مشروع Strong8K باقية في شيفرتنا**: `#package` في **6 مواضع** | `PlayerView.swift:181, 365, 583, 724, 1297, 1335` | أثرٌ لا يقبل التأويل: وسمُ مشروعٍ آخر داخل مصدر هذا التطبيق. ويصاحبه أن **231 من 256 تعليقاً (90 %) في `PlayerView.swift` منقولة حرفياً**، وكل عناوين `// MARK:` الخمسة عشر لطبقة التحكّم بنفس الترتيب |
| **C10** | **مثال «رمز الموزّع» في التوثيق هو حرفياً `"strong"`** | `AuthViews.swift:114` | اسم العلامة الأخرى مكتوب كمثال داخل شيفرتنا |
| **C11** | **17 مفتاح `UserDefaults` كلها ببادئة `s8k.` ومتطابقة بايتاً ببايت** — `s8k.fav.movies` · `s8k.watchlist` · `s8k.catorder2.live` · `s8k.theme.cache` · `s8k.lang` … | `Core.swift:38–1163`, `ActivationService.swift:98`, `ContentViews.swift:3156` | يصل إلى **التخزين على الجهاز**: التطبيقان يكتبان ملفَّي تفضيلات متطابقَين بنيوياً. مرصود وقت التشغيل، لا في الشيفرة فقط |
| **C12** | **`Podfile` متطابق تقريباً** بما في ذلك التعليقات | `blankstor/Podfile` مقابل `Strong8K/iOS/Podfile` | غير مرئي للمراجع — للتوثيق فقط |
| **C13** | **بادئة الأنواع `S8K` في كل التطبيق** (`S8KFont`, `S8KImage`, `s8kBlack`…) وبادئة مفاتيح `UserDefaults` | كامل الشجرة | غير مرئية للمراجع، لكنها في رموز الثنائي. أولوية منخفضة |

**غير مشترك (تأكيد إيجابي):** أيقونة التطبيق (بصمتان مختلفتان، وتصميمان لا يشبهان بعضهما)، صور الشعار الثلاث، معرّف الحزمة (`com.blanktv.player` مقابل `com.strong8k.player`)، اسم العرض (`Blank Prime` مقابل `Strong 8K`).

---

## 7. أهم خمسة إجراءات — مرتّبة بأثرها على الرقم

### 1️⃣ إعادة كتابة النصوص العربية المشتركة — **+8 نقاط تقريباً**
**الملف:** `BlankTV/Core.swift`، قاموس `L()` (تقريباً `:80–590`) + `BlankTV/Services.swift:30, 103, 173` + `BlankTV/Info.plist`.
**ماذا تفعل بالضبط:** لا تترجم من جديد — أعِد **صياغة** أعلى 150 نصاً ظهوراً بصوت تحريري مختلف: سياسة الخصوصية والشروط وإخلاء المسؤولية (فقرات كاملة، `Core.swift:465–495`)، نصوص التفعيل (`:507–512`)، رسائل الأخطاء (`:98–113`)، وصف كل مفتاح إعدادات (`:280–320`)، ونصّ `NSLocalNetworkUsageDescription`.
**لماذا هو الأول:** أرخص تغيير على الإطلاق (لا خطر انهيار، لا تعديل تخطيط، لا مراجعة بصرية على الجهاز)، وأقوى دليل عند المراجع. **صفر خطر هندسي.**
**الحجم:** ~150 سطر نصّي · 3–4 ساعات.

### 2️⃣ إعادة تصميم شاشة المشغّل — **+7 نقاط تقريباً**
**الملف:** `BlankTV/PlayerView.swift` (1363 سطراً، 92.3 % منها منسوخ).
**ماذا تفعل بالضبط — ولا تلمس المحرّك:** الإيماءات والمنطق تبقى كما هي (هندسة). غيِّر **طبقة العرض** فقط:
- تخطيط عناصر التحكّم: المرجع يضعها في صف سفلي كلاسيكي — انقلها إلى مفردات Blank نفسها المستعملة في صفحة التفاصيل (كبسولة بحجم المحتوى + أقمار دائرية 48pt).
- شريط التقدّم: استبدل `Slider` المعياري بقاعدة رفيعة بأسلوب المنصّة (نفس لغة `S8KPlayCapsule`).
- HUD الصوت والسطوع: المرجع = عمود عمودي في الوسط؛ اجعلها شارة أفقية صغيرة أعلى الحافة.
- الصفائح الأربع (`sleepSheet`, `subtitleSheet`, `speedSheet`, `audioSheet` — `PlayerView.swift:563–566`): أعِد بناءها على `SetScaffold` الذي تملكه أصلاً في الإعدادات V2.
**الخطر:** متوسط — المشغّل على المسار الحرج، ولا يمكن التحقق البصري إلا على الجهاز. نفّذها **على مراحل** (طبقة واحدة لكل بناء) بنفس نمط «العرض المعزول» الذي نجح مع البوّابة.
**الحجم:** ~400–500 سطر · 10–14 ساعة على 3–4 بناءات.

### 3️⃣ استبدال `ContentCard` وشارة `ChannelRow` — **+3 نقاط**
**الملف:** `BlankTV/DesignSystem.swift:1777` (`ContentCard`) و`BlankTV/ContentViews.swift:613–617` (شارة التشغيل).
**ماذا تفعل بالضبط:** الملصق 118×166 (نسبة 1:1.41) هو مقاس المرجع نفسه. غيّره إلى نسبة ولغة Blank التحريرية: ملصق أعرض قليلاً بزوايا `S8KRadius.sm` (7)، **العنوان خارج البطاقة بمحاذاة الحافة لا تحتها بمحاذاة `trailing`**، والشارة نصاً مسطّحاً على أرضية معتمة بدل مستطيل بتدرّج، وشريط التقدّم كخطّ عند الحافة السفلية للملصق (نفس الحيلة التي استُعملت في صفوف الحلقات). واستبدل دائرة التشغيل 32pt المتدرّجة بمثلّث خالص بلا خلفية.
**الخطر:** منخفض — مكوّن واحد، أربعة مواضع استدعاء.
**الحجم:** ~80 سطر · 2–3 ساعات.

### 4️⃣ استبدال المحتوى التجريبي بالكامل — **+3 نقاط**
**الملف:** `BlankTV/Core.swift:2359–2430` (`enum DemoContent`).
**ماذا تفعل بالضبط:** أبقِ روابط Blender المفتوحة (مشروعة ومجانية) لكن **بدّل كل نصّ ظاهر**: أسماء عرض مختلفة، أوصاف عربية مكتوبة من جديد، أسماء أقسام مختلفة، وملصقات مختلفة (استخدم ملصقات `gwposter1…6` التي تملكها أصلاً، أو صوراً من الخادم المركزي). غيّر أيضاً «`Blank Prime Originals (تجريبي)`» ومسمّيات الحلقات.
**لماذا:** هذه هي الشاشة التي يراها المراجع بلا اشتراك. أثرها يفوق حجمها بكثير.
**الخطر:** منخفض جداً.
**الحجم:** ~50 سطر · ساعة واحدة.

### 5️⃣ تنظيف الآثار الحرفية — **+3 نقاط** (وأرخص بند في التقرير)
**الملفات:**
- `BlankTV/Assets.xcassets/AccentColor.colorset/Contents.json` → غيّر إلى ليموني Blank `#CBFF06` = `srgb(0.796, 1.000, 0.024)`. **سطر واحد، وهو أعلى عائد/جهد في التقرير كله.**
- `BlankTV/Core.swift:595–596` → انقل النطاق إلى نطاق خاص بـ Blank (أو إلى `Store`/التهيئة البعيدة) بدل `strong8k.app` المكتوب في الشيفرة.
- `BlankTV/PlayerView.swift:181, 365, 583, 724, 1297, 1335` → **احذف وسوم `#package` الستة** (وسوم تذاكر مشروع Strong8K). بحث/استبدال، صفر خطر.
- `BlankTV/AuthViews.swift:114` → غيّر مثال رمز الموزّع من `"strong"` إلى مثال محايد.
- **`SplashView` + `PrivacyView` + `TermsView` (`AuthViews.swift`)** → فرقها الحرفي **صفر سطر**، و`SplashView` هي **أوّل شاشة في التطبيق**. أعِد بناء الثلاث بمفردات BLANK (شاشة الإطلاق تحديداً: استعمل `BrandLogo` + الليموني بدل تركيب المرجع).
- `BlankTV/DesignSystem.swift:815–832` (`S8KFont`) → أزِح السلّم بمقدار ملموس: مثلاً `display 40/black · title1 30/black · title2 24/heavy · title3 19/bold · headline 16/semibold · body 15 · callout 13.5 · subhead 13/bold · caption1 11.5`. الطباعة أعرض تأثيراً من أي مكوّن مفرد، والتغيير محصور في ملف واحد.
  *(ملاحظة: هذا يمسّ مظهراً معتمداً — يحتاج موافقتك واختباراً على الجهاز، تماماً كما ينصّ `DEVICE_MATRIX.md` §6.)*
**الحجم:** ~30 سطراً + جولة تحقّق بصري · 2–3 ساعات.

### ما بعد الخمسة (الدفعة التالية، بنفس الترتيب)
6. `ParentalControlView` (175 سطراً متتالياً) و`PlaylistsView` (116) — `SettingsView.swift:1200 / :707`؛ أعِد بناءهما على `SetScaffold` الذي تملكه.
7. `ActivationView.swift` (87 % منسوخ) — `ActivationRequiredView` / `UpdateRequiredView` / `MaintenanceView`.
8. شريط الإعلان ولافتة الإدارة في الرئيسية (`HomeView.swift:1037–1080`) ولافتة الخطأ (`:853`).
9. رأس صفحة البحث (`ContentViews.swift:3308`) و`ContentTabBar` (`:1325`) و`CategoryPickerSheet` (`:1406`) و`LoadingView` (`DesignSystem.swift:1702`).
10. بطاقات اختيار طريقة الاتصال في `LoginView` (`AuthViews.swift:354`).
11. المكوّنات التي فرقها صفر في `DesignSystem.swift` (`S8KButtonStyle`, `OutlineButton`, `GoldDivider`, `ErrorView`, `EmptyState`, `S8KGlassGroup`) و`ContentViews.swift` (`CategoryRow`, `SearchField`, `FolderCard`, `FlexWrap`) — كلها صغيرة، وكل واحدة تعديل مركّز.
12. مفاتيح `UserDefaults` ذات البادئة `s8k.` (17 مفتاحاً) → بادئة `blank.` مع ترحيل صامت لمرة واحدة (لا تكسر بيانات المستخدمين الحاليين). **مرصودة وقت التشغيل**، وليست في الشيفرة فقط.
13. تغيير ترتيب/مجموعة أيقونات التبويبات (رموز SF مختلفة عن `house/film/tv/gearshape`).

**بعد تنفيذ 1–5 فقط:** الرقم المتوقّع **≈ 66–70 %**، والحكم يتحوّل من «حدّي» إلى «لا — تطبيقان مختلفان».

---

# الجزء الثاني — ما يستحق النقل هندسياً

> **قاعدة هذا الجزء:** هندسة فقط. كل فكرة تصميمية موسومة صراحةً بـ **«تصميم — لا تنقلها»**.
> كل بند دُقِّق مقابل شيفرة BLANK الحالية قبل التوصية به — لا نوصي بشيء نملكه أصلاً.

**الخلاصة قبل التفاصيل:** BLANK **متقدّم على المرجع** في طبقة العرض (نوافذ `ForEach`، `scrollTargetBehavior`،
`containerRelativeFrame`، مذكّرة ThumbHash السالبة، حارس إعادة استخدام الخلية، ذاكرة LRU للمحرّك، فرز آمن من NaN،
`s8kWindowSize()` بدل `UIScreen.main.bounds`). المرجع يتفوّق في **أربعة مواضع** لم نُكملها: **طبقة البيانات التي تُغذّي
تلك العروض**، و**صحّة التشغيل وقياسه**، و**تخزين بيانات الاعتماد**، و**نجاة العمل في الخلفية**.
ونحو **60 %** من الحجم الزائد في المرجع شيفرةُ خادم مركزي **غير قابلة للنقل بقرارنا** (فصلنا الخلفية عمداً).

**وأهم ما في هذا الجزء ليس «نقلاً» أصلاً:** ثلاثة من أعلى البنود قيمةً هي **أخطاء حيّة في BLANK اليوم**، وبند رابع هو **آلية بنيناها بالكامل ولم نوصلها بأي مستدعٍ**.

---

## الطبقة 1 — أفضل عائد ÷ مخاطرة · يوم عمل واحد تقريباً · كلها خطر منخفض

### 1. إعادة بناء فهرس FTS عند القراءة من ذاكرة القرص — 4 أسطر · 15 دقيقة
عند إقلاع بارد يُخدَم من ذاكرة القرص (12 ساعة)، المرجع يعيد بناء فهرس SQLite/FTS إن لم يكن مُهيّأً. نحن لا نفعل، وفهرسنا لا يُكتب إلا عند جلب **شبكي**.
- **المرجع:** `Core.swift:2244–2248` · **عندنا:** `Core.swift:1736–1742` — نفس الكتلة تماماً، والإعادة غائبة.
- **الأثر (متحقَّق منه):** `CatalogDB.isSearchable` تعود `false` ← يسقط `SearchVM` إلى `ContentViews.swift:3184–3221` الذي ينفّذ `contains` على **الكتالوجات الثلاثة كاملة**، بتخصيص نصّ جديد لكل عنوان، بلا طيّ وبلا فهرس. بعد إقلاع مخزَّن هذا هو **مسار البحث الوحيد** — وهو بالضبط ما بُني الفهرس لإلغائه.
- **الخطر:** منخفض. مهمّة منفصلة `.utility`، والعملية idempotent.

### 2. `isLoading` لا يُطفأ أبداً على عنصر مُسخَّن مسبقاً — 6 أسطر · 30 دقيقة · **خلل حيّ عندنا**
مراقب `status` في AVPlayer لا يُطلَق إلا عند **تغيّر**، فللمرجع مسار سريع صريح.
- **المرجع:** `PlayerEngine.swift:447–451` — `if pItem.status == .readyToPlay { resumeIfNeeded(); avPlayer.playImmediately(atRate: 1.0); isLoading = false }`
- **عندنا:** `PlayerEngine.swift:349–350` — `avPlayer.play()` مجرّدة، بلا فحص جاهزية.
- **النتيجة:** نحن **نستعمل** `MediaPrefetcher` فعلاً (`ContentViews.swift:2571`)، فالعنصر المُسخَّن قد يكون `.readyToPlay` **و**`isPlaybackLikelyToKeepUp` لحظة تبنّيه. عندها لا يُطلَق `statusObs` (`:432`) ولا `likelyKeepUpObs` (`:462`)، ولا شيء آخر يطفئ `isLoading` ← **دوّامة تحميل دائمة فوق فيديو يعمل بشكل سليم**. أي أن هذا الخلل **يُبطل بالضبط مكسبَ «التشغيل الفوري» الذي بنيناه**.

### 3. `saveProgress()` عند `load()` — سطران · 20 دقيقة · **فقدان بيانات عندنا**
«الحلقة التالية» تنتقل من N إلى N+1 على نفس الـ VM، فيجب حفظ موضع العنصر الخارج أولاً.
- **المرجع:** `PlayerEngine.swift:464` و`VLCPlayer.swift:118` — `if !isLive { saveProgress() }`
- **عندنا:** `PlayerEngine.swift:363–364` و`VLCPlayer.swift:117–118` تقفزان مباشرة إلى `setItem`. ونحن نُفعّل الانتقال التلقائي (`PlayerView.swift:278+`) ← **كل حلقة غير أخيرة في جلسة مشاهدة متتالية تضيع من «متابعة المشاهدة»**.

### 4. تأخير إعادة الاتصال عند تبديل المحرّك — 8 أسطر · 45 دقيقة
المحرّك البديل ينتظر 1.5 ثانية حتى يُحرّر مقبسُ المحرّك المهدوم الفتحةَ الوحيدة على لوحات `max_connections=1`؛ وإلا استقبل المحرّك الثاني خطأ 456 و**فشل هو الآخر على بثّ سليم تماماً**.
- **المرجع:** `PlayerView.swift:51, 122–127, 213/221, 535–540` · **عندنا:** الوسيط والمواضع الأربعة كلها أُزيلت — `PlayerView.swift:541` صار `.onAppear { vm.setup(); … }` مجرّداً.
- أعلى قيمة لكل سطر في فرق المشغّل كلّه، بالنسبة لتطبيق IPTV.

### 5. الخروج عند `Task.isCancelled` بعد فحص Turbo — سطر واحد · 10 دقائق
`try? await s.data(for: head)` تبتلع `CancellationError`، فتمضي المهمّة الملغاة و**تُطلق تحميلاً خلفياً ثانياً لنفس المعرّف** — يرفضه المزوّد كاتصال مكرّر.
- **المرجع:** `Downloads.swift:287–291` · **عندنا:** `Downloads.swift:267–271`.
- **تعرّضنا أسوأ من المرجع:** `pause()` عندنا (`Downloads.swift:391–396`) تستدعي `t.cancel()` فتُشعل المسار حتى بلا أي تسليم.

### 6. إفراغ ذاكرات الصور عند تحذير الذاكرة / الخلفية — ~15 سطراً · 15 دقيقة · **جديد: لا يملكه أي من التطبيقين**
لا أحد منهما يستدعي `removeAllObjects` من `didReceiveMemoryWarningNotification` ولا عند `scenePhase == .background`. مع 96 ميغابايت من الصور المفكوكة (`DesignSystem.swift:1230`) + مشغّلَي `AVPlayer` حيَّين في `MediaPrefetcher` — هذا طُعمٌ مثالي لقتل التطبيق (jetsam). و`MediaPrefetcher.clear()` موجودة (`MediaPrefetcher.swift:57`) و**لا يستدعيها أحد** في التطبيقين.

### 7. فكّ ملصقات الشبكات عند 500 بكسل لا 800 — ~15 تعديلاً · 30 دقيقة
- **المرجع:** `DesignSystem.swift:548–553` — `totalCostLimit = 64MB` مع مبرّر مكتوب بجانبه (كتالوج 176 ألف فيلم، 928 جلباً/جلسة، حرارة «serious»)، وكل موضع ملصق يمرّر `maxPixel: 500`.
- **عندنا:** `DesignSystem.swift:1229–1230` — `240 / 96MB`، وأكثفُ شبكتين تأخذان **الافتراضي 800** (`ContentViews.swift:1974` و`:2423` بلا وسيط؛ والجلب المسبق عند `:1949`/`:2492` يمرّر 800). و16 موضع `S8KImage` في التطبيق تُغفله.
- التكلفة تتناسب مع **مربّع** `maxPixel` ← **2.56×** من البايتات المفكوكة وزمن الفكّ على أكبر الشاشات، مع سقف ذاكرة **أعلى**. نحن أفضل أصلاً في الشعارات (240) وأشرطة الرئيسية (420/160) — المشكلة في الشبكات تحديداً.

### 8. رايتا AirPlay + حارس التشغيل الخارجي في مراقب الفيديو — ~10 أسطر · ساعة · **تُنقلان معاً**
- **المرجع:** `PlayerEngine.swift:435–436` يضبط `allowsExternalPlayback` **و**`usesExternalPlaybackWhileExternalScreenIsActive = true`؛ ومراقبه عند `:523–547` يتخطّى التبديل حين `isExternalPlaybackActive`، **ويُسقِط** شرط `presentationSize != .zero` (بثّ HEVC داخل TS **يملك** مسار فيديو لكنه لا يُعرض — فذلك الشرط كان يُبطل الفحص أصلاً).
- **عندنا:** صفر ذكر لـ `allowsExternalPlayback`؛ و`PlayerEngine.swift:417–429` يُبقي شرط `!sized` المُبطِل، بلا حارس AirPlay، ويكتب نصاً عربياً غير مُعرَّب مباشرة عند `:425` حيث يستعمل المرجع `L("player.err.no_video")`.
- ونحن **نعرض منتقي AirPlay** أصلاً (`VLCPlayer.swift:682–691`) — أي أن الواجهة تَعِد بميزة لم يُهيَّأ لها المشغّل.

### 9. نقل `m3uURL` إلى الـ Keychain بصلاحية `AfterFirstUnlock` — ~25 سطراً · ساعة · **أمن + موثوقية**
في تسجيل Xtream المباشر يكون `m3uURL` حرفياً `http://host/player_api.php?username=X&password=Y`.
- **المرجع:** `Core.swift:748–757` (مفتاح Keychain بـ `kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly`)، `:778–779` (`accessible:` كوسيط)، `:1042–1058` (ترحيل شفّاف لمرة واحدة من UserDefaults، لا يحذف النسخة الصريحة إلا بعد نجاح الكتابة).
- **عندنا:** `Core.swift:1011–1017` يخزّنه في **UserDefaults نصاً صريحاً**؛ و`Core.swift:816` يُثبّت `WhenUnlockedThisDeviceOnly` بلا تجاوز لكل مفتاح — فحتى بعد النقل، أي تحديث كتالوج أو تنزيل يبدأ والجهاز مقفل سيقرأ `nil`. **النصفان لازمان معاً.**

### 10–16. بنود الطبقة 1 الصغيرة

| البند | المرجع | عندنا | الحجم |
|---|---|---|---|
| توجيه VLC عبر `BasePlayerVM.remoteURL` المشترك (يُنهي سياسة تحليل رابط مكرّرة انحرفت فعلاً) | `VLCPlayer.swift:340–347` | `VLCPlayer.swift:316–328` | −12 سطراً |
| تهدئة دوّامة التخزين 0.3/1.2ث ← 1.0/0.7ث | `PlayerView.swift:1101–1109` | `PlayerView.swift:1102–1106` | سطران |
| `network-caching` 1500 ← 3000 (1500 هو **الحدّ الأدنى** المتحقَّق منه لا الهدف؛ توقّفات ميدانية 52 ← 2691 هي ما دفع الرفع) | `VLCPlayer.swift:266–273` | `VLCPlayer.swift:259–261` | سطر |
| `clock-jitter=0` + `clock-synchro=0` (زمن أوّل إطار للـ VOD من 16ث إلى ~4ث بلا تصغير المخزن) | `VLCPlayer.swift:280–281` | `VLCPlayer.swift:250–263` — `makeMedia` فيه 3 خيارات فقط | سطران |
| تقييد تبنّي التسخين بـ `!isLive` وتطبيق ضبط المخزن على الفرعين | `PlayerEngine.swift:410, 420` | `PlayerEngine.swift:328–336` | سطران |
| احترام `reduceMotion` في تلاشي الصور | `DesignSystem.swift:693, 731–732` | `DesignSystem.swift:1445` — يتحرّك دائماً | 3 أسطر |
| مهلة `get_vod_streams` من 22 إلى 45 ثانية (أكبر حمولة، شبكة خلوية بطيئة) | `Core.swift:2589` | `Core.swift:2066` — الافتراضي 22 | سطر |

---

## الطبقة 2 — يوم إلى يومين · خطر منخفض إلى متوسط

### 17. تحصين حفظ سجلّ التنزيلات — ~40 سطراً · ساعة ونصف · **أعلى خطورة في التقرير**
المرجع يكتب مغلّفاً `{v:1, items:[…]}` ويفكّه **عنصراً عنصراً** عبر غلاف `FailableItem`، فيُسقِط المدخل التالف بدل إبادة الملف، وينقل ملفاً غير قابل للتحليل إلى `downloads.corrupt.json`.
- **المرجع:** `Downloads.swift:486–531` · **عندنا:** `Downloads.swift:462–471` — `decode([DownloadItem].self)` بمنطق «الكل أو لا شيء»، و`return []` عند الفشل، ثم `persist()` تكتب `[]` فوقه فوراً.
- و`DownloadItem` يحوي حمولة `Movie`/`Episode`/`Series` كاملة (`Downloads.swift:34–36`). أي: **إضافة حقل واحد غير اختياري إلى `Movie` تمحو مكتبة التنزيلات كاملةً من الواجهة لدى كل مستخدم**، بينما تبقى ملفات بحجم غيغابايتات يتيمةً على القرص إلى الأبد — لا شيء يمسح `Documents/Downloads` بعدها. صامت ودائم.
- **تحفّظ يذكره المرجع نفسه** (`:492–495`): الفكّ العنصري لا ينقذ إلا مجموعة **مختلطة**؛ فكل حقل مستقبلي على تلك النماذج يجب أن يكون اختيارياً أو بقيمة افتراضية.

### 18. `DatabasePool` بدل `DatabaseQueue` — ~5 أسطر · ساعة
- **المرجع:** `Core.swift:1753–1768` — `DatabasePool` بوضع WAL «كي تمضي قراءات القوائم على لقطة **بينما** يكتب تحديثُ كتالوجٍ خلفي، بدل أن يتعطّل القارئ خلف الكاتب».
- **عندنا:** `CatalogDB.swift:28–36` — `DatabaseQueue` متسلسل. كل بحث FTS وكل قراءة مُصفَّحة تتعطّل خلف `save()` الجُملي لـ 20–50 ألف صف.
- الطرفان يستعملان `.read {}` / `.write {}` على بروتوكول `DatabaseWriter` المشترك، فالتبديل متوافق مصدرياً.

### 19. طبقة قياس جودة التشغيل (QoE) — ~170 سطراً + ~20 موضع نداء · 4–6 ساعات
عندنا **صفر** قياس تشغيل. `EngineStats.swift:17–26` يعدّ **أيّ محرّك اختير** فقط — لا **كيف شغّل**.
- **المرجع:** `PlayerEngine.swift:335–336, 487–488, 591–602` يستخرج من `AVPlayerItemAccessLog`/`ErrorLog` (معدّل البتّ المرصود والمُعلَن، `numberOfStalls`، `numberOfDroppedVideoFrames`، رموز الأخطاء). و`Telemetry.swift` يضيف زمن أوّل إطار لكل محرّك، ونِسَب إعادة التخزين بساعة جدارية (`:140–149`)، و`thermalState`، و`isLowPowerModeEnabled`، والبصمة الذاكرية عبر Mach `task_vm_info` (`:193–203`). ولأنّ VLC بلا سجلّ وصول، فبكرةُ الحالة `hasPlayed` (`VLCPlayer.swift:546–565, 593`) هي مصدر QoE الوحيد لـ VOD.
- **انقل طبقة القياس واترك `snapshot()`** — لا خادم لدينا. اربطها بلوحة «تشخيص محرّك التشغيل» الموجودة في الإعدادات، وبـ `EngineDecisionCache`/`StreamRouter` كي يصير التوجيه **مبنياً على قياس لا على تخمين**.
- **ملاحظة حاكمة:** كل بنود ضبط المشغّل الأخرى (خيارات ساعة VLC، `network-caching`، توجيه HLS) **غير قابلة للتحقّق بدون هذا البند**. نفّذه قبلها.

### 20. بحث مؤجَّل (debounced) مدعوم بـ FTS داخل كل تبويب — ~80 سطراً · 4 ساعات
- **المرجع:** `ContentViews.swift:102–123 / :1682–1705 / :2063–2078` — إلغاء السابق ← تأجيل 300 مللي ← FTS5 ← سقف 200، بقيادة `.onChange(of: vm.search)`.
- **عندنا:** مساران متزامنان على الخيط الرئيسي، بلا سقف وبلا تأجيل. `searchResults` **خاصية محسوبة** تُقرأ من أجسام العروض مباشرة (`ContentViews.swift:1559`، وتُستهلك في `:257/366/1679/1704/2229/2247`). والأسوأ: `applyFilter()` عند `:885–898` تعيد تنفيذ `S8KSearch.fold($0.name)` **داخل مُرشِّح كل فيلم** — رغم أن `foldedNames` محسوبة مسبقاً عند `:845` ولا تُستعمل هناك.

### 21. حفظ دليل البرامج في SQLite (اطبع من القرص ثم أعِد التحقّق) — ~40 سطراً · 3 ساعات
- **المرجع:** جدول `epg_cache` + `CatalogDB.epgGuide/saveEPG` (`Core.swift:1967–1993`)، يُستهلك بنمط SWR عند `ContentViews.swift:899–917`.
- **عندنا:** `Core.swift:1825–1841` — قاموس في الذاكرة بعمر 300 ثانية، يُمسح عند `reset()` وعند كل إعادة تشغيل. كل إقلاع بارد يعود إلى المزوّد لكل قناة — **وهذا خطر حظر خطّ (fail2ban) على نطاق واسع**.
- الجدول محايد المصدر: نُغذّيه من `get_short_epg` بدل الخادم المركزي. نقل نظيف، ويخدم البند المؤجَّل «دليل البرامج» في `PROJECT_HANDOFF.md §10`.

### 22. تأكيد مهمّة خلفية + تسليم Turbo — ~24 سطراً · ساعة
- **المرجع:** `Strong8KApp.swift:165–186` — فرع `.background` في `scenePhase` يسلّم التحويلات الجارية إلى جلسة `URLSession` الخلفية، ويغلّف الإفراغ عند التعليق بـ `beginBackgroundTask`/`endBackgroundTask`.
- **عندنا:** `BlankTVApp.swift:170–174` **بلا فرع `.background` إطلاقاً**، و`beginBackgroundTask` لا تظهر في التطبيق كلّه. ونحن نُطلق `Task.detached { CatalogDB.save(built, scope:) }` (`Core.swift:1762`) — إدراج 20–50 ألف صف. صغِّر التطبيق أثناءه فيعلّقه النظام.
- **تحفّظان:** `Store.shared.turboDownloads` (`Core.swift:913–916`) لا يُكتب إليه أبداً (لا صفّ إعدادات له)، فـ `runTurbo` شيفرة ميتة حالياً والتسليم كامن؛ ولا تَرِث سلوك «إعادة البدء من البايت صفر» (انظر قائمة الأخطاء).

### 23. تسجيل ذاكرة المحرّك عند **عرضٍ** مؤكَّد لا على مؤقّت ثانيتين — ~15 سطراً · ساعتان
- **المرجع:** يسجّل من داخل مراقب الفيديو، فقط بعد `playerLayer.isReadyForDisplay` (`PlayerEngine.swift:538–544`)، وفقط حين لا يُثبَّت محرّك قسراً (`:865–869`).
- **عندنا:** `PlayerView.swift:270–277` يسجّل عند `t > 2` بعتبة زمنية بحتة — بينما مراقب الفيديو عندنا لا يزال يعدّ إلى 6 ثوانٍ (`PlayerEngine.swift:419`). أي أن قناة سوداء (HEVC داخل TS) **تُخزَّن كقناة AVPlayer ناجحة عند الثانية 2، ثم تُعلَن معطوبة عند الثانية 6**. ولا حارس «لا تسجّل أثناء التثبيت»، فتتلوّث الذاكرة بالمحرّك المثبَّت لكلّ ما شُغّل أثناءه.

### 24. رفع `favorites` خارج أجسام العروض — ~20 سطراً · ساعتان
- **عندنا:** `ContentViews.swift:1579` و`:2136` — `vm.movies.filter { favs.movies.contains($0.id) }` كخاصية محسوبة تُقرأ من جسم الصفحة. و`FavoritesService` هو `@StateObject` (`:1565`) ← **كل تبديل مفضّلة يعيد نشر الصفحة ويمسح الكتالوج كاملاً**. المرجع يحلّها بقراءات مفهرسة من قاعدة البيانات (`ContentViews.swift:1663–1679`).

### 25. حمولات MetricKit تغادر الجهاز — ~40 سطراً · ساعتان · **فقط إن أنشأتَ نقطة نهاية**
- **المرجع:** `Diagnostics.swift:18–57` يرفع الحمولات المخزّنة ولا يحذف الواحدة إلا عند HTTP 200.
- **عندنا:** `Diagnostics.swift:19` هو كل مسار البدء. تُجمَع `MXAppLaunchMetric` و`MXAnimationMetric` (نسبة تلعثم التمرير) و`MXAppExitMetric`، تُقلَّم عند 24 ملفاً، ثم تُهمَل بصمت.
- **لا خادم لدينا** ← تبنَّه كعرض محلي في «الإعدادات ← التشخيص»، أو أجِّله.

---

## الطبقة 3 — عدة أيام · خطر متوسط · أكبر عائد

### 26. إكمال ترحيل التصفيح (keyset paging) على GRDB — 2–3 أيام · **أكبر مكسب ذاكرة في التقرير**
`MoviesVM` عندنا (`ContentViews.swift:817–847`) يحتفظ بالكتالوج كاملاً في `movies` + `filtered` + `grouped` (نسخة كاملة حقيقية لكل بنية داخل مصفوفات لكل قسم، `:836`) + `foldedNames` (`:845`) — ويتكرّر ذلك في `SeriesVM` (`:2034–2058`) و`LiveTVVM` (`:16–45`)، زائد ثلاث مصفوفات كاملة أخرى في `HomeVM` (`HomeView.swift:28–30`). على خطٍّ فيه 176 ألف عنوان: **أربع عمليات إعادة بناء مستقلّة على الخيط الرئيسي في كل تحميل** (`ContentViews.swift:870–875`).
المرجع يضع ذلك كلّه خلف راية `paged`: صفحات 150 صفاً بقراءة خارج الخيط الرئيسي، معاينات مجلدات ≤14 عنصراً، عدّ O(1)، وتعطيل `applyFilter()` (`ContentViews.swift:975–1059, 1638–1679, 1898–1921`).
- **والأساس مبنيّ عندنا بالكامل وبلا مستدعٍ واحد:** `CatalogDB.pageChannels/pageMovies/pageSeries` (`CatalogDB.swift:206–235`)، `countMovies` (`:264`)، `movieCategoryCounts` (`:273`)، `categories` (`:281`)، `isPopulated` (`:170`)، `load(scope:ttl:)` (`:147`). المستدعى فعلياً هو `*ByIds` فقط، ومن `SearchVM` وحده (`ContentViews.swift:3237–3247`). والتعليق عند `CatalogDB.swift:14–15` يقولها صراحةً: *«CatalogDiskCache يبقى المسار الحيّ حتى تُحوَّل الـ VMs (الخطوة 4)»*.
- **هذا إكمالُ ترحيلٍ بدأناه، لا كتابةٌ جديدة.** وهو البند المؤجَّل رقم 6 في `PROJECT_HANDOFF.md §6`.

### 27. الطباعة الفورية من الذاكرة (SWR) مع فكّ خارج الخيط الرئيسي — ~120 سطراً · يوم
- **المرجع:** كل VM يطبع من القرص **قبل** أي شبكة، على مهمّة منفصلة `.userInitiated` (`ContentViews.swift:39–48, 1062–1069`؛ `HomeView.swift:62–79`).
- **عندنا:** `ContentViews.swift:62` و`HomeView.swift:135` تضبط `isLoading = true` ثم تنتظر الشبكة؛ وذاكرة القرص تُخدَم **خلف** سلسلة انتظار `ContentService` وتُفَكّ على مُمثِّل `PlaylistService` لا على مهمّة منفصلة. فالهيكل العظمي يظهر طوال الرحلة كاملةً.
- **خطر متوسط:** يجب ألّا تُمحى بيانات مخزَّنة سليمة بإعادة تحقّق فارغة (المرجع يحرسها عند `:54` و`:1075`) — وهي نفس القاعدة التي تعلّمناها في P7 (`PROJECT_HANDOFF.md §9`).

### 28. جلب صور بنافذة منزلقة مع إلغاء — ~150 سطراً · 1–2 يوم · **جديد: لا يملكه أي من التطبيقين**
كلاهما يُطلق `prefetch(prefix(30))` مرة واحدة من `onAppear` كمهمّات منفصلة بلا مقبض (المرجع `ContentViews.swift:1926`؛ نحن `:1949`). لا شيء يُلغيها حين يتجاوزها المستخدم بسرعة. نافذة مبنية على الفهارس المرئية مع إلغاء = **أكبر مكسب صور غير مأخوذ في القاعدتين**.

### 29. دورة حياة جلسة الصوت — ~80 سطراً · خطر منخفض · **جديد: لا يملكه أي من التطبيقين**
لا أحد منهما يعالج `AVAudioSession.interruptionNotification` (مكالمة/Siri — و**VLC لا يستأنف تلقائياً**)، ولا `routeChangeNotification` (`.oldDeviceUnavailable` ← إيقاف عند نزع السمّاعة؛ السلوك الحالي **يُطلق الصوت من سمّاعة الجهاز** وهو مخالف لإرشادات Apple)، ولا `mediaServicesWereResetNotification` (يصمت التطبيق نهائياً).
**أعلى أثر ملموس للمستخدم لكل سطر في القائمة كلّها.**

### 30. مفوّض استعادة PiP — ~60 سطراً · خطر متوسط
كلاهما يُنشئ المتحكّم ويضبط `canStartPictureInPictureAutomaticallyFromInline` (عندنا `PlayerEngine.swift:305–307`) لكن **لا أحد منهما يضبط `pipController.delegate`** ← زر «استعادة» في نافذة PiP لا يفعل شيئاً، وتنظيف `…DidStopPictureInPicture` مستحيل.

### 31. توجيه HLS-VOD + فحص قدرة صامت — ~60 سطراً · خطر متوسط
تمرير VOD البعيد عبر ABR الأصلي في AVPlayer بدل التدفّق التقدّمي عبر VLC — **أهمّ إصلاح لإعادة التخزين**. المرجع يشترطه بفحص GET نطاقي ≤1 كيلوبايت لمرة واحدة يؤكّد أن المزوّد يخدم HLS-VOD، ويُطلقه من شاشة تصفّح خاملة كي لا ينازع فتحة الاتصال الوحيدة (`PlayerEngine.swift:389–400, 404, 852–858, 884–888`؛ الفحص في `Telemetry.swift:222–252`، واستعمال أنيق لـ `URLSession.bytes` بقطع صارم عند 1 كيلوبايت).
- **عندنا:** `StreamRouter.swift:45–49` يُثبّت السياسة بلا أي مخرج.
- المرجع يُطلقه خلف راية بعيدة لسبب وجيه. لا خادم لدينا ← اجعله مفتاحاً محلياً في الإعدادات، افتراضياً **مطفأ**، **ولا تشحنه قبل البند 19 (القياس)**.

---

## غير قابل للنقل — يعتمد على الخادم بقرارنا

`HostFailover` + دوران المضيفين مع قاطع دائرة (`ActivationService.swift:468–560`، موصول عند `Core.swift:2253–2295` و`PlayerView.swift:513–524`) · مفتاح `force_engine` البعيد (`PlayerEngine.swift:895–899`) · رايات الإطلاق المعتم (`vlc_net_cache`, `vod_engine`, `grid_engine`, `prewarm_next`, `hls_probe`, `live_telemetry`, `catalog_fg_refresh`) · `CentralEPG` / `CentralVOD` / `CatalogCentral` / `CatalogShadow` / `CentralRefresh` (`Core.swift:2877–3436`) · رفع `Diagnostics` · `Telemetry.snapshot()` · بوّابة التفعيل المتفائلة ومهلة 7 أيام دون اتصال (`ActivationService.swift:404–426`).
كلّها معلّقة بـ `strong8k.app` الذي فصلناه عمداً. **لكنّ الأنماط تستحقّ الحفظ**: قاطع الدائرة، والمقابض البعيدة المقيَّدة، والإطلاق المعتم — إن عاد سطحُ تهيئةٍ لاحقاً (وهو ما يفتحه البند 3 في `PROJECT_HANDOFF.md §6`).
وميت في المرجع نفسه — لا تنقله: `CatalogDiskCache.exists()` (`Core.swift:1721–1726`, بلا مستدعٍ) و`prewarm_next` (راية لا تقرؤها أي شيفرة تشغيل).

---

## تصميم — لا تتبنَّه (تحذير صريح)

هذه أفكار **شكلية** في المرجع، وتبنّيها يخالف القاعدة الحاكمة ويخفض نسبة التمايز:
- خلايا ملصقات ثابتة 150pt و`.adaptive(minimum: 104/150)` (`ContentViews.swift:1907/1944`) — تعرض شريطاً أفقياً من ملصق عمودي على iPad؛ نسبتنا 2:3 بمقاس 116/168 هي الصحيحة.
- `ContentTitleBar` بأزرار دائرية و`S8KFont.title1` (`:1117, 1145–1153`).
- بطل ثابت 240pt + نقاط كبسولية + حدّ ذهبي (`HomeView.swift:483–499`).
- وميض رمادي مسطّح / تعبئة هيكل عظمي بـ `Color.s8kElevated` — قِسناه عندنا عند تباين 1.35:1 ورفضناه (`DesignSystem.swift:1531`).
- بطاقات «متابعة المشاهدة» بعرض ثابت 220pt (`HomeView.swift:706–717`).
- شريط `safeAreaInset` العلوي في المرجع (`HomeView.swift:191–205`) نصفه هندسة ونصفه تصميم — قيِّمه، ولا تتبنَّه كما هو.

---

## أفكار سيّئة — لا تنسخها

| # | الخلل | الموقع في المرجع | الحالة عندنا |
|---|---|---|---|
| 1 | **سرّ خلفية مكتوب في الشيفرة** — `appKey = "s8k_1ba20e7bead5716b…"` قابل للاستخراج من أي IPA | `ActivationService.swift:16` | **نحن نظيفون — أبقِنا كذلك** |
| 2 | `Dictionary(uniqueKeysWithValues:)` **تنهار** عند تكرار المفتاح (لوحات IPTV تُعيد نفس `category_id` مرّتين روتينياً) | `Core.swift:2582` | **أصلحناها** بـ `uniquingKeysWith:` (`Core.swift:2057–2061`) |
| 3 | `UIScreen.main.bounds` للتخطيط | `VLCPlayer.swift:416` | **أصلحناها** (`VLCPlayer.swift:396–400` + `s8kWindowSize()`) |
| 4 | فرز على NaN يكسر الترتيب الصارم | `Models.swift:242`, `ContentViews.swift:1088` | **أصلحناها** (`Models.swift:246`) |
| 5 | `ForEach(0..<heroes.count, id: \.self)` | `HomeView.swift:484`, `DesignSystem.swift:830` | **نحن نستعمل نطاقات ثابتة فقط** |
| 6 | إخلاء ذاكرة المحرّك يمسح الخريطة كاملة (`if m.count >= 500 { m = [:] }`) | `Core.swift:950` | **LRU عندنا أفضل** (`EngineDecisionCache.swift:45–54`) |
| 7 | `S8KImage` يُبقي طبقة ThumbHash حيّة للأبد، وبلا حارس إعادة استخدام، وبلا مذكّرة سالبة | `DesignSystem.swift:696–713, 718–733, 635–644` | **أصلحنا الثلاثة** |
| 8 | تحويل `Button` إلى `.onTapGesture` (~28 موضعاً) — **مرفوض**: ادّعاء المرجع أن الزر «يُطلَق عند إعادة البناء» **غير صحيح**؛ SwiftUI يخزّن الإغلاق ولا ينفّذه. والتحويل يكلّف سمة `.isButton` وتفعيل VoiceOver وتركيز Switch Control وحدّ 44pt والإحساس بالضغط | `Downloads.swift:826–834` | **لا تنفّذه.** لكن **انتبه:** في `ContentViews.swift` عندنا **10 تعليقات تدّعي استعمال `onTapGesture`** بينما `grep -c onTapGesture` = 0 ← **احذف التعليقات القديمة** |
| 9 | تسليم Turbo يعيد البدء من البايت صفر (فيلم 4غب عند 90 % ← يُهدَر ~3.6غب، ربما على الشبكة الخلوية) | `Downloads.swift:231–241` | قيِّده بنسبة التقدّم إن نقلتَ البند 22 |
| 10 | `MediaPrefetcher` يحتفظ بمشغّلات `AVPlayer` حيّة بلا عمر افتراضي — `cap = 2` يحدّ **العدد** لا **العمر** | في التطبيقين | **تعرّضنا أسوأ**: نحن نستدعي `prefetch()` فعلاً والمرجع لا يستدعيه. أضِف عمراً ~30ث + `clear()` عند الخلفية/تحذير الذاكرة **قبل** أي عمل آخر على المُسخِّن |
| 11 | `NWPathMonitor` يبدأ عند تهيئة المفرد ولا يُلغى أبداً | `Telemetry.swift:44` | قيِّده أو أسقطه إن نقلتَ البند 19 |
| 12 | فكّ قسري في مسار القياس الساخن + سلسلة بصمة جهاز | `Telemetry.swift:107, 176` | أعِد كتابتهما أو أسقطهما |
| 13 | تهريب مؤشّر من `withCString` إلى libVLC (عمره ينتهي عند القوس) | `VLCPlayer.swift:429–432` | **موجود عندنا حرفياً** عند `VLCPlayer.swift:413–416` — أصلحه عندنا |
| 14 | `AnyView` داخل بانِي خلفية لكل خلية | `HomeView.swift:518–520` | **موجود عندنا** عند `HomeView.swift:461–463` |
| 15 | `Set<String>` غير محدودة في ذاكرة الصور (مدخل لكل رابط، بلا تقليم) | `DesignSystem.swift:575` | **موجود عندنا**: `hashMisses` عند `DesignSystem.swift:1298` |
| 16 | `print()` على مسار تحميل الكتالوج | `HomeView.swift:65/71/77` | **موجود عندنا** عند `HomeView.swift:186/194/201` — غلّفه بـ `#if DEBUG` |

---

## فجوات مشتركة — لا يملكها أي من التطبيقين (بناء جديد، لا نقل)

`preferredPeakBitRate` وتقييد ABR وسقف معدّل البتّ على الشبكة الخلوية · جعل الحرارة/وضع الطاقة المنخفضة **تقود** التشغيل (المرجع **يبلّغ** عن الحرارة فقط، بينما VLC — وهو محرّك VOD الافتراضي في الاثنين — يفكّ برمجياً) · `AVPlayerItemPlaybackStalled` (كلاهما يستعمل مؤقّت ثانيتين بدلاً منه) · مراقبة `loadedTimeRanges` / `isPlaybackBufferFull` · `seekableTimeRanges` والقفز إلى حافة البث المباشر / DVR · `preferredMaximumResolution` · تقييد HDR · `AVQueuePlayer` أو تسخين **الحلقة التالية** (نحن نُسخّن الأفلام فقط، `ContentViews.swift:2571`) · `AVAssetDownloadTask` / تنزيل HLS / FairPlay (كلاهما MP4 تقدّمي فقط ← **VOD بصيغة HLS غير قابل للتنزيل**) · إعادة محاولة التنزيل بتراجع أسّي، وإخلاء LRU، وسقف تخزين، والتحقّق من السلامة، وكنس الملفات الجزئية، وفحص المساحة بحسب الحجم الفعلي (كلاهما يفحص عتبة ثابتة 1غب ولا يقرأ `Content-Length`) · خنق تحديث تقدّم التنزيل (كلاهما يُطلق `Task { @MainActor }` لكل نداء `didWriteData` ويعيد نشر القائمة كاملة) · `isExcludedFromBackup` تُطبَّق عند إنشاء المجلّد فقط · `EquatableView` / `.equatable()` (صفر استعمال في الاثنين) · **ولا أحدَ منهما يحذف نطاقات `CatalogDB` القديمة عند تبديل الحساب**، فتنمو قاعدة SQLite بلا حدّ عبر تبديل قوائم التشغيل.

**وملاحظة مهمّة عن «الحداثة»:** `os.Logger` والـ signposts، و`@Observable`، و`SwiftData`، و`nonisolated(nonsending)` في Swift 6 — **صفر استعمال في التطبيقين**؛ كلاهما على Combine/`ObservableObject`. أي أنّ تبنّي واجهات iOS 17/18/26 الحديثة **فجوة مشتركة لا فرصة نقل** — ولا يوجد في المرجع ما نسرقه هنا. أحدثُ ما فيه فعلاً: `URLSession.bytes` مع قطع مبكر (`Telemetry.swift:233`)، و`NWPathMonitor`، و`ProcessInfo.thermalState`، و`MetricKit`، و`DatabasePool` بوضع WAL.

---

## مواضع نحن متقدّمون فيها — لا تتراجع عنها

راية `isPartial` التي تمنع كتابة كتالوج ناقص فوق ذاكرة سليمة (`Core.swift:1757, 2082`) — أنظف من حارس المرجع · `uniquingKeysWith` · `ratingDouble` الآمن من NaN · `EngineDecisionCache` بنمط LRU · تجزئة `StreamRouter.classify` · `s8kWindowSize()` · `S8KProgressBar` بدل `GeometryReader` لكل صف (`DesignSystem.swift:1476`) · `.scrollTargetBehavior(.paging)` (`HomeView.swift:298`) · `ForEach` بنافذة · مذكّرة ThumbHash السالبة + `hashMemory` محدود التكلفة · حارس `shownURL` لإعادة استخدام الخلية · تصغير عدواني للشعارات والأشرطة · إصلاح الدوران (`PlayerView.swift:87–92`) · `MediaPrefetcher` **موصول فعلاً** (نظيره في المرجع شيفرة ميتة) · `.id(contentGen)` للدخول الفوري بدل شاشة إقلاع حاجزة.

---

## خطة التنفيذ المقترحة (دفعات مطابقة لنمط عملنا: بند لكل بناء، مع مراجعة عدائية)

| الدفعة | البنود | لماذا معاً |
|---|---|---|
| **أ — إصلاح أخطاء حيّة** | 2 · 3 · 5 · 13 · 14 · 15 · 16 | كلها أخطاء مؤكَّدة عندنا، صغيرة، وخطرها منخفض، ولا علاقة لها بالتصميم |
| **ب — الذاكرة والصور** | 6 · 7 · 10 | مسار واحد (الصور/الذاكرة)، ويقلّل احتمال قتل التطبيق |
| **ج — الأمن والثبات** | 9 · 17 · 18 | بيانات الاعتماد + سلامة سجلّ التنزيلات + تزامن قاعدة البيانات |
| **د — القياس** | 19 | **بوّابة إلزامية** قبل أي ضبط تشغيل لاحق |
| **هـ — ضبط المشغّل** | 4 · 8 · 10–16 · 23 · 29 · 30 | بعد أن صار القياس متاحاً للحكم |
| **و — طبقة البيانات** | 1 · 20 · 21 · 24 · 26 · 27 · 28 | أكبر مكسب وأكبر مساحة انفجار — آخِر شيء، وبموافقة صريحة |

**البند 31 (توجيه HLS-VOD) بعد الدفعة «هـ» فقط، خلف مفتاح إعدادات مطفأ افتراضياً.**

> **تنبيه توفيقي بين الجزأين:** الدفعة «هـ» تلمس `PlayerView.swift`، وهو نفسه الإجراء رقم 2 في الجزء الأول (إعادة تصميم المشغّل).
> **نفِّذهما في نفس الجولة**: أعِد بناء طبقة العرض (تمايز) وأنت تُصلح طبقة المحرّك (هندسة) — فتكسب المكسبين ببناءٍ واحد، وتتجنّب مراجعتين متعارضتين على نفس الملف.

---

_انتهى التقرير. جميع القياسات الآلية أُجريت على فرع `main` بتاريخ 2026-07-27، وكل ادّعاء في الجزأين تحقّقتُ منه في شيفرة التطبيقين مباشرةً._
