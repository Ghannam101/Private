# تقرير التمايز — Blank Prime مقابل Strong 8K
**إعادة قياس مستقلّة · 2026-07-29** · _يحلّ محلّ قياس 2026-07-27 (الذي خلص إلى ≈ 45 %)_

> **قيد مطلق التُزم به:** مصدر التطبيق المرجعي `C:\Users\user\Strong8K-App\Strong8K\iOS\` **للقراءة فقط**.
> لم يُكتب ولم يُعدَّل ولم يُحذف فيه شيء. الملف الوحيد المكتوب في هذه الجلسة هو هذا التقرير.

---

## 0. الخلاصة التنفيذية

| السؤال | الجواب |
|---|---|
| **نسبة الاختلاف فيما ينظر إليه مراجع Apple تحت البند 4.3** | **≈ 55 %** (كانت ≈ 45 %) |
| **الحكم** | **حدّي ← مائل إلى الأمان، لكن ثلاثة جيوب لا تزال قاتلة** |
| **أخطر دليل منفرد** | `SplashView` — **أوّل شاشة يراها المستخدم** — لا يزال فرقها الحرفي **صفر سطر** بعد ثلاثة أيام من العمل |
| **ثاني أخطر دليل** | **شاشة المشغّل**: طبقة عناصر التحكّم متطابقة **85.9 %**، وكل عناوين `// MARK:` الخمسة عشر بنفس الاسم ونفس الترتيب |
| **أكبر مكسب تحقّق منذ آخر تقرير** | **النصوص**: العربية المشتركة من 72 % ← **44 %**؛ والمحتوى التجريبي أُعيد بناؤه بالكامل؛ و`AccentColor` صار ليمونياً |
| **الهندسة الداخلية** | **65.2 %** من الأسطر المهمّة متطابقة — **مقصود، مقبول، ولا يُحاسَب عليه** |

**السطر الواحد:** المسار الذي يبدأ من **البوّابة ← الرئيسية ← الأفلام ← المسلسلات ← التفاصيل ← الإعدادات** صار تطبيقاً مختلفاً فعلاً.
المسار الذي يمرّ بـ **شاشة الإطلاق، والمشغّل، والتنزيلات، والرقابة الأبوية، و«حول»** لا يزال نفس التطبيق.

---

## 1. ما تغيّر منذ قياس 2026-07-27

بين التقريرين **22 عملية دفع** (`bf1e469` … `40b5438`، كلّها بتاريخ 2026-07-28). أثرها المقيس:

| البند في خطة التقرير السابق | الحالة اليوم | الدليل |
|---|---|---|
| **1️⃣ إعادة كتابة النصوص العربية** | ✅ **منفّذ جزئياً — أثر كبير** | العربية المتطابقة **55.9 %** بدل ≈ 84 %؛ الإنجليزية **56.7 %**. لكن **الفرنسية والتركية والإسبانية لا تزال 79.6 % متطابقة** |
| **2️⃣ إعادة تصميم شاشة المشغّل** | ❌ **لم يُنفَّذ** | طبقة التحكّم 85.9 % متطابقة (`PlayerView.swift:837–1090` مقابل `ref :827–1055`) |
| **3️⃣ استبدال `ContentCard`** | ❌ **لم يُنفَّذ** | `DesignSystem.swift:1940` لا تزال 118×166 بنفس الشارة ونفس التعليقات؛ حيّة في 4 مواضع (`HomeView.swift:1210, 1215, 1435, 1456`) |
| **4️⃣ استبدال المحتوى التجريبي** | ✅ **منفَّذ بالكامل** | `Core.swift:2403+` — أفلام مختلفة كلياً (Sprite Fright · Cosmos Laundromat · Caminandes · Glass Half) مقابل (Big Buck Bunny · Sintel · Tears of Steel · Elephants Dream) عند `ref Core.swift:3496+`. أوصاف وأقسام وقنوات جديدة |
| **5️⃣ تنظيف الآثار الحرفية** | ⚠ **نصف منفَّذ** | ✅ `AccentColor` صار `srgb(0.796, 1.000, 0.024)` = ليموني · ✅ وسوم `#package` الستة **حُذفت** · ✅ مثال `"strong"` حُذف · ❌ `SplashView` + `PrivacyView` لا يزالان **صفر فرق** · ❌ سلّم الطباعة **لا يزال متطابقاً 100 %** · ❌ `Info.plist` **لا يزال متطابقاً بالبايت** · ❌ `strong8k.app` لا يزال في `Core.swift:525–526` و`ActivationView.swift:14` |
| مفاتيح `UserDefaults` ببادئة `s8k.` | ❌ لم يُنفَّذ | 17 مفتاحاً، 34 موضع استعمال |

**إضافات جديدة تحسب لصالح التمايز** (لم تكن في التقرير السابق): «مبدّل الحسابات» (`AccountSwitcherView`)، و«بوّابة الاشتراكات» (`SubscriptionsGateView`)، و«صفحة تفاصيل» منسدلة (`S8KDetailsSheet`)، وطقم الهوية `S8KBrand` + `brandlint.py` (يجعل إعادة التسمية تعديلاً واحداً).

---

## 2. البنية — جرد الملفات

**عندنا: 25 ملف Swift · 20,748 سطراً — عند المرجع: 19 ملفاً · 19,138 سطراً.**

| عندنا (BlankTV/) | أسطر | | عند المرجع (Strong8K/) | أسطر |
|---|---:|---|---|---:|
| ContentViews.swift | 3611 | | ContentViews.swift | 3787 |
| DesignSystem.swift | 2631 | | Core.swift | 3575 |
| Core.swift | 2494 | | DesignSystem.swift | 1396 |
| HomeView.swift | 1752 | | PlayerView.swift | 1370 |
| SettingsView.swift | 1389 | | SettingsView.swift | 1362 |
| PlayerView.swift | 1380 | | HomeView.swift | 1080 |
| Downloads.swift | 903 | | PlayerEngine.swift | 949 |
| VLCPlayer.swift | 814 | | Downloads.swift | 924 |
| PlayerEngine.swift | 777 | | VLCPlayer.swift | 777 |
| **GatewayView.swift** | **688** | | ThumbHash.swift | 657 |
| Services.swift | 659 | | Services.swift | 610 |
| ThumbHash.swift | 657 | | ActivationService.swift | 560 |
| AuthViews.swift | 654 | | AuthViews.swift | 503 |
| Models.swift | 464 | | Models.swift | 468 |
| ActivationView.swift | 396 | | ActivationView.swift | 407 |
| **CatalogDB.swift** | **366** | | Strong8KApp.swift | 298 |
| BlankTVApp.swift | 336 | | **Telemetry.swift** | **276** |
| **RailEngine.swift** | **207** | | Diagnostics.swift | 98 |
| ActivationService.swift | 154 | | DeviceID.swift | 41 |
| **EngineStats.swift** | 79 | | | |
| **EngineDecisionCache.swift** | 64 | | | |
| MediaPrefetcher.swift | 61 | | | |
| Diagnostics.swift | 60 | | | |
| **StreamRouter.swift** | 50 | | | |
| DeviceID.swift | 41 | | | |

**الأسماء الغليظة = لا مقابل لها في الطرف الآخر.**

### 2.1 الشاشات والميزات الموجودة عند طرف دون الآخر

من جرد `struct`: **107 من 141** بنية عندنا تحمل اسماً موجوداً عند المرجع = **75.9 %**. الباقي:

**عندنا فقط (34):** `GatewayView` (بوّابة جدار الملصقات) · `AccountSwitcherView` (تعدّد الحسابات) · `SubscriptionsGateView` · `UnifiedReorderView` · `HeroCarouselView` · `RankRail` · `SettingsProV2` + `SetScaffold` + `SetConnectionPage` + `SetPlayerPage` + `SetAppPage` + `SetAboutPage` (مركز إعدادات بصفحات فرعية) · `S8KPlinth` + `S8KPlayCapsule` + `S8KSatellite` + `S8KDetailHeading` + `S8KExpandableText` + `S8KDetailsSheet` + `S8KTitleDetails` (مفردات صفحة التفاصيل) · `S8KMetrics` + `S8KMetricsRoot` + `S8KPinnedPageBar` + `S8KSectionBar` (نظام التخطيط) · `S8KShimmer` + `S8KListSkeleton` + `S8KPosterGridSkeleton` + `SkeletonBlock` · `S8KProgressBar` · `S8KNoTouchDelay` · `EngineStatsView` · `BrandTheme` · `StreamClass` · `HomeRail`.

**عند المرجع فقط (21):** طبقة إثراء **TMDB** كاملةً — `CastMember` · `CastPhotoRow` · `ActorWork` · `ActorWorksView` · `DirectorLink` · `PosterCollectionView` · `SimilarPosterRow` · `TMDBAttribution` · `MetaSection` — و**دليل البرامج EPG** — `ChannelGuideView` · `ProgramCardView` · `EPGMeta` — و`ContentBootView` · `S8KSupportChannel` · `S8KSkeleton` · `SkeletonRows` · `SkeletonPosterGrid`.

> **هذا فارق ميزات حقيقي في الاتجاهين**، وهو حجّة مباشرة تحت 4.3: التطبيقان لا يقدّمان الوظائف نفسها. المرجع تطبيق «موسوعي» بطبقة بيانات أفلام خارجية ودليل قنوات؛ Blank Prime مشغّل بواجهة تحريرية وبوّابة دخول وإدارة حسابات متعدّدة.

### 2.2 الهندسة المنقولة عمداً — ولا تُحاسَب

`ThumbHash.swift` (100 % متطابق — خوارزمية MIT عامّة، **يجب** أن تبقى كذلك) · `PlayerEngine` · `VLCPlayer` · `Downloads` (طبقة النقل) · `Services` · `CatalogDB` · `Models` · `MediaPrefetcher`. مجموع الأسطر المهمّة المتطابقة على الشجرة كلّها **65.2 %** (كان 69.6 %).

> Apple **لا تفكّك الثنائيات ولا تقارن الشيفرة المصدرية** بين تطبيقَي مطوّر واحد. البند 4.3 يُطبَّق على **ما يُرى**. مشاركة المحرّك بين تطبيقين لمالك واحد ممارسة صناعية عادية. **انقل الهندسة، لا الشكل.**

---

## 3. ما تقارنه Apple فعلاً — التقييم بالأبعاد والأوزان

### الأوزان المعلنة

| البُعد | الوزن | لماذا هذا الوزن |
|---|---:|---|
| 1 — الهوية البصرية | **25 %** | أوّل 5 ثوانٍ من التصفّح؛ وهو ما يظهر في لقطات App Store المعروضة جنباً إلى جنب |
| 2 — العمارة التنقّلية | **20 %** | «هل يتحرّك التطبيقان بالطريقة نفسها؟» — سؤال المراجع الثاني |
| 3 — تركيب الصفحات | **25 %** | ما يُرى عند التصفّح الفعلي، شاشةً شاشة. أكبر مساحة تعرّض |
| 4 — الدخول والتهيئة | **10 %** | شاشة واحدة، لكنها **إلزامية** لكل مراجع بلا استثناء |
| 5 — مجموعة الميزات | **10 %** | «هل يفعل التطبيقان الشيء نفسه؟» — الحجّة المضادّة الأقوى للمالك |
| 6 — النصوص والعبارات | **10 %** | لا تُلاحَظ إلا عند التدقيق — لكنها **قاطعة** إن لوحظت |

البُعد الهندسي **خارج الحساب عمداً** (انظر §2.2).

---

### البُعد 1 — الهوية البصرية: **58 %** _(كان 52 %)_

**مختلف (دليل):**

| العنصر | Strong 8K | Blank Prime | الحكم |
|---|---|---|---|
| الخلفية الأساسية | `#0A0A0A` أسود — `ref DesignSystem.swift:14` | `#001A0B` أخضر داكن — `DesignSystem.swift:809` | **مختلف تماماً** |
| السطح/البطاقة/المرتفع | `#0E0E10 · #151518 · #1C1C20` — `ref :15–17` | `#04220F · #072B14 · #0B361C` — `:810–812` | مختلف |
| اللون المميّز | ذهبي `#FFD700`/`#C8860A` | **ليموني `#CBFF06`** + تركوازي `#00BC72` — `:813–814` | **مختلف تماماً** |
| **`AccentColor.colorset`** | `srgb(0.784, 0.525, 0.039)` | **`srgb(0.796, 1.000, 0.024)`** | ✅ **أُصلح** — كان الاكتشاف الحرج C1 |
| نصف الأقطار | 6/10/14/18/24/32 — `ref :269–275` | 4/7/10/13/16/22 — `:951–957` | مختلف (~30 % أضيق) |
| أيقونة التطبيق | md5 `ee4d974286` | md5 `3c6e1745e4` | **مختلفة 100 %** |
| صور الشعار (×3) | `88d3bbdc4e · 89a60a6116 · 65be53e672` | `d3bbcee39f · cb12d0e5e7 · 715a14ccf6` | مختلفة |
| اسم العرض / معرّف الحزمة | `Strong 8K` / `com.strong8k.player` | `Blank Prime` / `com.blanktv.player` | مختلف |
| أصول البوّابة | لا وجود لها | 6 ملصقات `gwposter1…6` مجمّعة | ملكية |

**لم يختلف — وهذا ما يخفض الدرجة:**

| العنصر | الدليل | الخطورة |
|---|---|---|
| **سلّم الطباعة متطابق 100 %** | 13 رمزاً بنفس المقاس والوزن حرفياً: 34/black · 28/heavy · 22/bold · 18/bold · 15/semibold · 15 · 14 · 13/semibold · 12 · 11/medium · 10/semibold · 9/bold · mono12 — `DesignSystem.swift:915–932` مقابل `ref :239–253`. الإضافة الوحيدة عندنا هي `field = 16` | **عالية** — الإيقاع الطباعي أوّل ما يُقرأ بصرياً، ولا تُغطّيه الألوان |
| **سلّم المسافات متطابق 100 %** | 4/8/12/16/20/24/32/48 — `DesignSystem.swift:935–944` مقابل `ref :256–265` | عالية |
| **درجات النص المحايد متطابقة** | `white.opacity(0.60/0.35/0.20)` — `:857–859` مقابل `ref :31–33` | متوسطة |
| **`SplashView` — أوّل شاشة — فرقها صفر سطر** | 68 سطراً في الملفين، `diff` = **0** (`AuthViews.swift:10–77`). نفس التسلسل: شعار 132 → علامة كلمة 26 → دوّارة → «معرّف الجهاز» بخط أحادي 13 → `padding(.bottom, 54)` | **حرجة** |
| **`PrivacyView` فرقه صفر سطر · `TermsView` سطران** | `AuthViews.swift` | عالية |
| **نفس رموز SF للتبويبات الخمسة** | `house · dot.radiowaves.left.and.right · film · tv · gearshape` (+ نسخ `.fill`) — `DesignSystem.swift:2062–2075` مقابل `ref :1159–1172` | متوسطة |
| **وصفة العلامة الكلمة نفسها** | `[كلمة محايدة semibold rounded متباعدة] + [كلمة بلون مميّز heavy ×1.02 tracking 0.04] + نفس الظل` — `DesignSystem.swift:1816` مقابل `ref :908`. **تحسّن جزئي**: صرنا نستعمل لوناً صلباً بدل تدرّج، والتباعد 0.24/0.10 بدل 0.34/0.18 | متوسطة (كانت عالية) |
| **رأس `ActivationRequiredView` متطابق** | شعار 96 + ظل + اسم بمقاس 26 وزن black و`tracking(6)` وتعبئة `goldFlat` — `ActivationView.swift:289–297` مقابل `ref :247+` (كتلة 91 سطراً متّصلة) | عالية — **هذه شاشة يراها المراجع على جهاز غير مفعّل** |

---

### البُعد 2 — العمارة التنقّلية: **55 %** _(كان 50 %)_

**مختلف:**
- **شاشة الدخول**: `GatewayView.swift` — **688 سطراً · 0 % استنساخ · صفر كتلة متطابقة**. جدار ملصقات ثلاثي الصفوف يتحرّك في اتجاهات متناوبة بلا انقطاع + بطاقة دخول سفلية + حبّة لغة + مبدّل كبسولي. لا مقابل له في المرجع (`ref` يعرض `LoginView` = نموذج حقول).
- **شريط التبويبات**: عندنا شريط عائم ينكمش إلى «قرص» في الزاوية وحقل بحث يتشكّل من الحبّة نفسها (`DesignSystem.swift:1995+`); المرجع شريط زجاجي عريض ثابت بكبسولة ذهبية متحرّكة (`ref :1174+`).
- **ترتيب التبويبات**: `live, movies, home, series, settings` — الرئيسية في **المنتصف** كزرّ بارز (`DesignSystem.swift:2082`) مقابل `home, live, movies, series, settings` (`ref :1145`).
- **الإعدادات**: عندنا **مركز تنقّل + 4 صفحات فرعية** (`SettingsProV2` + `SetScaffold` + `SetConnectionPage/PlayerPage/AppPage/AboutPage` — `SettingsView.swift:108–535`). المرجع: **قائمة أكورديون واحدة** (`ref SettingsView.swift:8`، 730 سطراً في بنية واحدة).
- **صفحتا التفاصيل**: أُعيد بناؤهما — بلا `NavigationStack`، لوحة فنية مثبّتة + منصّة معتمة + كبسولة تشغيل + أقمار دائرية. القياس: `MovieDetailView` **25.4 %** متطابق فقط، `SeriesDetailView` **12.2 %**.

**لم يختلف:**
- **نفس التبويبات الخمسة، بنفس العناوين، بنفس الأيقونات.** الفرق هو الترتيب والغلاف فقط.
- **نفس جذر التطبيق**: `SplashView { splashDone = true }` ثم بوّابة ثم `TabView` — `BlankTVApp.swift:195/220` مقابل `ref Strong8KApp.swift:194/208`، نفس السطور تقريباً.
- **نفس المسار من الإقلاع إلى التشغيل**: إطلاق ← شاشة إطلاق ← دخول ← تبويبات ← ملصق ← تفاصيل ← تشغيل ← مشغّل ملء الشاشة. هذا الهيكل قياسي في الصنف، لكنه لا يضيف تمايزاً.
- **75.9 %** من أسماء البنى مشتركة.

---

### البُعد 3 — تركيب الصفحات: **50 %** _(كان 40 %)_

قِيست كل شاشة على حدة: نسبة الأسطر المتطابقة (بعد تجريد التعليقات والفراغ) بين البنية عندنا والبنية المسمّاة نفسها في المرجع.

| الشاشة | عندنا | متطابق | الحكم |
|---|---:|---:|---|
| `GatewayView` (الدخول) | 446 سطراً | **0 %** | ✅ ملكية خالصة |
| `SeriesDetailView` | 263 | **12.2 %** | ✅ أُعيد بناؤها |
| `MovieDetailView` | 169 | **25.4 %** | ✅ أُعيد بناؤها |
| `HomeView` (الملف كلّه) | 1335 | 35.1 % | ✅ مختلفة عملياً |
| `SettingsView` (الملف كلّه) | 1234 | 37.0 % | ✅ الغلاف ملكيّ |
| `MoviesView` | 260 | 37.3 % | ✅ الهيكل المرئي مختلف (§3.1) |
| `LiveTVView` | 313 | **55.9 %** | ✅ الأسطر مشتركة لكن **الهيكل المرئي على الهاتف مختلف** (مشغّل مصغّر لاصق) — §3.1 |
| `ParentalControlView` | 176 | **67.6 %** | ⚠ خطر |
| `AddPlaylistView` | 46 | **82.6 %** | ✗ |
| **شاشة المشغّل — طبقة التحكّم** | 205 | **85.9 %** | ✗ **خطر حرج** |
| `PINEntryView` | 93 | **89.2 %** | ✗ |
| `DownloadsView` | 152 | **91.4 %** | ✗ |
| `AboutView` | 47 | **91.5 %** | ✗ |
| `PlaylistsView` | 111 | **93.7 %** | ✗ |
| `LockedCategoriesView` | 110 | **96.4 %** | ✗ |
| `ParentalGate` | 31 | **100 %** | ✗ |
| `DownloadControl` + `DownloadsView` معاً | 228 | **92.5 %** | ✗ |

**المشغّل بالتفصيل** — الملف 87.5 % مستنسخ (940 من 1074 سطراً بعد التجريد، 20 كتلة متّصلة، أطولها **157 سطراً**). و**كل عناوين `// MARK:` الخمسة عشر بنفس الاسم ونفس الترتيب**:
`Episode context` · `Live channel zapping` · `Gesture zones` · `Hold-to-2x speed boost` · `Double-tap seek (±10s, accumulating) + Netflix ripple` · `Side HUD views` · `Screen-lock overlay` · `Skip-intro + auto-next overlays` · `Controls overlay (cinematic, over the video)` · `Orientation` · `Controls auto-hide` · `Sleep timer` · `Subtitle sheet` · `Playback speed sheet` · `Audio track sheet`.
✅ **وسوم `#package` الستة حُذفت** (كانت الاكتشاف C9).

**`ContentCard` لا تزال متطابقة عملياً** — `DesignSystem.swift:1940` مقابل `ref :1028`: نفس 118×166، نفس `S8KRadius.md`، نفس حدّ `s8kBorder`، نفس الشارة (`caption3` أسود `goldFlat` `S8KRadius.xs` `padding(7)`)، نفس `spacing: 7`، نفس `caption1.semibold`/`caption2`، **ونفس التعليقات حرفياً** (`// Image container` · `// Badge` · `// Progress bar` · `// Title` · `// Subtitle`). الفارق الوحيد: `S8KProgressBar` بدل `GeometryReader` داخلي، و`Button` بدل `onTapGesture`. **وهي حيّة في 4 مواضع في الرئيسية** (`HomeView.swift:1210, 1215, 1435, 1456`) — أي في نفس السياق تماماً كالمرجع (`ref HomeView.swift:753, 775`).

**شبكات التصفّح**: نفس النمط (`LazyVGrid` بأعمدة `.adaptive`) بأرقام مختلفة — `116/168 spacing 14` عندنا (`ContentViews.swift:1910`) مقابل `104/150 spacing 12` عند المرجع (`ref :1879`). لكن **خليّة الملصق نفسها مختلفة بنيوياً**: عندنا `.aspectRatio(2.0/3.0, .fit)` على صندوق `Color.clear` (`ContentViews.swift:1978`, `:2434`)، وعند المرجع **ارتفاع ثابت `.frame(height: 150)`** (`ref :2042`, `:2334`)؛ وشارة السنة عندنا رقاقة بتدرّج ذهبي والعنوان `caption2.bold`، وعند المرجع رقاقة سوداء 75 % بنصّ ذهبي والعنوان `caption2.semibold`.

### 3.1 التدقيق البصري المستقلّ — هيكل كل شاشة، من الأعلى إلى الأسفل

دُقّقت الشاشات مرّة ثانية بقراءة يدوية لما **يُرسم فعلاً**، لا بمقياس الأسطر وحده. النتيجة **تصحّح مقياس الأسطر صعوداً** في التصفّح والمباشر، وتؤكّده هبوطاً في المشغّل.

| الشاشة | Blank Prime | Strong 8K | الحكم |
|---|---|---|---|
| **غلاف التبويبات** | قرص عائم في الزاوية ينكمش عند التمرير (`DesignSystem.swift:2192–2249`)؛ عند التوسّع كبسولة أيقونات دائرية `ExpandedNavBar` (`:2378–2438`) بترتيب أفلام · مسلسلات · مباشر · بحث · جرس · رئيسية؛ **البحث يحوّل الشريط نفسه إلى حقل نصّ** (`:2254+`). **الإعدادات ليست تبويباً** بل صورة رمزية أعلى الرئيسية (`HomeView.swift:1055–1070`) | شريط سفلي مصنفر بعرض الشاشة، 5 خلايا أيقونة + عنوان، كبسولة ذهبية تنزلق تحت المحدّد (`ref :1177–1241`) | **أوضح فارق مرئي في التطبيقين** |
| **الرئيسية** | بطل **بلا حواف، دوّار قابل للسحب** بارتفاع محسوب (`HeroCarouselView:262–496`; `metrics.heroHeight` `DesignSystem.swift:172–191`) → شريط الخطأ → متابعة المشاهدة → **شريطا Top-10 برقم مفرّغ 94pt** (`RankRail:503–592`) → أفلام أحدث → مسلسلات أحدث → مباشر → دعم. الشريط العلوي **عنصران** (شعار + صورة رمزية 48pt) | بطل **بطاقة داخلية ثابتة 240pt** بزوايا `S8KRadius.xl` وحدّ ذهبي و`padding` أفقي (`ref :495–500`)، يتلاشى بلا سحب → شريط إعلان → لافتة إدارة 110pt → دعم → **3 بطاقات اختصار زجاجية** → متابعة → مباشر/أفلام/مسلسلات. الشريط العلوي **5+ عناصر** (شعار + 4 أزرار مربّعة 40pt) | **مختلفة** |
| **الأفلام / المسلسلات** | بطل بلا حواف → **صفّ أدوات** (دائرتان ذهبيّتان 44pt + كبسولة `Menu`/`Picker` للفلتر) → Top-10 → أشرطة الأقسام. الهويّة كبسولة مصنفرة عائمة (`S8KSectionBar`). **لا حقل بحث ظاهر** | `ContentTitleBar` (عنوان `title1` + عدّاد) → **حقل بحث ظاهر دائماً** → **شريط 4 كبسولات** (الكل/المفضلة/الأجدد/السجل) → أشرطة الأقسام. **لا بطل ولا Top-10** | **مختلفة** |
| **تفاصيل الفيلم** | بلا `NavigationStack`؛ لوحة فنية ثابتة 44 % من ارتفاع النافذة، والمحتوى **منصّة `S8KPlinth`** بزوايا علوية 28pt تنزلق فوق اللوحة؛ **لا ملصق مصغّر**؛ الإغلاق **قمر زجاجي دائري**. الترتيب: عنوان 30pt أسود → قاعدة ذهبية 52×4 → سطر بيانات بفواصل نقطية → **كبسولة تشغيل بحجم المحتوى + أقمار دائرية 48pt** → القصّة بنصّ قابل للتوسيع → طاقم التمثيل **نصّاً مسطّحاً** | `NavigationStack` + عنوان تنقّل + `xmark` في شريط الأدوات؛ خلفية 270pt مع **ملصق 88×125 متراكب أسفل يمين** + رقاقات `infoTag` محدودة؛ **زرّ `GoldButton` بعرض كامل** + مربّعان مستديران للقلب والتنزيل؛ ثم كتل `MetaSection` (قصّة / طاقم **كرقاقات محدودة أو صور** / رابط المخرج / **«أعمال مشابهة»** / إسناد TMDB) | **مختلفة** |
| **تفاصيل المسلسل** | نفس هيكل اللوحة+المنصّة → **شريط موسم = كبسولة `Menu` واحدة** → قائمة حلقات **مقلوبة عمداً**: رقم 22pt في مزراب 44pt خاص (يتحوّل إلى ✓ عند المشاهدة)، والمصغّرة 112×63 **في الجهة المقابلة**، والتقدّم خطّاً على حافة المصغّرة، **صفوف شفّافة بلا بطاقة** | رأس 256pt + ملصق 84×118 → **كبسولة تبويبين «الحلقات / حول»** → زرّ متابعة بعرض كامل → **شريط كبسولات مواسم أفقي** → صفوف حلقات **بالتشريح المعتاد**: مصغّرة 120×68 في المقدّمة بشارة تشغيل دائرية 32pt فوقها، ثم النصّ، ثم `chevron`، على **بطاقة `s8kSurface`** | **مختلفة** |
| **الإعدادات** | **مركز تنقّل**: توهّج ذهبي علوي → دائرة 84pt بالأحرف الأولى + اسم 23pt + كبسولة الخطّة + «تبديل الحساب ⌄» → **6 مستطيلات داكنة كلٌّ منها `NavigationLink` بأيقونة وعنوان ووصف و`chevron`** → خروج → تذييل | **صفحة مسطّحة واحدة، كل شيء مفرود**: عنوان → بطاقة ملف مربّعة 60pt → قوائم التشغيل → التفعيل → الاشتراك → الخادم → المشغّل → التطبيق → القانوني → خروج → تذييل. **بلا `NavigationLink` إطلاقاً**، كل شيء عبر `.sheet` | **مختلفة** |
| **المباشر** | رأس ثابت لا يتمرّر: كبسولة هويّة → **مشغّل مصغّر 16:9 لاصق يعمل فعلاً** (`InlineLivePlayer`) → **شريط معلومات مصغّر** (قلب · زرّ ملء شاشة دائري ذهبي · اسم القناة بنقطة حمراء · `EPGNowNext`) → ثم يتمرّر صفّ الأدوات والأشرطة. **النقر على قناة يعرضها في المشغّل المصغّر، لا يفتح ملء الشاشة** | `ContentTitleBar` → حقل بحث → شريط كبسولات → أشرطة. **النقر على قناة يفتح المشغّل ملء الشاشة فوراً**. وللمرجع `ChannelGuideView` + `ProgramCardView` (جدول EPG كامل مع تذكيرات) لا مقابل له عندنا | **مختلفة على الهاتف** (والانقسام الثلاثي على iPad **متطابق**) |
| **المشغّل** | — | — | **نفس الشاشة** (انظر أدناه) |

**المشغّل بالتدقيق البصري — هذا هو الموضع الأسوأ في التطبيق كلّه.** ترتيب `ZStack` للجسم متطابق عنصراً بعنصر (`PlayerView.swift:334–511` مقابل `ref :321–490`)، و:
- **الشريط العلوي**: `chevron.down` · العنوان (+ نقطة LIVE حمراء) · فاصل · AirPlay · PiP · قفل · تدوير — **نفس الترتيب ونفس `padding(.top, isLandscape ? md : 50)`**.
- **صفّ النقل الأوسط**: `HStack(spacing: 36)` = سابق · `gobackward.10` · تشغيل/إيقاف · `goforward.10` · تالٍ — **نفس القيم ونفس الشروط**.
- **الأسفل**: رقاقة النوم → `Slider` بلون `.s8kGoldHigh` بنفس شيفرة السحب والاهتزاز والمواضع الثابتة وبنفس **فقاعة الوقت العائمة عند `position(y: -32)`** → المنقضي/المدّة → **صفّ الرقائق `HStack(spacing: 6)`**: كتم · مسار صوتي · ترجمة · سرعة · نسبة · نوم.
- **HUD الصوت والسطوع متطابق حرفياً** (`:696–706` مقابل `ref :695–705`): لوحة سوداء 55 % بحشو 16pt، أيقونة 24pt، **مسار كبسولي عمودي 5×110 بتعبئة ذهبية**، الصوت يساراً والسطوع يميناً، `padding(.horizontal, 30)`.
- **الصفائح الأربع بنفس الترتيب ونفس أسلوب العرض** (`:562–565` مقابل `ref :564–567`).

**الفروق الوحيدة في المشغّل:** زرّ التشغيل (دائرة 66pt بتدرّج + رمز 27pt عندنا مقابل `play.circle.fill` 64pt عند المرجع) · تدرّج التعتيم عندنا `.allowsHitTesting(false)` · المرجع يملك صفيحة خامسة (`ChannelGuideView`) ورقاقة «دليل البرامج» · خطّ الوقت. **هذا كلّ شيء.**

### 3.2 شيفرة مستنسخة **ميّتة** — تُحذف مجّاناً

سِتّ بنى عرض في شجرتنا **معلَنة ولا تُرسم أبداً** (صفر موضع استدعاء)، وكلّها منقولة من المرجع. لا تظهر في أي لقطة، فلا تفيد التمايز؛ لكنها **موجودة في المصدر كدليل نسب**:
`HomeView.navBar` (`HomeView.swift:925`) · `HomeView.announcementBar` (`:1074`) · `HomeView.bannerSection` (`:1096`) · `HomeView.railsSection` (`:1192`) · `featuredBanner` في `MoviesView` (`ContentViews.swift:1806`) و`SeriesListView` (`:2338`) · `ContentTabBar` (صفر استدعاء).
> **ملاحظتان:** (أ) شريط الإعلان ولافتة الإدارة كانا الاكتشاف «متطابق بايتاً ببايت» في التقرير السابق — تبيّن الآن أنهما **ميّتان عندنا وحيّان عند المرجع**، أي أن الخطر أقلّ ممّا قُدِّر، والحلّ **حذف** لا إعادة تصميم. (ب) لا تستشهد بهذه البنى كعناصر تمايز في أي ردّ على App Review — فهي لا تُرسم.

---

### البُعد 4 — الدخول والتهيئة: **68 %** _(كان ضمن البُعد 2)_

**مختلف:** البوّابة كاملةً (688 سطراً، 0 % استنساخ)، و`SubscriptionsGateView` (لا مقابل له)، و«الدخول الفوري» بلا شاشة تحميل، ومبدّل الحسابات.
**لم يختلف:** `SplashView` (**صفر فرق** — وهي الشاشة التي تسبق البوّابة وتراها الأعين أولاً)، وكتلة حقول `LoginView` (34 سطراً متطابقاً — `AuthViews.swift:127` مقابل `ref :135`)، ورأس `ActivationRequiredView`، وشاشات `MaintenanceView`/`UpdateRequiredView`/`ActivationCheckingView`/`TrialBanner`/`LoadingDots` — **نفس السبع بنى، بنفس الأسماء، بنفس الترتيب** في `ActivationView.swift`.

---

### البُعد 5 — مجموعة الميزات: **60 %** _(جديد)_

**عند المرجع ولا نملكه:** طبقة **TMDB** كاملةً (شريط صور طاقم التمثيل، أعمال الممثّل، أفلام مشابهة، مجموعات، إسناد TMDB الإلزامي) · **دليل برامج EPG** بشاشة قنوات وبطاقات برامج غنيّة · `MetaSection` · `HostFailover` وتدوير المضيفين · طبقة `Telemetry` (276 سطراً) · الخادم المركزي (`CentralEPG`/`CentralVOD`/`CatalogCentral`).

**عندنا ولا يملكه:** بوّابة جدار الملصقات · **تعدّد الحسابات** مع مبدّل · بوّابة الاشتراكات · ترتيب موحّد للأقسام عبر الأنواع الثلاثة (`UnifiedReorderView`) · دوّار البطل (`HeroCarouselView`) · شريط المراتب (`RankRail`) · صفحة تشخيص محرّك التشغيل (`EngineStatsView`) · صفحة تفاصيل منسدلة (`S8KDetailsSheet`) · مخزن `CatalogDB` بـ GRDB/FTS5 · نظام تخطيط `S8KMetrics` · طقم هوية `S8KBrand` قابل لإعادة العلامة.

هذا **أقوى ما يملكه المالك من حجّة** أمام 4.3: تطبيقان لا يقدّمان الوظائف نفسها ولا يخدمان الحاجة نفسها.

---

### البُعد 6 — النصوص والعبارات: **45 %** _(كان 28 %)_

قياس على قاموس `L10n.table` مباشرةً (`Core.swift`) — 372 مفتاحاً عندنا، 402 عند المرجع، **298 مفتاحاً مشتركاً**:

| اللغة | عدد نصوصنا | متطابقة حرفياً مع المرجع | النسبة |
|---|---:|---:|---:|
| **العربية** | 372 | 208 | **55.9 %** |
| **الإنجليزية** | 372 | 211 | **56.7 %** |
| **الفرنسية** | 372 | 296 | **79.6 %** ⚠ |
| **التركية** | 372 | 295 | **79.3 %** ⚠ |
| **الإسبانية** | 372 | 296 | **79.6 %** ⚠ |

**التحسّن حقيقي في العربية والإنجليزية** (كانتا ≈ 84 % و82 %). لكن **جولة إعادة الكتابة لم تلمس الفرنسية والتركية والإسبانية إطلاقاً** — من يبدّل اللغة إلى أيّ منها يرى تطبيقاً متطابقاً نصياً بنسبة 80 %.

**27 نصاً عربياً طويلاً (>30 حرفاً) لا يزال متطابقاً حرفياً**، منها ما يظهر في مسارات يسلكها المراجع:
`alert.delete.msg` («سيُحذف حسابك وجميع بياناتك نهائياً…») · `pc.enable_hint` · `pc.recovery_hint` · `refresh.msg` · `home.content_error.sub` · `error.server_rejected` · `player.err.failed` · `player.err.start_failed` · `play.subtitle.empty.sub` · `play.audio_track.empty.sub` · `downloads.empty.sub` · `downloads.notif.denied`.

و**66 نصاً عربياً حرفياً مشتركاً** خارج القاموس، أغلبها مفردات صنفية لا مفرّ منها (`الرئيسية`، `أفلام`، `الكل`، `خطأ`)، لكن منها أربعة قابلة للتغيير فوراً:
`"أدخل رابط السيرفر (مثال: http://server.com:8080)"` · `"رابط قائمة التشغيل غير صالح — يجب أن يبدأ بـ http"` · `"الملف ليس قائمة M3U صالحة — جرّب إضافة type=m3u_plus للرابط"` · `"هذا الجهاز لا يدعم تشغيل التطبيق لأسباب أمنية"`.

**و`Info.plist` لا يزال متطابقاً بالبايت** (32 سطراً) بما فيه النص العربي `«للتواصل مع أجهزة AirPlay على نفس الشبكة»` — وهو نصّ **يظهر في تنبيه إذن نظامي** يقرؤه المستخدم والمراجع.

---

## 4. الرقم الواحد

| البُعد | الدرجة | الوزن | المساهمة |
|---|---:|---:|---:|
| 1 — الهوية البصرية | 58 % | 25 % | 14.5 |
| 2 — العمارة التنقّلية | 55 % | 20 % | 11.0 |
| 3 — تركيب الصفحات | 50 % | 25 % | 12.5 |
| 4 — الدخول والتهيئة | 68 % | 10 % | 6.8 |
| 5 — مجموعة الميزات | 60 % | 10 % | 6.0 |
| 6 — النصوص والعبارات | 45 % | 10 % | 4.5 |
| **المجموع الموزون** | | **100 %** | **≈ 55.3 %** |

# **نسبة التمايز = ≈ 55 %** _(كانت ≈ 45 %)_

**سلّم القراءة:** 0 % = نسخة طبق الأصل · 100 % = تطبيقان لا علاقة بينهما.
`55 %` تعني: **الجلد بُدِّل على معظم مساحة التطبيق؛ والهيكل العظمي لا يزال ظاهراً من تحته في أربعة مواضع.**

---

## 5. التقييم الصريح للمخاطر — أين سيقول المراجع «هذا نفس التطبيق»؟

نُرتَّب بترتيب **(احتمال أن يراه المراجع) × (قوّة الدليل)**.

### 🔴 خطر 1 — شاشة الإطلاق `SplashView` · **فرقها صفر سطر · يراها 100 % من المراجعين**
`AuthViews.swift:10–77` مقابل `ref AuthViews.swift:10–77`: **68 سطراً، `diff` = 0**.
نفس التركيب بالضبط: شعار بمقاس 132 يتلاشى ويكبر من 0.88 → شعار كلمة بمقاس 26 → فاصل → دوّارة دائرية بمقياس 0.9 → عنوان «معرّف الجهاز» بمقاس caption2 → المعرّف بخط أحادي 13pt عريض قابل للتحديد → `padding(.bottom, 54)`. حتى **توقيتات الحركة نفسها**: 0.7 / 0.25 / 0.5 / 0.32 / 0.25.
التطبيقان يستعملان `UILaunchScreen_Generation = YES` — أي أن **الشاشة السوداء المولَّدة ثم `SplashView`** هي كامل تجربة الإطلاق. الفرق الوحيد الذي يراه المستخدم هو صورة الشعار ولون الدوّارة.
**هذا أوّل ما يوضع في لقطتَين جنباً إلى جنب.**

### 🔴 خطر 2 — شاشة المشغّل · **85.9 % من طبقة التحكّم متطابقة · يفتحها كل مراجع**
المشغّل هو **الشاشة الأطول عمراً في أي تطبيق IPTV**، ولا يمكن مراجعة التطبيق دون فتحه.
`PlayerView.swift:837–1090` مقابل `ref :827–1055` = 176 من 205 أسطر متطابقة. وكل الطبقة: 20 كتلة متّصلة ≥10 أسطر، أطولها 157 سطراً (مناطق الإيماءات)، و123 سطراً (طبقة التحكّم العلوية)، و127 سطراً (المؤقّت والترجمات).
**ما يراه المراجع متطابقاً** (تدقيق بصري مستقلّ، §3.1): ترتيب طبقات `ZStack` للجسم عنصراً بعنصر · ترتيب أزرار الشريط العلوي السبعة · `HStack(spacing: 36)` لصفّ النقل الخماسي · شريط التقدّم بلونه وفقاعة وقته العائمة عند `y: -32` · **HUD الصوت والسطوع متطابق حرفياً** (لوحة سوداء 55 %، أيقونة 24pt، مسار كبسولي عمودي **5×110** بتعبئة ذهبية، `padding(.horizontal, 30)`) · صفّ الرقائق السداسي بترتيبه · الصفائح الأربع بترتيبها وأسلوب عرضها · تراكب القفل · وميض «تخطّي المقدّمة» و«الحلقة التالية» · نبضة النقر المزدوج.
**الفروق الوحيدة:** زرّ التشغيل (دائرة 66pt بتدرّج عندنا مقابل `play.circle.fill` 64pt)، وتعتيم لا يلتقط اللمس، وغياب صفيحة «دليل البرامج» عندنا، وخطّ الوقت. **أربعة فروق، مقابل خمس عشرة نقطة تطابق.**

### 🟠 خطر 3 — النصوص في الفرنسية والتركية والإسبانية · **79.6 % متطابقة**
تبديل اللغة زرّ واحد في الإعدادات، وموجود في **البوّابة نفسها**. جولة إعادة الكتابة غطّت العربية والإنجليزية فقط. من يقرأ أيّ من الثلاث الأخرى يرى **296 من 372 نصاً متطابقاً حرفياً** — بما فيها فقرات كاملة.

### 🟠 خطر 4 — سلّما الطباعة والمسافات متطابقان 100 %
`DesignSystem.swift:915–944` مقابل `ref :239–265`. 13 رمز خط + 8 مسافات، **قيمة بقيمة، وزناً بوزن**. الألوان تتغيّر؛ **الإيقاع البصري لا**. عين المراجع تقرأ الإيقاع قبل أن تسمّي اللون: نفس ارتفاعات العناوين، نفس التسلسل الهرمي، نفس كثافة الصفحة.

### 🟠 خطر 5 — الشاشات الثانوية المستنسخة بالكامل
`ParentalGate` **100 %** · `LockedCategoriesView` **96.4 %** · `PlaylistsView` **93.7 %** · `AboutView` **91.5 %** · `DownloadsView` **91.4 %** · `PINEntryView` **89.2 %**.
«حول» و«التنزيلات» و«الرقابة الأبوية» كلها على بعد نقرة واحدة من الإعدادات، وكلها في مسار مراجعة تفصيلية معتادة.

### 🟡 خطر 6 — `ContentCard` والرئيسية
العنصر البصري الأكثر تكراراً في التطبيق كلّه، متطابق عملياً بما في ذلك تعليقاته، وحيّ في 4 مواضع.

### 🟡 خطر 7 — آثار العلامة الأخرى داخل ثنائي Blank Prime
- `Core.swift:525–526` — `"https://strong8k.app/api/v1"` (primary **و** fallback).
- `ActivationView.swift:14` — `static let reportEmail = "report@strong8k.app"` — **ثابت ميّت عندنا** (لا مستدعي)، ولا يزال يشحن اسم النطاق الآخر.
- **`Info.plist` متطابق بالبايت**، بما فيه نصّ إذن AirPlay العربي.
- **17 مفتاح `UserDefaults` ببادئة `s8k.`** في 34 موضعاً — يظهر عند فحص وقت التشغيل لا في الشيفرة فقط.
- **39 معرّف نوع ببادئة `S8K`** في رموز الثنائي (أولوية منخفضة: غير مرئية للمراجع).

### 🟢 ما صار آمناً فعلاً (تأكيد إيجابي)
البوّابة · صفحتا التفاصيل · مركز الإعدادات · شريط التبويبات · اللوحة اللونية · `AccentColor` · أيقونة التطبيق والشعارات · المحتوى التجريبي · وسوم `#package` · مثال `"strong"` · اسم العرض ومعرّف الحزمة · طبقة TMDB/EPG الغائبة عندنا مقابل ميزاتنا الملكية.

---

## 6. ما يحرّك الرقم أكثر — مرتّباً بـ (الأثر ÷ التكلفة)

### 1️⃣ إعادة بناء `SplashView` + `PrivacyView` + `TermsView` — **+4 نقاط · ساعتان · صفر خطر هندسي**
**الملف:** `BlankTV/AuthViews.swift:10–77` (وما يليه).
**ماذا تفعل:** أعِد تركيب شاشة الإطلاق من الصفر بمفردات BLANK الموجودة أصلاً — لا تُعِد ترتيب نفس القطع. مقترح ملموس بلا أي أصل جديد: خلفية `s8kBlack` + **شريط ليموني رفيع يتقدّم عبر عرض الشاشة** بدل الدوّارة الدائرية؛ العلامة الكلمة **محاذاة إلى الحافة السفلية اليسرى** لا في المنتصف؛ معرّف الجهاز يُنقل إلى الإعدادات ويُحذف من شاشة الإطلاق كلياً (هو معلومة تشخيصية لا شاشة ترحيب)؛ الشعار بمقاس مختلف (96 لا 132) وبلا تكبير من 0.88.
**لماذا أولاً:** أعلى نسبة (أثر ÷ جهد) في التقرير كلّه — 68 سطراً في ملف واحد، بلا مسار حرج، مقابل إغلاق **الدليل الأول** الذي سيقع عليه المراجع.

### 2️⃣ إعادة كتابة الفرنسية والتركية والإسبانية — **+3 نقاط · 3 ساعات · صفر خطر**
**الملف:** `BlankTV/Core.swift` — `L10n.table`، الحقول `.fr` و`.tr` و`.es` في المفاتيح الـ 298 المشتركة.
**ماذا تفعل:** أعِد صياغة القيم الثلاث لكل مفتاح تغيّرت فيه `.ar`/`.en` أصلاً — الترجمة موجودة، المطلوب صوت تحريري مختلف لا معنى مختلف. ابدأ بالـ 27 نصّاً الطويلة المذكورة في §3/البُعد 6.
**لماذا:** جولة العمل السابقة تركت **ثلاثة أخماس اللغات المشحونة** على 79.6 % تطابق. إتمامها يرفع البُعد 6 من 45 % إلى ≈ 62 %.

### 3️⃣ إزاحة سلّمي الطباعة والمسافات — **+3 نقاط · ساعة + جولة تحقّق على الجهاز**
**الملف:** `BlankTV/DesignSystem.swift:915–944` — ملفّ واحد، تعريفان.
**ماذا تفعل:** أزِح السلّم إزاحة ملموسة، مثلاً: `display 40/black · title1 30/black · title2 24/heavy · title3 19/bold · headline 16/semibold · body 15 · callout 13.5 · subhead 13/bold · caption1 11.5/medium · caption2 10.5 · caption3 9`، والمسافات إلى `5/9/13/18/22/28/36/56`.
**لماذا:** التغيير محصور في تعريفين لأن **كل الشيفرة تقرأ الرموز لا الأرقام** (وقد أتمّت ذلك دفعة `c7e3c34`). أثره يمتدّ إلى **كل شاشة في التطبيق** — أوسع أثر لكل سطر في القائمة.
**تحذير:** هذا يمسّ مظهراً معتمداً ويغيّر ارتفاعات الصفوف. يحتاج موافقتك واختباراً على الجهاز وفق `DEVICE_MATRIX.md §6`. نفّذه في بناء منفرد.

### 4️⃣ تنظيف آثار العلامة الأخرى — **+2 نقطة · 45 دقيقة · صفر خطر**
- `BlankTV/Info.plist` → أعِد كتابة `NSLocalNetworkUsageDescription` بنصّ خاص بنا (مثلاً «لاكتشاف أجهزة العرض على شبكتك المحلية») وأعِد ترتيب المفاتيح. **سطر واحد يُنهي تطابقاً بايتاً ببايت في ملف يقرأه المراجع الآلي.**
- `BlankTV/Core.swift:525–526` → استبدل `strong8k.app` بنطاق Blank (أو احذف `APIConfig` كلياً — التعليق نفسه يقول إن المسار لم يعد مستعملاً، فهو **شيفرة ميّتة تشحن اسم العلامة الأخرى**).
- `BlankTV/ActivationView.swift:14` → `reportEmail` ثابت ميّت بنطاق المرجع. احذفه أو غيّره.
- `BlankTV/Core.swift` + `ActivationService.swift:98` + `ContentViews.swift` → بادئة `s8k.` في 17 مفتاح `UserDefaults` (34 موضعاً) → `blank.` مع **ترحيل صامت لمرة واحدة** (لا تكسر بيانات المستخدمين الحاليين).

### 5️⃣ حذف الشيفرة المستنسخة الميّتة — **+1 نقطة · 20 دقيقة · صفر خطر · أرخص بند في التقرير**
**الملفات:** `HomeView.swift:925` (`navBar`) · `:1074` (`announcementBar`) · `:1096` (`bannerSection`) · `:1192` (`railsSection`) · `ContentViews.swift:1806` و`:2338` (`featuredBanner`) · `ContentTabBar` (صفر استدعاء).
**ماذا تفعل:** احذفها. ستّ بنى عرض منقولة من المرجع، **لا تُرسم أبداً**. لا تُكسب المستخدم شيئاً وتُخسر الحجّة أمام أي تدقيق مصدري. (`announcementBar` و`bannerSection` تحديداً كانا الاكتشاف «متطابق بايتاً ببايت» في التقرير السابق — الحلّ الآن حذف، لا إعادة تصميم.)

### 6️⃣ استبدال `ContentCard` وشارة `ChannelRow` — **+3 نقاط · 3 ساعات · خطر منخفض**
**الملف:** `BlankTV/DesignSystem.swift:1940` (4 مواضع استدعاء فقط: `HomeView.swift:1210, 1215, 1435, 1456`).
**ماذا تفعل:** غادر مقاس 118×166 ونسبته 1:1.41 (وهو مقاس المرجع). اعتمد نسبة 2:3 الحقيقية (`112×168`) مع `S8KRadius.sm`؛ انقل **العنوان خارج البطاقة بمحاذاة الحافة** بدل تحته بمحاذاة `trailing`؛ اجعل الشارة **نصاً مسطّحاً على أرضية معتمة** لا مستطيلاً بتدرّج؛ واجعل شريط التقدّم **خطاً عند الحافة السفلية للملصق** بلا حاوية. واحذف التعليقات الخمسة المنسوخة.

### 7️⃣ إعادة تصميم طبقة عرض المشغّل — **+7 نقاط · 10–14 ساعة على 3–4 بناءات · خطر متوسط**
**الملف:** `BlankTV/PlayerView.swift` (1380 سطراً؛ 87.5 % منها مستنسخ).
**ماذا تفعل — ولا تلمس المحرّك:** المنطق والإيماءات هندسة، تبقى. غيّر **ما يُرسم** فقط:
- **طبقة التحكّم** (`:837–1090`): انقل الصفّ السفلي الكلاسيكي إلى مفردات BLANK نفسها المستعملة في صفحة التفاصيل — كبسولة بحجم المحتوى + أقمار دائرية 48pt.
- **شريط التقدّم**: قاعدة رفيعة بلغة `S8KPlayCapsule` بدل `Slider` المعياري.
- **HUD الصوت والسطوع** (`:695`): المرجع عمود عمودي في الوسط ← اجعلها **شارة أفقية صغيرة أعلى الحافة**.
- **الصفائح الأربع** (`:1159 / :1271 / :1342 / :1380`): أعِد بناءها على `SetScaffold` الذي تملكه أصلاً في الإعدادات V2.
- **احذف/أعِد صياغة عناوين `// MARK:` الخمسة عشر** — ترتيبها الحرفي نفسه دليل نسب مستقلّ عن البكسلات.
**لماذا هو السابع لا الأول:** أعلى أثر مفرد في التقرير، لكنه أيضاً أغلى بند وأخطره (المشغّل على المسار الحرج، ولا تحقّق بصري إلا على الجهاز). نفّذه **على مراحل، طبقة لكل بناء**، بنفس نمط «العرض المعزول» الذي نجح مع البوّابة.
**وفّق بينه وبين الهندسة:** الدفعة «هـ» في §الجزء الثاني من التقرير السابق تلمس نفس الملف. نفّذ الاثنين في جولة واحدة.

### 8️⃣ الشاشات الثانوية المستنسخة — **+2 نقطة · 5–6 ساعات · خطر منخفض**
بالترتيب: `ParentalGate` (100 %) · `LockedCategoriesView` (96.4 %) · `PlaylistsView` (93.7 %) · `AboutView` (91.5 %) · `DownloadsView` (91.4 %) · `PINEntryView` (89.2 %) · `AddPlaylistView` (82.6 %).
**ماذا تفعل:** كلها قوائم/نماذج. أعِد بناءها على `SetScaffold` الذي تملكه (`SettingsView.swift:329`) — رأس موحّد + صفوف بلغة BLANK. هذا **إعادة استعمال مكوّن قائم**، لا تصميم جديد، فالتكلفة تقريباً تكلفة النقل.

### 9️⃣ رأس بوّابات التفعيل — **+1 نقطة · ساعة**
`ActivationView.swift:289–297` — الشعار 96 + `tracking(6)` + `goldFlat` هو رأس المرجع بعينه، وهو **الشاشة التي يراها المراجع على جهاز غير مفعّل**. استبدله بالعلامة الكلمة `S8KWordmark` التي تملكها، بلا `tracking` مضخّم وبلا تدرّج.

### 🔟 أيقونات التبويبات — **+1 نقطة · 20 دقيقة**
`DesignSystem.swift:2062–2075` — الخمسة رموز نفسها. بدائل بلا أي أصل جديد: `waveform` أو `antenna.radiowaves.left.and.right` للمباشر · `popcorn` أو `movieclapper` للأفلام · `rectangle.stack` أو `sparkles.tv` للمسلسلات · `slider.horizontal.3` للإعدادات · `square.grid.2x2` للرئيسية.

---

## 7. «يحتاج المالك» — ما لا يمكن إغلاقه بالهندسة وحدها

| البند | لماذا يحتاجك | ما المطلوب منك بالضبط |
|---|---|---|
| **إزاحة سلّم الطباعة (البند 3)** | يغيّر مظهراً **معتمداً** على كل شاشة | موافقة صريحة + جولة نظر على الجهاز بعد البناء |
| **إعادة تصميم المشغّل (البند 7)** | قرار تصميم لا تنظيف | موافقة على الاتجاه (كبسولة + أقمار مثل صفحة التفاصيل) قبل كتابة أي سطر |
| **نطاق خادم خاص بـ Blank** | لا نملك بديلاً عن `strong8k.app` | نطاقاً (أو قرار حذف `APIConfig` نهائياً — وهو **الأرجح**: الشيفرة ميتة أصلاً) |
| **بريد بلاغات خاص** | `report@strong8k.app` | عنواناً بديلاً، أو موافقة على حذف الثابت |
| **لقطات App Store** | لا شيء في الشيفرة يضبطها | لا تُصوّر: شاشة الإطلاق، ولا شاشة المشغّل، ولا التنزيلات، ولا «حول». صوّر: **البوّابة، والرئيسية بالبطل والأشرطة، وصفحة التفاصيل، ومركز الإعدادات، ومبدّل الحسابات**. هذه الخمس هي التي تقرأ كتطبيق مستقلّ — والباقي هو ما يقرأ كنسخة |

**لا يحتاج المالك:** كل بقية القائمة. لا تحتاج فنّاً جديداً ولا هوية جديدة — الأصول والمفردات (`S8KPlinth`, `S8KPlayCapsule`, `S8KSatellite`, `SetScaffold`, `S8KWordmark`, `BrandTheme`) **مبنيّة عندك أصلاً وغير مستعملة في هذه المواضع**.

---

## 8. الأثر المتوقّع

| بعد تنفيذ | النسبة المتوقّعة | الحكم |
|---|---|---|
| اليوم | **≈ 55 %** | حدّي — مائل إلى الأمان |
| البنود 1 + 2 + 4 + 5 (يوم عمل واحد، صفر خطر، بلا موافقة تصميم) | **≈ 64 %** | مقبول |
| + البنود 3 + 6 + 8 + 9 + 🔟 | **≈ 71 %** | آمن |
| + البند 7 (المشغّل) | **≈ 78 %** | **«تطبيقان مختلفان» بلا جدال** |

---

## 9. ملاحظات على الحجّة أمام App Review

- **معرّف الحزمة واسم العرض مختلفان بالفعل** (`com.blanktv.player` / `Blank Prime` مقابل `com.strong8k.player` / `Strong 8K`). هذا شرط لازم غير كافٍ — البند 4.3 لا يسأل عن المعرّف بل عن **التجربة**.
- **المرجع منشور على App Store بالفعل.** هذا يعني أن المقارنة **متاحة للمراجع بضغطة واحدة**، وأن الحجّة يجب أن تصمد أمام مقارنة مباشرة لا أمام ذاكرة.
- **أقوى ورقة بيدك ليست الشكل بل الوظيفة** (§البُعد 5): المرجع تطبيق بطبقة بيانات أفلام خارجية (TMDB) ودليل قنوات؛ Blank Prime مشغّل بواجهة تحريرية وبوّابة دخول وإدارة حسابات متعدّدة. **اكتب هذا صراحةً في ملاحظات المراجعة** (App Review Notes) — لا تترك المراجع يكتشفه.
- **مشاركة المحرّك بين تطبيقين لمالك واحد ليست مخالفة.** لا تخفِها ولا تعتذر عنها؛ 4.3 يقيس ما يُرى.

---

_انتهى. كل قياس في هذا التقرير أُجري آلياً على فرع `main` عند `40b5438` بتاريخ 2026-07-29، وكل ادّعاء تحقّقتُ منه في شيفرة التطبيقين مباشرةً. لم يُلمس مصدر التطبيق المرجعي بأي كتابة._
