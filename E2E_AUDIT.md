# تدقيق من طرف إلى طرف — التطبيق واللوحة

**التاريخ:** 2026-08-21 · **`main`** @ `0a05d4d` · **146 اختباراً خضراء**

> **تحديث الحالة — 2026-08-21 (لاحق)، بعد خمس دمجات، 158 تعريف اختبار.**
> جدول §2 أدناه **مؤرَّخ**؛ ما أُغلق منه بعد كتابته مُعلَّم في مكانه، والأصل باقٍ كما كُتب — تدقيق يُعاد كتابته ليطابق الحاضر يفقد قيمته كسجلّ. التفاصيل في §5.

## المنهجية

كل سطر مأخوذ من فحص فعليّ: الشيفرة، أو سجلّ CI، أو **App Store Connect عبر واجهته البرمجية**، أو **طلب HTTP حيّ إلى `panel.8k.site`**. حيث احتجتُ حكماً على قاعدة من Apple، بحثتُ عنها وذكرتُ ما وجدته. **لا شيء هنا مبنيّ على انطباع.**

---

## 0 · القرار المعماري — حُسم قبل أي شيء آخر

في المستودع قراران مسجَّلان بدَوَا متناقضَين:

| التاريخ | القرار |
|---|---|
| 2026-07-22 | **افصل كل شيء** — التطبيق يتحدث إلى مزوّد المستخدم وحده |
| 2026-07-23 | **ابنِ لوحة** فيها `/api/v1/host/resolve` — و«توصيل التطبيق» بند معلَّق |

**ليس تناقضاً.** ما فُصل اليوم هو وكيل المالك **السابق** (`strong8k.app` / `api.invalid`) — شيفرة ميتة يستحيل بلوغها. أمّا `panel.8k.site` فهي لوحتك **المستقلّة**، ولم تُوصَل بالتطبيق قط.

### ما يجوز توصيله — وما لا يجوز

بحثتُ في تفسير الإرشاد 2.5.2. الخلاصة الموثّقة:

> الإعداد البعيد **مسموح** حين يعدّل وظيفة **ظاهرة ومُعلَنة**. و**ممنوع** حين يشحن ميزات نائمة تُفعَّل بعد المراجعة. المعيار: **المراجع يجب أن يكون قد رأى كل ما سيُتحكَّم به عن بُعد.**

| نقطة النهاية | الحكم | السبب |
|---|---|---|
| `GET /host/resolve?code=` | ✅ **وصّلها** | بحث عنوان، كالـDNS. لا يغيّر سلوكاً ولا يكشف ميزة مخفية |
| `GET /hosts` | ✅ | نفس الشيء |
| `POST /heartbeat` | ⚠️ **بشرط** | مراقبة أجهزة = جمع بيانات. يجب إعلانه في بطاقة App Privacy وفي نصّ الخصوصية، وإلا فهو تناقض مباشر مع ما نقوله اليوم |
| `GET /config` → **أعلام الميزات** | ❌ **لا توصّلها** | «أطفئ الرقابة الأبوية عن بُعد» هو **حرفياً** النمط المرفوض. وهو بند `L-3` في تدقيق يوليو |
| `GET /config` → **وضع الصيانة** | ❌ | تعطيل التطبيق بعد الموافقة = مفتاح إيقاف |
| `GET /config` → **الحدّ الأدنى للإصدار** | ✅ | التحديث القسري ممارسة مقبولة وشائعة |
| `GET /config` → **ثيم/ألوان** | ❌ | إعادة تلوين وقت التشغيل = 4.2.6 حرفياً |

**القاعدة العملية:** اللوحة تبقى كما هي — لكن **التطبيق يقرأ منها ما يجوز فقط**.

---

## 1 · اللوحة — فحص حيّ من الخارج

```
/api/health              200   {"ok":true,"service":"blanktv-api"}
/api/v1/hosts            200   Host A, Host B
/api/v1/config           200   maintenance/version/announcement/features
/admin                   302 → /admin/login   (Filament يعمل)
TLS                      صالح · http → https ‏301
```

### 🔴 ما وجدتُه

| # | البند | الدليل |
|---|---|---|
| **S-1** | **`host/resolve` يُرجع مضيفاً لأي كود خاطئ.** جرّبتُ `code='"` فأعاد **Host B** | مستخدم يخطئ في الكود يُوصَل بمضيف شخص آخر. الاحتياط مقصود («التحويل عند تعطّل المضيف») لكنه لا يجب أن يشمل **كوداً غير موجود** — ذاك يستحق `404` |
| **S-2** | **مضيفا الاختبار حيّان في الإنتاج** — `Host A`/`Host B` على `hosta.example:8080` | الذاكرة نفسها تقول «احذف مضيفات الاختبار». الآن يراهما أي أحد |
| **S-3** | **`/api/v1/hosts` عامّ بلا أي مصادقة** — يسرد كل مضيفاتك | قائمة سيرفراتك التجارية مكشوفة لأي طلب |
| **S-4** | **لا `Strict-Transport-Security`** | متصفّح يقبل أول اتصال HTTP قبل التحويل — نافذة اعتراض |
| **S-5** | **HTTP/1.1 فقط** — لا HTTP/2 | كل طلب على اتصال منفصل |
| **S-6** | **`Server: nginx/1.28.0 (Ubuntu)`** يكشف الإصدار الدقيق | يختصر على المهاجم خطوة الاستطلاع |
| **S-7** | **لا 2FA على `/admin`** (مسجَّل كمعلَّق منذ يوليو) | لوحة إدارة بكلمة مرور واحدة |
| **S-8** | **لم أفحص تحديد المعدّل** — عمداً، كي لا أُرهق خادمك | مجهول، ويجب إثباته |

### ✅ ما هو سليم

`APP_DEBUG` مطفأ (‏404 نظيف، بلا أثر تتبّع) · `heartbeat` يتحقّق من المدخلات (422) · شهادة TLS صالحة · تحويل HTTP قسري · `X-Frame-Options` و`X-Content-Type-Options` موجودان.

---

## 2 · التطبيق — الحالة بعد عمل اليوم

### ✅ مُغلق

مستقلّ تماماً (صفر خوادم لنا) · 146 اختباراً · بوابة في مسارَي CI · `DatabasePool`+WAL · SQLite مصدر الإقلاع البارد · البحث العربي · كشف الاتصال · **كلمة المرور لم تعد على القرص نصّاً صريحاً** · بيان الخصوصية موحَّد في خمس لغات · بطاقة المتجر بالعربية والإنجليزية.

### 🔴 مفتوح

| # | البند | لماذا |
|---|---|---|
| ~~**A-1**~~ ✅ | ~~لا مراقبة انهيارات~~ · **أُغلق** — من `Caches` (يحقّ للنظام حذفها) إلى Application Support مع ترحيل، وصارت تُقرأ وتُنسخ من شاشة القياس | MetricKit فقط: مجمَّع، متأخر 24–48h. لمنتج مباع، انهيار عند مستخدمي العميل = لا أحد يعرف السبب |
| **A-2** | **الفهرس كاملاً في الذاكرة** | القرّاء الصفحيون مبنيّون ومُختبَرون وبلا مستهلك. **غير مقيس** |
| ~~**A-3**~~ ✅ | ~~`switchPlaylist`~~ · **أُغلق** (`1e6f614`) | لا يضبط `m3uURL` ويضبط `loginMode = .xtream` → فهرس خاطئ + خروج من الحساب |
| ~~**A-4**~~ ✅ | ~~`ConfigService` ميت~~ · **أُغلق** (`23b54c8`) | صار ثابتاً بعد الفصل — يمسّ 3 شاشات |
| **A-5** ◐ | **Dynamic Type — طبقة الرموز أُغلقت** | 14 رمزاً في `S8KFont` تتبع الإعداد → 263 موضعاً · 6 اختبارات تثبّت المنحنى بلا جهاز. **الباقي: 70 نصاً** بمقاس ثابت في ملفات الشاشات (و64 أيقونة SF تُترك ثابتة عن قصد) |
| ~~**A-6**~~ ✅ | ~~`mediaSelectionGroup` ×4~~ · **أُغلق** — `loadMediaSelectionGroup` غير المتزامنة + ذاكرة مخبّأة | قراءة `AVAsset` متزامنة على مسار فتح المشغّل |
| **A-7** | **Swift 6** | 176 تحذيراً · صفر أخطاء · يغيّر أين تعمل الشيفرة |
| **A-8** | **صفر اختبارات واجهة/تكامل** | التغطية منطق نقي ومخزن فقط |
| **A-9** | **لا لقطات شاشة** | يفرضها المتجر |
| **A-10** | **النطاق** | يغلق: البريدين · `privacyPolicyUrl` · `supportUrl` · نشر الموقع |
| **A-11** | **`contentRightsDeclaration`** | إقرار حقوقي — توقيع المالك |

---

## 3 · المراحل التنفيذية

المعيار في الترتيب: **(الأثر) ÷ (المخاطرة)**، والمؤكَّد قبل المحتمل.

### المرحلة 1 — الخادم (لا تلمس الثنائية)
`S-2` احذف مضيفات الاختبار · `S-1` أعِد `404` لكود غير موجود · `S-4` أضف HSTS · `S-6` أخفِ إصدار nginx · `S-5` فعّل HTTP/2 · `S-3` قرّر: هل `/hosts` عام؟ · `S-7` فعّل 2FA · `S-8` أثبت تحديد المعدّل

> **يتطلّب SSH.** المفتاح السابق كان محصوراً بجلسة انتهت — يلزم مفتاح جديد يُضاف إلى `authorized_keys`.

### المرحلة 2 — عيوب التطبيق المؤكَّدة (بلا جهاز)
~~`A-3`~~ ✅ · ~~`A-4`~~ ✅ · ~~**`A-1`**~~ ✅ — **المرحلة 2 مكتملة.**

### المرحلة 3 — توصيل اللوحة
شاشة إدخال «كود المضيف» + `host/resolve` + احتياط · **بلا** أعلام ميزات · و`heartbeat` **فقط** إن أُعلن في بطاقة الخصوصية

### المرحلة 4 — تحتاج جهازك
`A-2` قياس الذاكرة ثم القوائم الصفحية · ~~`A-6`~~ ✅ (أُغلق بلا جهاز) · `A-7` · `A-9` لقطات

### المرحلة 5 — إغلاق النشر
`A-10` النطاق (يغلق أربعة) · `A-11` إقرار الحقوق · `A-5` الـ70 نصاً الباقية

---

## 4 · الحكم

**قابل للعرض اليوم** — الرابط يعمل والبناء 140 مثبَّت خارجياً.

**غير قابل للنشر**، وغير مُثبَت الأداء. الفارق ثلاثة أشياء: **قياس على جهاز**، **مراقبة انهيارات**، **نطاق**.

**عن السَّعة:** لا يوجد خادم لنا في مسار التشغيل — كل مستخدم يتصل بمزوّده هو. **لا سقف لعدد المستخدمين لأنه لا مورد مشترك يُستهلك.** ما يتوسّع فعلاً هو حجم الفهرس **داخل الجهاز الواحد**، وهو `A-2`. وإن وُصلت اللوحة، تصير `host/resolve` أول مورد مشترك — وعندها تحديد المعدّل والتخزين المؤقّت (`S-8`) يتحوّلان من ملاحظة إلى شرط.

---

## 5 · ما تغيّر بعد كتابة هذا التدقيق

خمس دمجات إلى `main`، كل فرع **قُرئ سطراً سطراً قبل دمجه** — لا مصرِّف على Windows، فالمراجعة بالعين هي البوابة الوحيدة قبل Codemagic. ووجدت المراجعة **عيبين لم يكونا معروفين**، كلاهما داخل فرعين كانا يبدوان جاهزين:

| الفرع | ما وجدته المراجعة | لماذا يهمّ |
|---|---|---|
| `feat/crash-reports-surfaced` | قائمة الأعطال تُقرأ من القرص في **مُهيّئ الـ View**. قيمة `@State` الابتدائية **هي** المُهيّئ، وSwiftUI يعيد بناء بنية الـ View في كل تمريرة للأب — فيُفتح المجلد وتُقرأ كل حمولة ويُفكّ JSON على الخيط الرئيسي، مراراً، لقيمة يرميها SwiftUI بعدها | نفس صنف العيب الذي وُجد هذا الفرع أصلاً ليكون ذا قيمة |
| `perf/async-media-selection` | نتيجة جلب متأخرة تُطبَّق بلا تحقّق من هوية العنصر. `load()` يمسح الذاكرة المخبّأة لأنها تصف أصلاً واحداً، ثم يعود الجلب الطائر فيملؤها بمجموعة **الأصل القديم** | شاهد الحلقة 1، اضغط الحلقة 2 قبل رجوع قائمة التشغيل → مسارات ترجمة الحلقة 1 تظهر على الحلقة 2، والضغط على أحدها يختار داخل عنصر لم يعد على الشاشة |

**لم يُدمج `chore/concurrency-survey` عمداً.** الفرع يقول عن نفسه في `codemagic.yaml`: *«TEMPORARY — a survey, not a gate… is not meant to be merged»*، وهو يضيف `SWIFT_DEFAULT_ACTOR_ISOLATION` إلى إعدادات المشروع و`isolated deinit` (ميزة Swift 6.2) على مشروع مضبوط على `SWIFT_VERSION 5.0`. قيمته **معرفة لا شيفرة**، وهي مسجَّلة في `HANDOVER.md`: صفر أخطاء · 176 تحذيراً في 8 أنماط.

### البوابات بعد الدمجات

`chk.py` 37 ملفاً ✅ · `uilint.py` 28 ملفاً ✅ · `provelint.py` ✅ · `scopechk.py` ✅
كل رمز `S8KFont` مُشار إليه في الشيفرة له تعريف ✅ · كل ملفات الاختبار التسعة مسجَّلة في `project.pbxproj` ✅

`brandlint.py` **يفشل عمداً** على `report@strong8k.app` و`support@strong8k.app` — هذا حاجز `A-10` المقصود، لا عيب جديد.

> **مصيدة وجدتُها:** `TypeScaleTests.swift` كان على القرص لكنه **في أيٍّ من أقسام `project.pbxproj` الأربعة**. الاختبارات ما كانت لتُبنى ولا لتُشغَّل، وما كان شيء ليقول ذلك. الفحص أعلاه (تسعة ملفات × أربعة مواضع) صار جزءاً من البوابة لهذا السبب.

### روابط TestFlight — قُرئت حيّة من App Store Connect

| المجموعة | النوع | الرابط العام | البناءات |
|---|---|---|---|
| المختبرون | داخلية | — | 140 · 139 · 134 · 120 … |
| المختبرون1 | **خارجية** | `https://testflight.apple.com/join/EEuVDve9` | **105 · 71 · 22** ⚠️ |
| المختبرون2 | **خارجية** | `https://testflight.apple.com/join/6WV2CQbe` | **140** · 110 |

المجموعة الخارجية **لا تلتقط البناء الجديد تلقائياً** — السبب موثَّق في `ddac851`: `hasAccessToAllBuilds` لا معنى له إلا للمجموعات الداخلية، فكل بناء يُضاف يدوياً بـ`ascbeta.py`.

**`المختبرون1` رابط عامّ حيّ عالق على البناء 105.** إن كان قد أُرسل لأحد فهو يخدم نسخة متقادمة بعشرات البناءات، ولا شيء في أي مكان يبلّغ عن ذلك. يحتاج قراراً: يُحدَّث إلى 140 أو يُعطَّل.
