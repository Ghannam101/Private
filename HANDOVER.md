# تسليم «Blank Premium» — تدقيق من طرف إلى طرف + ما يلزم المشتري

**التاريخ:** 2026-08-21 · **الفرع:** `main` @ `650ae7e` · **الاختبارات:** 146 خضراء

## المنهجية

كل سطر هنا مأخوذ من **فحص فعليّ** — الشيفرة، أو سجلّ CI، أو **App Store Connect نفسه عبر واجهته البرمجية**. لا شيء منقول عن تقرير سابق بلا إعادة تحقّق: تدقيق `APP_REVIEW_AUDIT.md` (يوليو) وُجد أن نصفه صار قديماً.

**نموذج البيع:** المشتري يقدّم من **حسابه هو**، لا من حساب المالك. هذا يعني سجلّ تطبيق جديد، ومعرّف حزمة تحت فريقه، ونطاقه هو. القسم 3 هو قائمته.

---

## 1 · الثنائية (الشيفرة) — ما أملكه أنا

### مستقلّة تماماً — متحقَّق منه

بحثتُ عن كل عنوان شبكي في الهدف المشحون. الباقي **صفر** خوادم خلفية:

| العنوان | ما هو |
|---|---|
| `http://server.com/playlist.m3u` | نصّ توضيحي داخل حقل إدخال |
| `https://www.gnu.org/licenses/...` | رابط LGPL في شاشة التراخيص (التزام قانوني) |
| `https://www.youtube.com/watch` | فحص نطاق للمقطع الدعائي — قائمة سماح، لا اتصال |
| `https://wa.me/` | زرّ واتساب — **غير قابل للبلوغ**: مصدره `appConfig` الذي صار ثابتاً |

التطبيق يتحدث إلى **مزوّد المستخدم وحده**. طبقة الوكيل الخلفية (`APIClient` وما تبعها) حُذفت — 311 سطراً — بعد إثبات أنها غير قابلة للبلوغ: `Keychain.token` لم يكن يُكتب في أي موضع، و13 من 14 استدعاءً كانت ترمي قبل فتح اتصال.

### الحالة الهندسية

| المحور | الحالة |
|---|---|
| الاختبارات | **146 في 18 مجموعة · 1.2 ثانية** — تعمل في `ios-test` **وداخل `ios-release` قبل التوقيع** |
| المخزن | `DatabasePool` + WAL + `synchronous=NORMAL` · عزل لقطة · 25 اختباراً على SQLite حقيقي |
| البحث العربي | قواعد Lucene `ArabicNormalizer` على **الذاكرة والفهرس معاً** + ترحيل `v3_fold_fts` |
| كشف الاتصال | `Reachability` — رسالة انقطاع خاصة وإعادة تحميل ذاتية عند العودة |
| بيان الخصوصية | `C617.1` (كان `3B52.1` الخاطئ — رمز يؤتمت Apple رفضه) |
| نصّ إذن الشبكة | إنجليزي ودقيق (كان عربياً فقط ويلوم AirPlay خطأً) |
| الهوية | `brandlint` يفحصها ويقرأ الاسم من `S8KBrand` وقت التشغيل |

### التباين — مقيس لا منقول

| الزوج | النسبة |
|---|---|
| حبر الزرّ على `#CBFF06` | **15.48 : 1** (AA+AAA) |
| الأكسنت كنصّ على الأسطح الأربعة | 15.48 / 14.38 / 13.08 / 11.45 — كلها AA |

### ⛔ حاجز واحد باقٍ في الشيفرة

```
BlankTV/DesignSystem.swift:1949  reportEmail  = "report@strong8k.app"
BlankTV/DesignSystem.swift:1960  supportEmail = "support@strong8k.app"
```

نطاق **المالك السابق** داخل منتج يُباع كمستقلّ. `brandlint.py` يفشل عليه الآن عمداً — حاجز يهمس هو حاجز يُشحن.

---

## 2 · الخادم — `site/`

الموقع الثابت هو **كل** ما تبقّى من جانب الخادم، ووظيفته حقلان تفرضهما Apple.

| الملف | الحالة |
|---|---|
| `site/trex/index.html` | صفحة تعريف · **باسم Trex** |
| `site/trex/privacy/index.html` | سياسة خصوصية **ثنائية اللغة** — سليمة البنية |
| `site/trex/support/index.html` | صفحة دعم **ثنائية اللغة** |

**ثلاث مشكلات:**
1. **غير منشور.** لا CNAME، ولا إعداد استضافة، ولا CI. ملفات في مستودع فقط.
2. باسم **Trex** والمجلّد نفسه اسمه `trex`.
3. البريد فيه `@strong8k.app`.

**لماذا يوقف هذا التقديم:** Apple تفتح `privacyPolicyUrl` و`supportUrl` **أثناء المراجعة**. رابط لا يفتح = رفض بيانات وصفية.

---

## 3 · App Store Connect — قرأتُ الحالة الحقيقية من Apple

```
app      : Trex TV · com.blanktv.player · id 6789773663
state    : PREPARE_FOR_SUBMISSION · 17+
platforms: iOS فقط  (Mac / visionOS / tvOS مُطفأة)
locales  : ar-SA فقط
PRIVACY  : — فارغ —  (يمنع التقديم)
SUPPORT  : — فارغ —  (يمنع التقديم)
```

### الاسم — حُسم

الأرشيف كان يسجّل **«Blank Prime»**، وهو **محجوز عالمياً**: App Store Connect ردّ بـ HTTP 409، وبحث المتجر العام لا يُظهر أي تطبيق منشور بهذا الاسم — أي أنه حجزٌ غير منشور على حساب آخر.

المالك صحّح: اسم العميل **«Blank Premium»**. فحصتُه في متاجر US و SA و AE — **لا تطابق**. وهو الاسم المشحون الآن في كل موضع.

### 🔴 لا توجد لغة إنجليزية على البطاقة

`ar-SA` هي **اللغة الوحيدة**. مراجع Apple يقرأ الإنجليزية. الوصف والكلمات المفتاحية والعنوان الفرعي كلها عربية، فالمراجع يقرأ بطاقة لا يفهمها عن تطبيق في الفئة الأكثر تدقيقاً.

### تصحيح لتدقيقنا الداخلي

قال التدقيق إن كلمة **«IPTV»** في المتجر «إشارة قرصنة مباشرة». بحثتُ فوجدتُ تطبيقاً حيّاً اسمه حرفياً **«VixioTV - IPTV & M3U Player»** قُدِّم يونيو 2026 — مشغّل «أحضر محتواك». **الكلمة ليست سبب رفض بذاتها**؛ 5.2.3 يدور حول هل يوفّر التطبيق محتوى — ووصفنا يقول صراحةً إنه لا يوفّر.

فبقيت `iptv` و`xtream` و`m3u` (وهي أقوى كلمات البحث لهذا المنتج)، وتغيّرت `تريكس` إلى `بلانك` فقط.

### ناقص تماماً: **لقطات الشاشة**

لا توجد في المستودع، وApple تفرضها لكل مقاس جهاز. وهي كذلك ما سيراه العميل.

---

## 4 · قائمة المشتري — بالضبط، ملفاً ملفاً

| # | ما يغيّره | أين | عدد المواضع |
|---|---|---|---|
| 1 | **النطاق** — بريد الإبلاغ والدعم | `DesignSystem.swift:1949,1960` | 2 |
| 2 | **معرّف الحزمة** | `project.pbxproj` (`PRODUCT_BUNDLE_IDENTIFIER`) | 4 (2 للتطبيق + 2 للاختبارات) |
| 3 | **فريق التطوير** | `project.pbxproj` (`DEVELOPMENT_TEAM`) | 4 |
| 4 | **اسم العرض** | `project.pbxproj` (`INFOPLIST_KEY_CFBundleDisplayName`) | 2 |
| 5 | **اسم المنتج** | `DesignSystem.swift` — `S8KBrand.name` / `shortName` / `markSuffix` | 3 |
| 6 | **اللوحة** | `BrandTheme.active` | 1 |
| 7 | **الأصول** | `Assets.xcassets/` — `Logo.imageset` · `AppIcon.appiconset` · `AccentColor` | 3 |
| 8 | **CI** | `codemagic.yaml` — `BUNDLE_ID` + تكامل App Store Connect | 2 |
| 9 | **الموقع** | `site/` — إعادة تسمية، وإعادة توسيم، ونشر على نطاقه | 4 ملفات |
| 10 | **سجلّ المتجر** | حسابه: الاسم · الخصوصية · الدعم · **لغة إنجليزية** · لقطات · التصنيف العمري | — |

**البوابتان تمسكان 1 و5:** `python brandlint.py` يفشل على النطاق القديم وعلى أي حرفية اسم خارج `S8KBrand`، ويقرأ الاسم وقت التشغيل فلا يمكن أن يبقى يبحث عن اسم قديم.

**بعد كل تغيير:** `python chk.py && python brandlint.py`، ثم بناء `ios-test`.

---

## 5 · ما زال مفتوحاً عندنا — ولماذا لم يُنفَّذ

| البند | لماذا يحتاج جهازاً |
|---|---|
| ترحيل Swift 6 | مُستقصى بالكامل: **صفر أخطاء · 176 تحذيراً في 8 أنماط**. لكن عزل MainActor يغيّر **أين تعمل الشيفرة** (Keychain يقفز للخيط الرئيسي) — لا اختبار وحدة يكشف جموداً |
| القوائم الصفحية (م1.4ب) | مكسبها **ذاكرة**، ولا قياس قبل/بعد بلا جهاز. تنفيذها الآن تحسين أعمى |
| `mediaSelectionGroup` ×4 | مهجورة منذ iOS 16، وقراءة `AVAsset` **متزامنة** على مسار فتح المشغّل — لا يُختبَر آلياً |
| `v3_fold_fts` على فهرس حقيقي | مُختبَر على SQLite حقيقي لكن ببيانات اصطناعية |

---

## 6 · الحكم

**الثنائية جاهزة تقنياً.** مستقلّة، مُختبَرة، بهوية مطبَّقة ومقيسة، وأسباب الرفض المؤكَّدة التي رصدها تدقيق يوليو **أُغلقت كلها**.

**التقديم محجوب بأربعة أشياء لا أملك أنا أيّاً منها:**

1. **نطاق** — يحلّ البريدين في الشيفرة، وينشر الموقع، ويملأ حقلَي الخصوصية والدعم
2. **اسم متاح** — «Blank Prime» محجوز عالمياً
3. **لغة إنجليزية** على بطاقة المتجر
4. **لقطات شاشة**

الأربعة قرارات مالك أو مُدخلات، لا شيفرة.
