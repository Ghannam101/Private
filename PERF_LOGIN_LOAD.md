# تشخيص: ثلاث دقائق بين تسجيل الدخول وظهور البوسترات

_تقرير هندسي — 2026-07-28. تشخيص فقط: لم يُعدَّل أي ملف مصدر._
_الافتراض المرجعي: خط IPTV كبير — 56,000 قناة · 30,000 فيلم · 10,000 مسلسل (≈ 96,000 عنصر)._

---

## 0. الجواب في سطر واحد

**لا يمكن لأي بوستر أن يظهر قبل أن تنتهي كتابة `CatalogDB.save` بالكامل**، لأن مسار رسم كل صورة
ينتظر قراءة من نفس `DatabaseQueue` المتسلسل الذي تحتجزه تلك الكتابة (≈ 192,000 عبارة SQL داخل
معاملة واحدة) — والانتظار يمر أيضاً عبر قفل واحد مشترك يجعل كل الخلايا تصطف خلف بعضها.

- الكتابة: `CatalogDB.swift:305-341`، تُطلق من `Core.swift:1791`
- القفل والقراءة: `DesignSystem.swift:1386-1404` (القفل مُمسك عبر قراءة SQLite في السطر 1394-1397)
- نقطة الانتظار في واجهة الصورة: `DesignSystem.swift:1520-1531`

---

## 1. المسار كاملاً — من الضغط على «دخول» إلى أول بوستر

| # | المرحلة | الملف:السطر | ماذا تفعل فعلياً | التكلفة المقدَّرة (96k عنصر) | الدليل |
|---|---------|-------------|------------------|------------------------------|--------|
| 1 | `GatewayView.submit()` | `GatewayView.swift:664-680` | تُنشئ Task وتنادي `auth.loginXtream` | < 1 ms | لا عمل ثقيل |
| 2 | `AuthService.loginXtream` | `Services.swift:95-147` | فحص jailbreak، تطبيع الـ host، ترميز الاعتماد، كتابة `Store.m3uURL` (Keychain) | 5–30 ms (على الـ main actor) | `SecItemDelete`+`SecItemAdd` واحدتان |
| 3 | `validateCredentials` → `validateAuth` | `Core.swift:1986-1995`, `2030-2055` | **رحلة شبكة #1**: `player_api.php` بدون action، timeout 12s، ثم `JSONSerialization` على استجابة صغيرة | 0.3–12 s (شبكة بحتة) | `apiData(..., timeout: 12)` سطر 2033 |
| 4 | `loggedIn = true` → mount الـ TabView | `BlankTVApp.swift:196-206` | لا يوجد Boot screen بعد الآن؛ `TabView` **يقيّم أجسام التبويبات الخمسة كلها**، فتنطلق `LiveTVVM.load` + `MoviesVM.load` + `SeriesVM.load` + `HomeVM.load` معاً | فوري بصرياً (هياكل skeleton) | `contentGen` + `.id()` |
| 5 | تجميع الطلبات في `PlaylistService.load` | `Core.swift:1746-1753` | single-flight: كل النداءات السبعة تلتحق بعملية جلب **واحدة** | مجاني — وهذا صحيح ويجب ألا يُلمس | `inFlight` |
| 6 | `_load(force:)` | `Core.swift:1755-1794` | لا يوجد كاش قرصي في أول دخول → `loadXtreamDirect` | — | سطر 1765 |
| 7 | `loadXtreamDirect` — `validateAuth` **مكرر** | `Core.swift:2059` | **رحلة شبكة #2**: نفس الفحص الذي تم للتو في المرحلة 3 | 0.3–12 s **مهدورة** | نفس الدالة، بلا كاش |
| 8 | الفئات (3 طلبات) | `Core.swift:2067-2075` | متوازية فعلاً (`async let`)، لكنها **تُنتظر بالكامل قبل** إطلاق طلبات القوائم | 0.3–3 s (جولة تسلسلية ثالثة) | `let liveCats = ... await` قبل السطر 2094 |
| 9 | القوائم الثلاث الكبيرة | `Core.swift:2094-2107` | متوازية فعلاً، timeout 22s لكل واحدة، **مع إعادة محاولة حتى 3 User-Agents** عند رد غير 2xx | **10–60 s** — الحمل الأكبر شبكياً | `apiData` سطر 1929-1962 |
| 10 | حجم الـ JSON | — | live ≈ 400 B/عنصر × 56k ≈ **22 MB** · vod ≈ 380 B × 30k ≈ **11 MB** · series (plot+cast) ≈ 900 B × 10k ≈ **9 MB** → **≈ 42 MB** | تنزيل 42 MB على وصلة IPTV نموذجية 1–3 MB/s = **15–40 s** | تقدير من مفاتيح Xtream القياسية |
| 11 | `dictArray` × 3 | `Core.swift:1977-1979`, `2112-2114` | `JSONSerialization` → **96,000 NSDictionary + ≈ 1.1 مليون NSString** | **2–6 s CPU** و**ذروة ذاكرة 250–450 MB** | لا streaming، الثلاثة تبقى حيّة معاً حتى نهاية الدالة |
| 12 | حلقات بناء النماذج | `Core.swift:2117-2161` | 96,000 دورة، ≈ 8 نداءات `str()` لكل عنصر ≈ **770,000 نداء**، + 96,000 struct جديد | **0.5–1.5 s** | ثلاث حلقات `for d in …` |
| 13 | `CatalogDiskCache.save` | `Core.swift:1787` → `Core.swift:1713-1717`, `1635-1662` | `envelope(from:)` تنسخ الـ 96k إلى **96k struct جديد**، ثم `JSONEncoder` ينتج ≈ 35 MB، ثم كتابة atomic | **2–5 s + ≈ 150 MB ذاكرة مؤقتة** — و**هي متزامنة داخل الـ actor قبل `return built`**، أي أن كل التبويبات تنتظرها | السطر 1787 يسبق `return built` في 1793 |
| 14 | `CatalogDB.save` (detached) | `Core.swift:1791` → `CatalogDB.swift:305-341` | 4 عمليات حذف + **96,000 `insert`** + **96,000 `INSERT INTO catalog_fts`** داخل **معاملة واحدة**، بأولوية `.utility` | **25–90 s** (انظر §3) | الحلقات 313-336 |
| 15 | عودة البيانات إلى الـ VMs | `ContentViews.swift:60-79`, `879-895`, `2099-2115`; `HomeView.swift:132-151` | كلها `@MainActor` | **1.5–5 s تجميد main thread** (انظر §7) | تفصيل في §7 |
| 16 | أول بوستر | `DesignSystem.swift:1500-1531` | تبدأ التنزيل، ثم **تنتظر ThumbHash من SQLite قبل عرض الصورة المنزَّلة** | **محجوبة حتى تنتهي المرحلة 14** | السطر 1522 ثم 1525 |

**المجموع التقريبي:** 40–100 ثانية حتى يظهر **النص**، ثم 25–90 ثانية إضافية حتى تظهر **البوسترات**.
هذا يطابق «ثلاث دقائق» المُبلَّغ عنها.

---

## 2. التكلفة المهيمنة — مُسمّاة

> **`CatalogDB.save` تحتجز الاتصال المتسلسل الوحيد بقاعدة البيانات، ومسار عرض كل صورة يقرأ من نفس الاتصال قبل أن يعرض الصورة.**

السلسلة، بالسطور:

1. `CatalogDB.dbQueue` هو **`DatabaseQueue`** واحد — `CatalogDB.swift:28-36`.
   `DatabaseQueue` في GRDB يسلسل **القراءات والكتابات معاً** على طابور تنفيذ واحد. (لو كان
   `DatabasePool` + WAL لكانت القراءات تمر بجانب الكتابة.)
2. `CatalogDB.save` تفتح معاملة كتابة واحدة تحوي ≈ 192,000 عبارة — `CatalogDB.swift:308-339`.
   طوال هذه المعاملة، **أي `q.read` يقف في الطابور**.
3. `S8KImageCache.thumbHashImage` تنادي `CatalogDB.imageHash` — `DesignSystem.swift:1397` —
   و**تُمسك `hashLock` عبر تلك القراءة** (`DesignSystem.swift:1394-1400`). النتيجة المضاعفة:
   ليس فقط كل خلية تنتظر القاعدة، بل كل الخلايا تنتظر **قفلاً واحداً** يمسكه أول منتظر.
4. `S8KImage.load` تنتظر ThumbHash **قبل** أن تطبّق الصورة المنزَّلة:
   ```swift
   async let downloaded = S8KImageCache.shared.load(imageURL, key: u, maxPixel: mp)   // 1521
   let ph = await Task.detached { S8KImageCache.shared.thumbHashImage(u) }.value      // 1522 ← يحجب
   …
   let img = await downloaded                                                          // 1525
   ```
   التنزيل يبدأ أولاً (وهذا صحيح)، لكن **تسليم الصورة إلى الشاشة يقع بعد السطر 1522**.
   فحتى لو اكتمل التنزيل في ثانيتين، لا تُرسم الصورة قبل أن تُفرج المعاملة عن القاعدة.

**نتيجة:** البوسترات لا تتأخر بسبب الشبكة ولا بسبب فك الترميز؛ تتأخر بسبب **كتابة الفهرس**.
وهذا يفسّر تحديداً ملاحظة المالك أن المحتوى النصي يسبق البوسترات بفارق كبير.

مساهم ثانوي في نفس الطابور: `ensureThumbHash` (`DesignSystem.swift:1414-1426`) تُطلق مهمة خلفية
لكل صورة نُزِّلت، وكل واحدة تنادي `CatalogDB.hasImageHash` — فبمجرد انتهاء الكتابة يصطف خلفها
موجة من مئات القراءات الصغيرة.

---

## 3. لماذا تستغرق `CatalogDB.save` عشرات الثواني؟

| البند | العدد | الملاحظة |
|-------|-------|----------|
| `Chan/Mov/Ser/Cat.insert(db)` | 96,000 | GRDB يخزّن عبارة الـ INSERT مؤقتاً لسجلات `PersistableRecord` — هذا الجزء فعّال نسبياً. لكن كل جدول يحمل **فهرسين إضافيين** (`*_scope_pos` و`*_scope_cat_pos`, `CatalogDB.swift:86-111`) → ثلاث كتابات B-tree لكل صف |
| `db.execute(sql: ins, arguments:)` للـ FTS | 96,000 | **`Database.execute(sql:)` في GRDB 6 لا يستخدم مخزن العبارات المؤقت** — أي `sqlite3_prepare_v2` لنفس الـ INSERT **96,000 مرة**. تكلفة صافية مهدورة تُقدَّر بـ 2–5 s |
| ترميز FTS5 | 96,000 صف | التوكنة تتم على `name+genre+actors+plot+director` مع `unicode61 remove_diacritics 2`. صفوف الأفلام والمسلسلات تحمل `plot` و`cast` كاملين → أغلى بكثير من صفوف القنوات |
| الأولوية | `.utility` | `Task.detached(priority: .utility)` — `Core.swift:1791`. على iPhone هذا يُجدول غالباً على النوى الاقتصادية؛ العمل نفسه قد يستغرق 2–4× زمنه عند `.userInitiated` |
| ضغط الذاكرة | — | تعمل بالتوازي مع المرحلتين 11 و13 (ذروة 400+ MB)، فيزداد البطء |

**تقدير:** 25–90 s على جهاز حقيقي. لا أستطيع تثبيت الرقم من الكود وحده — انظر §9.

**ملاحظة مهمة:** المعاملة الواحدة **قرار صحيح** في ذاته (96k معاملة منفصلة ستكون أسوأ بمراحل).
المشكلة ليست المعاملة، بل أن **القراءات تشترك مع الكتابة في طابور واحد**.

---

## 4. المشتبهون — تأكيد أو تبرئة

### 1) أرجل الشبكة — ✅ متوازية، لكن مع ثلاث جولات تسلسلية زائدة
- الطلبات الثلاثة الكبيرة متوازية فعلاً عبر `async let` — `Core.swift:2094-2096`. **صحيح.**
- لكن قبلها ثلاث جولات ذهاب-وإياب متتابعة: `validateAuth` عند الدخول (`Services.swift:128`)،
  ثم **`validateAuth` مرة ثانية** داخل `loadXtreamDirect` (`Core.swift:2059`)، ثم الفئات الثلاث
  التي تُنتظر بالكامل (`Core.swift:2073-2075`) قبل إطلاق القوائم.
- الـ timeout: 22 s للقوائم (`Core.swift:1929`)، 12 s لفحص الاعتماد (`Core.swift:2033`).
- **إعادة المحاولة:** عند رد غير 2xx يُعاد الطلب بثلاثة User-Agents (`Core.swift:1940-1959`).
  لوحة ترد 403 أو 5xx على قائمة الأفلام تعني تنزيل 11 MB **ثلاث مرات**. (المهلة الزمنية
  مستثناة بصواب — سطر 1956.)
- **حجم الـ JSON و`JSONSerialization`:** ≈ 42 MB، 2–6 s CPU، وذروة ذاكرة 250–450 MB.

### 2) التحليل وبناء النماذج — ✅ خارج الـ main thread، لكن مكلف في الذاكرة
- `PlaylistService` هو `actor` غير-main، فالحلقات تعمل على خيط خلفي. **لا شيء منها على الـ main.**
- 96,000 دورة، ≈ 770,000 نداء `str()`، 96,000 struct.
- **العيب الحقيقي هنا ذاكرة لا زمن:** `liveStreams` و`vodStreams` و`seriesList` (أشجار
  القواميس الثلاث) تبقى حيّة حتى نهاية `loadXtreamDirect` (`Core.swift:2112-2166`)، بالتوازي مع
  المصفوفات المبنيّة. لا يُحرَّر أي منها بعد استهلاكه.

### 3) `CatalogDiskCache.save` — ⚠️ **مؤكَّد كعائق، وهو يحجب العودة**
- `Core.swift:1787`: تُنادى **بشكل متزامن داخل الـ actor قبل `return built`** (السطر 1793).
- ليست على الـ main thread — لكنها تحجب **الـ actor**، وكل الـ VMs السبعة ملتحقة بنفس
  `inFlight`. أي أن أول نص على الشاشة يتأخر بمقدار زمن هذا الترميز كاملاً.
- التكلفة: نسخة كاملة ثانية من الكتالوج (`envelope(from:)`, `Core.swift:1635-1662`) +
  `JSONEncoder` على ≈ 35 MB + كتابة atomic ≈ **2–5 s**.

### 4) `CatalogDB.save` — ⛔ **المشتبه به الأول، ومؤكَّد**
- معاملة **واحدة**، لا 96,000 — وهذا صحيح.
- نعم، تبني فهرس FTS5 **صفاً صفاً** (`CatalogDB.swift:334-336`).
- نعم، هي `Task.detached` ولا ينتظرها أحد **صراحةً** — ولكن:
  **`DatabaseQueue` واحد يخدم القراءة والكتابة**، ومسار الصور يقرأ منه في كل خلية.
  فالانتظار موجود، لكنه غير مرئي في الكود لأنه ليس `await` على المهمة بل تنافس على المورد.

### 5) فهرس FTS — ⛔ مؤكَّد
- يُبنى في `CatalogDB.swift:332-336`: حذف بالنطاق ثم 96,000 `INSERT` منفصل عبر
  `db.execute(sql:)` — بلا مخزن عبارات مؤقت، مع توكنة كاملة لكل صف.
- لا يوجد مسار «ابنِ الفهرس لاحقاً»؛ هو داخل نفس المعاملة التي تحجب كل القراءات.

### 6) ThumbHash / prefetch الصور — ✅ **سليم، لا تلمسه**
- كل نداءات `prefetch` محدودة النافذة: 40 شعاراً (`ContentViews.swift:544`)، 30 بوستراً
  (`1970`, `2514`)، 6–8 خلفيات (`874`, `2096`, `HomeView.swift:119`, `218`).
- `ensureThumbHash` تعمل فقط على صورة نُزِّلت فعلاً، وبأولوية `.background`، ومحمية بفحص وجود.
- **لا شيء يحسب أو يجلب هاشات للكتالوج كله.** هذا الجزء مصمَّم بشكل صحيح؛ المشكلة في
  الطابور الذي يقرأ منه، لا في نطاق العمل.

### 7) شاشة الإقلاع / `contentReady` — ⛔ هنا السؤال المعماري
- `ContentBootView` حُذفت، و`contentReady` أصبح `computed` يرفع `contentGen`
  (`BlankTVApp.swift:31-37`) الذي يعيد تركيب مكدس التبويبات عبر `.id()` (السطر 206). **هذا صحيح.**
- **لكن الواجهة ما زالت مرهونة بالكتالوج الكامل.** كل `ContentService.*`
  (`Core.swift:2328-2351`) تنادي `PlaylistService.load()` التي لا تعود **إلا** بعد أن تكتمل
  القوائم الثلاث + الفئات الثلاث + الترميز إلى القرص. لا يوجد أي مسار يعطي «أول شاشة»
  من البيانات المتوفرة.
- عملياً: **قائمة القنوات — التي غالباً تصل أولاً — تنتظر قائمة الأفلام (أبطأ حمولة).**
  المستخدم ينتظر أبطأ العناصر الثلاثة، بينما 95% مما يراه على الشاشة الأولى جاهز منذ زمن.
- `CatalogDB` يملك بالفعل قراءة مُصفَّحة (`pageChannels`/`pageMovies`/`pageSeries`,
  `CatalogDB.swift:206-235`) — البنية التحتية للعرض التدريجي موجودة وغير مستخدَمة.

### 8) حجب الـ main thread — قائمة كاملة

| الموقع | العمل | التقدير |
|--------|-------|---------|
| `Services.swift:18` | `Keychain.upgradeAccessibilityIfNeeded()` — 8 عناصر × (load+delete+add) = 24 نداء `SecItem`، **مرة واحدة فقط** عند أول إقلاع بعد الترقية | 10–50 ms، مرة واحدة |
| `Services.swift:123` | كتابة `Store.m3uURL` → Keychain (delete+add) | 3–15 ms |
| `Services.swift:76`,`133` | `upsertPlaylist` → `JSONEncoder` على قائمة صغيرة في UserDefaults | < 5 ms |
| `ContentViews.swift:30` | `Dictionary(grouping: 56k channels)` | 40–120 ms |
| `ContentViews.swift:45` | `rebuildSearchIndex` — **56,000 نداء `String.folding` (ICU)** | 150–400 ms |
| `ContentViews.swift:857` | نفس الشيء لـ 30,000 فيلم | 80–220 ms |
| `ContentViews.swift:871` | `movies.sorted { … }` — **ترتيب 30,000 عنصر كامل** (struct بـ 14 حقل String)، ≈ 450k مقارنة + نقل بنى بحجم ≈ 200 بايت مع retain/release | **300–1200 ms** |
| `ContentViews.swift:872` | `movies.sorted { Int($0.id) … }` — ترتيب ثانٍ كامل، مع **≈ 900,000 نداء `Int(String)` داخل المُقارِن** | **400–1500 ms** |
| `ContentViews.swift:2093-2094` | نفس العيب لـ 10,000 مسلسل | 100–400 ms |
| `HomeView.swift:88-103` | `rebuildHero` — أربع عمليات ترتيب على **الفهارس** لا العناصر | 60–150 ms — **هذا مكتوب بشكل صحيح** |
| `ContentViews.swift:887`,`2107` | `applyFilter()` — مسح 30k/10k | 20–60 ms |

**المجموع: 1.5–5 ثوانٍ من التجميد الفعلي للواجهة**، على شكل عدة دفعات متتالية بعد وصول البيانات.
ليس هو السبب الرئيسي، لكنه يجعل اللحظة التي *كان* يُفترض أن تظهر فيها البوسترات متقطّعة.

`ContentViews.swift:871-872` و`2093-2094` هما **بالضبط** الخطأ الذي أُصلح سابقاً في
`HomeVM.rebuildHero` (انظر التعليق في `HomeView.swift:78-87`) — لكنه لم يُصلَح في
`MoviesVM` و`SeriesVM`.

### 9) انحدار الـ Keychain اليوم — ✅ **بريء**
قِيس عدّاد القراءات على مسار الدخول تحديداً:

- `Store.m3uURL` مُخزَّن مؤقتاً في الذاكرة (`Core.swift:985`, `992-1031`)، و**الـ setter يملأ
  الكاش** (`Core.swift:1026`). في `loginXtream` تُكتب القيمة أولاً (`Services.swift:123`)، فكل
  قراءة لاحقة على مسار الدخول تُخدَم من الذاكرة.
- عدد قراءات الـ Keychain الفعلية على مسار الدخول = **صفر بعد الكتابة الأولى**.
  القارئان داخل الـ actor (`Core.swift:1757` و`1987`) يصيبان الكاش.
- `m3uLock` (`Core.swift:986`) يُمسك لعمليات O(1) فقط (فحص مُتغيّر Optional). لا يُمسك عبر
  شبكة ولا عبر قاعدة بيانات. **لا يوجد تنافس ذو أثر.**
- `upgradeAccessibilityIfNeeded` (`Services.swift:18`) تعمل **مرة واحدة** في عمر التثبيت،
  8 عناصر، خلف علم في UserDefaults (`Core.swift:752-757`) — عشرات المللي ثانية عند أول إقلاع.
- لا يوجد أي نداء لـ `m3uURL` داخل حلقة أو داخل `body` لعرض متكرر (تحقّقت من الـ 30 موقعاً؛
  الوحيد داخل تدفق متكرر هو `ContentViews.swift:3234` في البحث، مرة لكل استعلام).

**الخلاصة: تغيير اليوم لم يساهم في الثلاث دقائق. لا تتراجع عنه.**
(التحفّظ الوحيد: الغياب لا يُخزَّن مؤقتاً عمداً — `Core.swift:1018` — فلو كان الـ Keychain غير
مقروء لسبب ما، كل قراءة ستكون رحلة حقيقية. هذا قرار صحيح لكنه يستحق قياساً لو ظهرت أعراض غريبة.)

---

## 5. قائمة الإصلاحات — مرتّبة

### أ. «يجعله يبدو فورياً» — وهو ما طلبه المالك

| # | الإصلاح | الموقع | التوفير المتوقع | الخطر | الجهد |
|---|---------|--------|------------------|-------|-------|
| **A1** | **لا تنتظر قاعدة البيانات قبل عرض صورة.** اعرض الصورة المنزَّلة فور وصولها؛ اجعل ThumbHash انتهازياً بالكامل (اقرأ الذاكرة المؤقتة فقط بشكل متزامن، وطبّق نتيجة القاعدة لاحقاً وفقط إن كانت الصورة لم تصل بعد) | `DesignSystem.swift:1520-1531` | **يزيل الحجب كاملاً — عشرات إلى مئات الثواني** | **منخفض جداً** (إعادة ترتيب انتظار) | ساعة |
| **A2** | **لا تمسك `hashLock` عبر قراءة SQLite.** اقرأ خارج القفل، وأمسك القفل فقط لتحديث المجموعة (مع قبول قراءة مكرّرة نادرة) | `DesignSystem.swift:1394-1400` | يزيل تسلسل كل الخلايا خلف قفل واحد | منخفض (يجب الحفاظ على منطق `forgetHashMiss`) | ساعة |
| **A3** | **عرض تدريجي: انشر كل قائمة فور وصولها** بدل انتظار الثلاث. أبسط شكل: اجعل `PlaylistService` ينشر لقطة جزئية (القنوات أولاً، ثم الأفلام، ثم المسلسلات) وتستهلكها الـ VMs عند وصولها | `Core.swift:2094-2166`, `Core.swift:2328-2351`, الـ VMs | القنوات تظهر بعد أسرع طلب لا أبطأه: **توفير 10–40 s محسوس** | **متوسط** — يمس عقد `M3UContent` والـ VMs الأربعة | 1–2 يوم |
| **A4** | **ارسم أول شاشة من `CatalogDB` المُصفَّح** (`pageChannels`/`pageMovies`) بدل انتظار المصفوفة الكاملة — البنية موجودة وغير مستعملة | `CatalogDB.swift:206-235` + الـ VMs (البند #6 المؤجَّل في `PROJECT_HANDOFF.md`) | يجعل الدخول الثاني فورياً حرفياً | متوسط-عالٍ | 2–4 أيام |

> A1 + A2 وحدهما يُتوقع أن يحوّلا «ثلاث دقائق حتى البوستر» إلى «البوستر يصل مع اكتمال تنزيله»،
> دون أي تغيير معماري. **ابدأ بهما.**

### ب. «يجعله أسرع فعلاً»

| # | الإصلاح | الموقع | التوفير | الخطر | الجهد |
|---|---------|--------|---------|-------|-------|
| **B1** | انقل `CatalogDiskCache.save` خارج مسار العودة (`Task.detached`, تماماً كما `CatalogDB.save`) | `Core.swift:1787` | **2–5 s من كل تسجيل دخول** | **منخفض جداً** (`M3UContent` نوع قيمة) | 10 دقائق |
| **B2** | احذف `validateAuth` المكرّرة داخل `loadXtreamDirect` (تمّت للتو في `Services.swift:128`)، وأطلق **الفئات الثلاث والقوائم الثلاث في جولة متزامنة واحدة** | `Core.swift:2059`, `2067-2096` | **جولتا ذهاب-وإياب: 1–15 s** حسب اللوحة | منخفض (احرص على بقاء `panelTimeZone` مضبوطاً) | 2 ساعة |
| **B3** | استبدل `db.execute(sql: ins, …)` بعبارة مُحضَّرة مُعاد استخدامها (`db.cachedStatement`)، أو ابنِ الـ FTS بعبارة واحدة `INSERT INTO catalog_fts … SELECT … FROM movie` | `CatalogDB.swift:332-336` | **2–5 s من زمن الكتابة** | منخفض | 2 ساعة |
| **B4** | حوّل `dbQueue` إلى **`DatabasePool` + WAL** حتى لا تقف أي قراءة خلف كتابة إطلاقاً | `CatalogDB.swift:28-36` | يزيل **فئة الخطأ** كاملة، لا حالة واحدة | **متوسط** — يتطلب ضبط `journalMode` والانتباه لملف قائم | نصف يوم |
| **B5** | أصلح الترتيبين الكاملين في `MoviesVM`/`SeriesVM` كما أُصلح `HomeVM.rebuildHero` (رتّب الفهارس، حلّل المفتاح مرة واحدة) | `ContentViews.swift:871-872`, `2093-2094` | **0.8–3 s من تجميد الـ main thread** | منخفض جداً (نمط مثبت في `HomeView.swift:88-99`) | ساعة |
| **B6** | انقل طيّ الأسماء (`rebuildSearchIndex`) خارج الـ main actor | `ContentViews.swift:44-47`, `856-858`, `2078-2081` | 0.3–0.7 s | منخفض | ساعتان |
| **B7** | حرّر كل شجرة قواميس بعد استهلاكها مباشرة بدل إبقاء الثلاث حيّة | `Core.swift:2112-2166` | **200–400 MB من ذروة الذاكرة** — يمنع الخنق الحراري/التبديل على الأجهزة الأصغر | منخفض | ساعة |
| **B8** | ارفع أولوية `CatalogDB.save` إلى `.userInitiated`، **بعد** تنفيذ B4 فقط (قبله سيزيد الحجب سوءاً) | `Core.swift:1791` | 2–4× أسرع للكتابة | مشروط بـ B4 | دقائق |

---

## 6. ما هو صحيح بالفعل — **لا تلمسه**

- **`inFlight` single-flight** في `PlaylistService.load` (`Core.swift:1738-1753`) — يمنع سبعة
  جلب متوازية للكتالوج نفسه. حذفه سيضاعف المشكلة سبع مرات.
- **تحمّل الفشل الجزئي + `isPartial`** (`Core.swift:2097-2114`, `1779-1792`) — والقاعدة التي
  تمنع كتابة كتالوج ناقص فوق الكاش السليم. القاعدة موثَّقة في `PROJECT_HANDOFF.md §9`.
- **الفشل السريع على المهلة الزمنية** في `apiData` (`Core.swift:1950-1958`).
- **`HomeVM.rebuildHero`** — ترتيب الفهارس بدل العناصر (`HomeView.swift:75-119`). هذا هو النمط
  الذي يجب تعميمه لا تغييره.
- **نوافذ الـ prefetch المحدودة** (40/30/6-8) — كلها صحيحة.
- **الذاكرة السالبة في `thumbHashImage`** (`DesignSystem.swift:1381`, `1396`) — تمنع استعلاماً
  لا يمكن أن يعيد شيئاً. أبقِها؛ فقط أخرج القراءة من تحت القفل.
- **`contentReady` كخاصية محسوبة** ترفع `contentGen` (`BlankTVApp.swift:31-37`) — إعادتها إلى
  `@Published Bool` مخزَّن ستُبقي التبويبات على الهيكل إلى الأبد (خطأ موثَّق).
- **المعاملة الواحدة** في `CatalogDB.save` — المشكلة في مشاركة الطابور لا في المعاملة.
- **نقل الاعتماد إلى الـ Keychain** (تغيير اليوم) — بريء تماماً من هذا التقرير.

---

## 7. ما لا أستطيع تحديده من الكود، وكيف يُحسم

| السؤال المفتوح | القياس الحاسم |
|-----------------|----------------|
| الزمن الحقيقي لـ `CatalogDB.save` على جهاز المالك | `os_signpost` حول جسم `CatalogDB.save` (`CatalogDB.swift:307-340`) + عدّاد صفوف؛ اقرأه في Instruments (Points of Interest) |
| الحجم الفعلي لكل حمولة JSON ولوحته | `print(data.count)` بعد كل `apiData` في `Core.swift:2105-2107`، وسجّل زمن كل طلب |
| هل ترد لوحة المالك بغير 2xx وتُعاد المحاولة 3×؟ | سجّل `status` داخل حلقة الـ UA (`Core.swift:1947`) — إن ظهر 403/5xx فالتنزيل يتضاعف ثلاث مرات |
| ذروة الذاكرة والاختناق | Instruments → Allocations + Memory Graph أثناء أول تسجيل دخول؛ راقب هل يتجاوز 700 MB |
| الزمن الفعلي لتجميد الـ main thread عند مرحلة 15 | Time Profiler، أو `MetricKit` (`Diagnostics.swift` موجود بالفعل) — ابحث عن `hangs` |
| كم من الثلاث دقائق شبكة وكم منها جهاز؟ | أبسط اختبار حاسم: شغّل الدخول مرتين متتاليتين. الثانية تصيب `CatalogDiskCache` (TTL = 12 ساعة، `Core.swift:1765`) وتتخطى الشبكة كلياً. الفرق بين الزمنين = تكلفة الشبكة بالضبط |

---

## 8. ترتيب التنفيذ الموصى به

1. **A1 + A2** — إصلاح صغير، بلا خطر، ويزيل السبب المهيمن. (يوم واحد بما فيه المراجعة)
2. **B1 + B5** — 3–8 ثوانٍ إضافية، خطر شبه معدوم.
3. **B2 + B3 + B7** — جولات شبكة، عبارات SQL، ذاكرة.
4. **B4** — تحويل إلى `DatabasePool`، ليصبح الخطأ مستحيلاً بنيوياً، ثم **B8**.
5. **A3** ثم **A4** — العرض التدريجي: هنا يتحول التطبيق من «أسرع» إلى «فوري».

كل بند خلف مراجعة عدائية قبل البناء، كما نصّ `PROJECT_HANDOFF.md §11`.
