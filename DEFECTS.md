# سجلّ الأعطال والقواعد المستخلَصة

هذا ليس تقريراً يُقرأ مرّة. هو **مصدر القواعد** التي تحكم العمل في هذا المستودع.

كل عطل هنا **وقع فعلاً**. لكلٍّ منه: ماذا حدث، لماذا مرّ، والقاعدة التي يُنتجها.
والعمود الأخير هو الأهم: **هل يُمسَك آلياً؟** ما يمكن أتمتته أُتمِت — لأن قاعدة
تعتمد على تذكّر الإنسان قاعدةٌ ستُخرَق.

## البوّابات

```
python chk.py        # بنية الملفات: توازن الأقواس + أرضية حجم
python scopechk.py   # عمق الأقواس: استعمال اسم بعد إغلاق نطاقه
python brandlint.py  # الهوية محصورة في S8KBrand
python uilint.py     # قواعد الواجهة المستخلَصة من هذا السجلّ
```

الأربع تُشغَّل قبل **كل** التزام. فشل أيّها = لا التزام.

---

## أولاً: أعطال اللمس والواجهة

### D-01 — توسيع منطقة اللمس **خارج** الـ`label`
**ما حدث:** `.s8kMinTouch` طُبِّق على الـ`Button` نفسه بدل محتواه.
**لماذا يمرّ:** يبدو صحيحاً تماماً، ويترجم بلا تحذير.
**النتيجة:** لا يوسّع شيئاً — الإيماءة تبقى على الشكل الصغير — **ويبتلع لمسات الجيران** فوق ذلك. أسوأ من عدم فعل شيء.
**القاعدة:** التوسيع **داخل** `label:` دائماً.
**آلي:** ✅ `uilint.py` → `touch-outside-label`

### D-02 — التوسيع **قبل** `clipShape`
**ما حدث:** `.s8kMinTouch(3)` ثم `.clipShape(Capsule())`.
**لماذا يمرّ:** يقرأ صحيحاً؛ والقصّ يبدو شأناً بصرياً.
**النتيجة:** `clipShape` **يقصّ اختبار اللمس أيضاً**، فالتوسيع يُلغى بصمت.
**القاعدة:** التوسيع **بعد** كل `clipShape` في السلسلة.
**آلي:** ✅ `uilint.py` → `touch-before-clip`

### D-03 — التوسّع في مساحة الجار
**ما حدث:** زرّان بينهما ٨pt، ووُسِّع كلٌّ منهما ٨pt في كل اتجاه.
**النتيجة:** كلٌّ يأكل مساحة الآخر — والنتيجة أسوأ من الهدف الصغير.
**القاعدة:** اقرأ `spacing` الحاوية أولاً. لا تتجاوز نصف الفجوة. في الصفوف الضيّقة استعمل `s8kMinTouchV` (رأسي فقط) أو `s8kMinTouch(h:v:)`.
**آلي:** ❌ يحتاج حساب تخطيط — **يُراجَع يدوياً**.

### D-04 — `.fill` على صورة نسبتها ليست نسبة الحاوية
**ما حدث:** بوستر عمودي ٢:٣ رُسم بـ`.fill` على ملء الشاشة في المشغّل.
**لماذا يمرّ:** `.fill` هو الافتراضي الذهني لخلفية «ملء الشاشة»، وهو صحيح للصور العريضة.
**النتيجة:** تكبير ~٣× وقصّ معظم الصورة، ثم يظهر الفيديو بنسبته → **يبدو أن المشغّل يتمدّد**. اكتشفه المالك على الجهاز، لا مراجعتان عدائيتان قبله.
**القاعدة:** `.fill` **فقط** حين تكون نسبة المصدر مضمونة مطابقة للحاوية. وفي هذا المشروع `backdropURL` قيمته `nil` دائماً لمحتوى Xtream — أي أن كل «خلفية» هي **بوستر عمودي**.
**آلي:** ✅ `uilint.py` → `fill-fullbleed`

### D-05 — عنصر تحميل يملأ الحاوية ثم ينكمش
**ما حدث:** `S8KImage` يعرض لمعاناً رمادياً يملأ المساحة المعطاة، ثم ينكمش إلى شكل الصورة.
**النتيجة:** **تغيير حجم مرئي ثانٍ** في الطريق إلى المحتوى.
**القاعدة:** في سياق ملء الشاشة، ارسم البتماب **الجاهزة في الذاكرة** أو لا ترسم شيئاً. لا تعرض حالة تحميل بحجم مختلف عن حالة المحتوى.
**آلي:** ❌ يحتاج حكماً بصرياً.

### D-06 — أنماط تخطيط محظورة
كلٌّ منها سبّب عطلاً حقيقياً هنا:
- `GeometryReader` كجذر عرض → اختفاء المقدّمة كاملةً.
- `ForEach(0..<متغيّر)` → انهيار عند تغيّر العدد.
- `UIScreen.main.bounds` → خطأ في النوافذ المقسّمة على iPad.
- `.frame(height:)` ثابت على صفّ نصّي → قصّ عند تكبير الخط.
- بوّابة/لوحة مفاتيح بلا `ScrollView` → حقول تحت لوحة المفاتيح.
- **فخّ التضخيم:** ابن يحمل `.ignoresSafeArea()` **يُضخّم** الـ`ZStack` الأب.
**آلي:** ✅ جزئياً — `uilint.py` → `banned-pattern`

---

## ثانياً: أعطال الحالة والتزامن

### D-07 — حالة خاصّة بالحساب لا تُمسَح في `reset()` ⚠️ **وقع مرّتين**
**ما حدث:** مساران سريعان خزّنا نتائج، و`reset()` لم يمسحهما. ثم: `inFlight` نفسه لم يكن يُلغى.
**النتيجة:** **قنوات وأفلام الحساب السابق تُقدَّم للحساب الجديد — وروابطها تحمل اسم مستخدمه وكلمة مروره.** ليس بياناً قديماً، بل **عبور اعتماديات**.
**القاعدة:** أي حقل يُشتقّ من الاشتراك — نتيجة، مهمّة جارية، ذاكرة مؤقّتة، فهرس — **يُمسَح في `reset()` في نفس الالتزام الذي يُضاف فيه**. وأي مهمّة قد تنجو من `reset()` تُحرَس بـ**عدّاد جيل**.
**آلي:** ⚠️ جزئياً — يُراجَع في كل مراجعة عدائية كسؤال ثابت.

### D-08 — تخزين نتيجة **فارغة** أو فاشلة
**ما حدث:** نتيجة `[]` من طلب فشل خُزِّنت كأنها جواب.
**النتيجة:** الشاشة تُثبّت علم «مُحمَّل» وترجع مبكراً للأبد → **متصفّح فارغ طوال الجلسة بلا خطأ يُعاد منه**.
**القاعدة:** الفراغ **ليس جواباً يُخزَّن**. إمّا يُعاد المحاولة، أو يُفوَّض إلى المسار الموثوق.

### D-09 — مسار سريع يسابق الشبكة ضدّ نسخة على القرص
**النتيجة:** فتحة دافئة كانت بلا شبكة صارت رحلتين؛ **وبلا إنترنت صار المسار السريع الشيء الوحيد الذي يفشل** — فوق كتالوج صالح على القرص.
**القاعدة:** قبل أي طلب «تسريع»، اسأل: هل يوجد جواب محلي؟ إن نعم — **لا شبكة**.

### D-10 — قراءة الذاكرة المؤقّتة **قبل** فحص صلاحيتها
**النتيجة:** نتيجة حُسبت على كتالوج فارغ عادت بعد وصول الكتالوج الحقيقي، **ولا شيء يصحّحها**.
**القاعدة:** **فحص الصلاحية أولاً، ثم الذاكرة المؤقّتة.** دائماً، بلا استثناء.

### D-11 — عمل مؤجَّل يعدّل حالة غير منشورة
**النتيجة:** `Task` مؤجَّلة تُصلح الحالة لكن **لا تُطلق إعادة رسم** — فالمستخدم يبقى على الشاشة الخاطئة حتى يتفاعل.
**القاعدة:** ما يُؤجَّل خارج مسار الرسم إمّا يُعدّل `@Published`، أو يُبنى **كسولاً عند القراءة** بدل الاعتماد على إعادة رسم لن تأتي.

### D-12 — `try` حيث المسار الكامل يستعمل `try?`
**النتيجة:** طلب «كمالي» (تصنيفات) صار بوّابة → **صفحة خطأ كاملة فوق مكتبة حُمِّلت بنجاح**.
**القاعدة:** المسار السريع يرث **تسامح** المسار الكامل حرفياً. أي اختلاف في معالجة الخطأ عيبٌ حتى يُثبَت العكس.

### D-13 — تصفير مُراكِم عند **الإصدار** بدل **الوصول**
**النتيجة:** ضغطة تقديم أثناء قفزة جارية كانت **ترجع للخلف**.
**القاعدة:** المُراكِم يبقى حتى **يصل رأس التشغيل إليه**، لا حتى يُرسَل الطلب.

---

## ثالثاً: أعطال المترجم والأدوات

### D-14 — تعريف داخل إغلاق يُستعمل بعد إغلاقه
`let` داخل `label:` استُعمل على مُعدِّل بعده → فشل بناء.
**آلي:** ✅ `scopechk.py`

### D-15 — `===` على `Task`
`Task` **بنية لا كائن**، فمقارنة الهوية لا تُترجم. أُمسك قبل الشحن.
**القاعدة:** لا `===` إلا على ما يُعرَف أنه `class`/`actor`. للهوية عبر الزمن: **عدّاد جيل**.

### D-16 — بوّابة لا تتحقّق من شيء ⚠️
`chk.py` كان يدور على قائمة فارغة ويخرج بنجاح دائماً. **جلسة كاملة ظنّت نفسها متحقَّقة.**
**القاعدة:** كل بوّابة **تُثبَت بإدخال العطل الذي تدّعي إمساكه** في نسخة مؤقتة والتأكد من فشلها.

### D-17 — ملف فارغ يوازن الأقواس
سكربت أفرغ ملفاً من ٧٠٥ أسطر إلى صفر، و`chk.py` نجح لأن الصفر متوازن.
**القاعدة:** أرضية حجم في البوّابة، وكل تعديل نصّي يُثبّت بموضع صريح ويُتحقّق من عدده.
**آلي:** ✅ `chk.py`

### D-18 — قياس مركَّب في النقطة الخطأ
مؤشّر التشغيل كان يبدأ عند تبديل القناة فقط، لا عند فتح المشغّل؛ ومؤشّر الإقلاع كان يُغلقه بوستر **لاحق** → **إقلاع سريع يُسجَّل بطيئاً**.
**القاعدة:** أداة قياس فيها عطل **أخطر من غياب القياس** — لأنها تمنح ثقة برقم خاطئ. كل مؤشّر جديد يُراجَع بسؤال: *ما هي كل المسارات التي تفتحه، وكل المسارات التي تُغلقه؟*

### D-19 — رسالة التزام تصف نصف محتواه
تعديلان في ملف واحد، والرسالة تصف واحداً.
**القاعدة:** الرسالة تصف **كل** ما في الالتزام، أو يُقسَّم.

### D-20 — مُنسِّق يتبع الجهاز بدل سياسة التطبيق
**ما حدث:** التطبيق يحقن لغة في البيئة، لكن أي `DateFormatter`/`NumberFormatter` يُبنى في الكود **لا يرى تلك البيئة** — يتبع الجهاز. فعلى هاتف عربي تظهر أرقام عربية-هندية في تطبيق سياسته الأرقام الغربية في كل اللغات.
**الأسوأ:** `ByteCountFormatter` **لا يملك خاصية لغة أصلاً** — لا يمكن تثبيته، فوجب استبداله بالكامل.
**القاعدة:** سياسة الأرقام تُثبَّت **مرّة واحدة** على مستوى البيئة (`-u-nu-latn`). وأي مُنسِّق يُبنى في الكود **يخرج** من تلك السياسة، فيجب أن يُصرّح بلغته صراحةً.
**آلي:** ✅ `uilint.py` → `formatter-no-locale`

### D-21 — أرقام متناسبة العرض في عدّاد يتغيّر
**ما حدث:** ساعة المشغّل تتغيّر كل ثانية بخطّ متناسب، فعرض «1» أضيق من «8» → **الصفّ كلّه ينزاح مع كل نبضة**، والشريط يبدو مهتزّاً. ونفس الأمر في أرقام الترتيب وشارات التقييم.
**لماذا يمرّ:** ليس عطلاً يمكن الإشارة إليه — هو ما يجعل الشاشة **تبدو هاوية**.
**القاعدة:** أي رقم يتغيّر في مكانه يُضبط بأرقام **جدولية** (`monospacedDigit`). وتُطبَّق على **الخطّ** لا على `Text`، حتى لا يتغيّر النوع المستنتَج.

---

## القاعدة الحاكمة على المراجعة نفسها

عطل **D-04** مرّ من **مراجعتين عدائيتين**. لأن كلتيهما سُئلت عن الترجمة والتزامن والبيانات — **ولم يُسأل أحد: كيف يبدو هذا على شاشة حقيقية؟**

**لذلك:** أي تغيير يمسّ ما يُرى — نسبة، مقاس، انتقال، حالة تحميل — يحمل في مراجعته سؤالاً صريحاً عن **الشكل**، لا عن الصحّة وحدها. ومراجعة الكود **لا تعوّض** الاختبار البصري على جهاز.
