# حالة الربط بين التطبيق واللوحة — 2026-08-22

> ورقة استئناف لمسار واحد: قياس التطبيق، وربطه بلوحة `panel.8k.site`.
> ليست تدقيقاً ولا خطّة شاملة. ما فيها **مُتحقَّق منه بالتنفيذ**، ومصادره مذكورة.

---

## 1 · لماذا بدأ هذا أصلاً

المالك أبلغ عن **تأخير ملحوظ عند تشغيل الأفلام والمسلسلات، دون القنوات**.

قراءة الشيفرة أعطت **يقيناً واحداً وفرضية واحدة**، وشحن أيٍّ منهما إصلاحاً كان سيكون تخميناً:

**اليقين.** `BasePlayerVM.resolvedURL` يفحص التنزيلات للأفلام والحلقات فقط — `case .live: break` يتخطّاه — والفحص يُنشئ مجلد التنزيلات إن غاب ثم **يُعدّده**. يُستدعى ٣–٤ مرّات لكل فتح، إحداها من داخل `PlayerView.body`.

**الفرضية.** الفيلم المُشاهَد جزئياً قد يدفع انتظارة الفتح **مرّتين**: VLC يملأ مخزنه من بداية الملف ويعرض صورة، **ثم** يحرّك `resumeIfNeeded` الموضع — فيرمي المخزن ويطلب نطاقاً جديداً ويعيد التحليل. القناة لا نقطة استئناف لها ولا تبلغ ذلك السطر إطلاقاً.

**الحلّ المبحوث، إن أكّده القياس:** خيار `:start-time=` في libVLC. من المصدر لا من منتدى — في [`src/input/input.c` فرع 3.0.x](https://github.com/videolan/vlc/blob/3.0.x/src/input/input.c) تقرأه `StartTitle()` أثناء `Init()` وتدفع `INPUT_CONTROL_SET_TIME` إلى طابور التحكّم، فيُنفَّذ في **بداية** `MainLoop` — قبل تغذية المفكِّكات وملء المخزن، **وبلا اشتراط قابلية سعي ولا تمييز شبكة عن ملف**. دورة تخزين واحدة بدل اثنتين.

تطبيق VLC الرسمي يضبط `position` بعد التشغيل (`VLCPlaybackService.m`)، لكن حالته الأساسية ملفات محلّية والقفزة فيها مجانية. **لا يُنقل على الثقة.**

---

## 2 · ما شُحن — التطبيق

### البناء 153 (على `main`، مدموج)

ستّ مراحل تُقاس من اللمسة إلى الصورة، وتظهر في **الإعدادات ← قياس السرعة** مع زرّ نسخ:

| المرحلة | تفصل |
|---|---|
| `اللمسة ← المشغّل` | كلفتنا نحن: عرض الغطاء، بناء الشاشة، اختيار المحرّك |
| `اختيار المحرّك` | فكّ ذاكرة القرارات من UserDefaults |
| `حلّ الرابط` ×N | تعداد مجلد التنزيلات — تجميعي لأنه متكرّر |
| `بدء المحرّك` | هل حُجب الخيط الرئيسي عند تسليم الرابط |
| **`قفزة الاستئناف`** | **الفرضية الرئيسية** |
| `التشغيل ← أول إطار` | المجموع |

والتقرير المنسوخ **يبدأ برقم البناء**. ضاع تشخيص كامل في 21 أغسطس على تأخير مُبلَّغ عنه في «150» بينما App Store Connect تقول لم يثبّته أحد، ولا شيء في النصّ كان يحسم أيّ ثنائية أنتجته.

> **لم يصل القياس بعد.** المطلوب من المالك: فيلم شُوهد جزئياً · فيلم لم يُفتح قط · قناة — ثم نسخ التقرير. الفرق بين الثلاثة يحسم الفرضية وحده.

### فرع `feat/panel-telemetry` — **أخضر، غير مدموج**

`d54b433` · بناء `ios-test` نجح: تصريف · محاكي · 158 اختباراً.

`BlankTV/PanelClient.swift` — تسجيل مرّة، نبض كل 45ث أثناء التشغيل حاملاً عنوان ما يُشاهَد، ودفعات أحداث CTA-2066. **لا توجد فيه دالّة تقرأ سلوكاً من اللوحة** — قيدٌ لا سهو.

الأحداث كلّها في `BasePlayerVM`، والمحرّكان يرثانها فلا يتباعدان: `playback_requested` · `playback_started` (مع `startup_ms` و`resume`) · `playback_failed` · `rebuffer` · `playback_ended`.

**عيبان أُمسِكا أثناء الكتابة:** `setItem` كان سيُغلق الجلسة بعد إسناد المحتوى الجديد (فيُقيَّد الفيلم المتروك على القناة المفتوحة)؛ وإعادة التخزين تُحسب بعد الإطار الأول فقط، وإلا حُسب انتظار البدء مرّتين.

---

## 3 · ما شُحن — اللوحة

`/var/www/blanktv-panel` · Laravel 13.21 · Filament 5.7 · PHP 8.3 · MySQL · Redis

**صارت تحت Git**، ولم تكن: خطّ أساس `ae95182` (130 ملفاً) ثم `fc8de44`. أي تراجع أمرٌ واحد.

### العيب الذي وجدته وأُغلق

```
POST /api/v1/heartbeat  {"device_id":"zz"}  →  {"ok":true}
```

بلا مصادقة، وبلا صيغة، وبلا ربط بجهاز معروف. **أنشأ فعلاً سجلّ جهاز وجلسة حيّة** (حُذفا). لوحة تقول «متصل الآن» فوق نقطة يحقنها أي أحد ليست لوحة حقائق.

الآن: القراءات عامّة، وكل **كتابة** خلف رمز لكل جهاز مربوط بـ`device_id` في الجسم. `/register` هو الباب الوحيد المفتوح — بمفتاح تطبيق وحدّ 20/دقيقة، ويكتب صفّاً واحداً قابلاً للتتبّع.

### مُتحقَّق منه حيّاً على الخادم

| الاختبار | النتيجة |
|---|---|
| تسجيل بلا مفتاح | `{"ok":false,"error":"bad_app_key"}` |
| تسجيل بمفتاح | رمز 64 محرفاً |
| نبضة برمز صالح | `{"ok":true}` |
| **رمز صالح مع `device_id` آخر** | **`unauthenticated`** |
| دفعة حدثين | `{"ok":true,"accepted":2}` |
| قراءة اللوحة | وسيط 3,120ms · جلسة حيّة واحدة |

بيانات الاختبار كلّها حُذفت بعده.

### `PlaybackQoE` — لوحة رباعية المعيار

[CTA-2066](https://www.forasoft.com/learn/video-quality/articles-vqm/streaming-qoe-metrics): زمن البدء (**وسيط وp95، لا متوسط** — المتوسط يُخفي الحالة النمطية والحالة التعِسة معاً) · معدّل الفشل · نسبة إعادة التخزين (**زمن ÷ زمن**، لا عدد توقّفات) · الخروج قبل بدء الفيديو.

**وجدول فارغ يقول إنه فارغ.** صفٌّ من أصفار واثقة فوق لا بيانات هو العطب الذي بُني هذا كلّه لمنعه.

---

## 4 · مطلوب من المالك — بالترتيب

1. **`BLANKTV_APP_KEY` في متغيّرات بيئة Codemagic.** القيمة في `.secrets/blanktv_app_key`. بدونه البناء ينجح والقياس **معطّل** — ويقول ذلك في السجلّ بدل أن يصمت.
2. **القياسات الثلاث** من البناء 153 (فيلم مستأنف · فيلم جديد · قناة) ← الإعدادات ← قياس السرعة ← نسخ الكل.
3. **قرار: أجهزة الاختبار الستّ من يوليو** (`hb-1`, `lb-a…d`, `dev-uuid-001`) — تُحذف؟ كلّها من `curl` من داخل الخادم يوم بنائه.
4. **`S-2`**: `hosta.example` و`hostb.example` ما زالا يُقدَّمان لكل من يسأل `/api/v1/hosts`.
5. **`A-10` النطاق** — `brandlint` يفشل عليه عمداً، ويُغلق أربعة حواجز دفعة.

---

## 5 · ما تبقّى من العمل

- **دمج `feat/panel-telemetry`** — أخضر، لكنه غير مقروء سطراً سطراً بعد. *الفرع غير المدموج ليس شيفرة مُراجَعة.*
- **الإصلاح** — بعد وصول القياس. لا قبله.
- `S-1` (كود مضيف مجهول يُرجع مضيفاً) · `S-4` HSTS · `S-6` إخفاء إصدار nginx · `S-7` 2FA · `S-8` تحديد المعدّل
- تنظيف الجلسات الميّتة بـTTL على الخادم (النافذة 120ث مضبوطة في `config/blanktv.php`، وما ينظّف الصفوف القديمة لم يُكتب بعد)

---

## 6 · قرارات مُتّخذة، لا تُعاد مناقشتها بلا سبب

**المفتاح ليس في المستودع.** كنتُ سأضعه في الشيفرة، وكان سيكون خطأً جسيماً في نموذج البيع: المشتري يستلم المصدر فيستلم مفتاح لوحة المالك. يُحقن الآن وقت البناء (`codemagic.yaml`)، وبدونه العميل صامت — فبناء محلّي لا يُبلّغ إلى اللوحة الحيّة بالخطأ.

**عنوان اللوحة ثابت بناء**، لا قيمة تصل عبر الشبكة. ما يصل عبر الشبكة يمكن تغييره بعد المراجعة.

**لا OpenTelemetry كاعتماد.** [مقاييسه وسجلّاته للـSwift ما زالت قيد التطوير](https://github.com/open-telemetry/opentelemetry-swift) والتطبيق يواجه موعد CocoaPods في ديسمبر. **أسماء الحقول تتبع اصطلاحاته** — نكسب المعيار ونؤجّل التبعية، ويصير الانتقال لاحقاً تغييرَ ناقلٍ لا تغييرَ نموذج بيانات.

**لا Laravel Reverb الآن.** [صار الافتراضي في 2026 وهو مجّاني](https://redberry.international/laravel-reverb-real-time-communication/)، لكن تحديثاً دورياً لعناصر Filament يكفي. يُضاف حين تصير الثانية الواحدة فارقة.

**تصحيح لموقفي السابق — مرّتين بالغتُ:**
- **الخصوصية ليست حاجزاً.** بنصّ Apple: ما يُعالَج على الجهاز وحده **ليس «جمعاً»**؛ والتشخيص إلى خادمك **ليس «تتبّعاً»** (التتبّع = ربط ببيانات طرف ثالث للإعلان، أو وسيط بيانات). لا نافذة ATT. الكلفة صناديق تُؤشَّر.
- **[2.5.2 يمنع الشيفرة لا البيانات](https://www.revenuecat.com/blog/engineering/ios-remote-testing-ban).** الإعداد البعيد وأعلام الميزات ممارسة معتمدة في ملايين التطبيقات. الخطر الحقيقي **2.3.1**: سلوك لم يره المراجع. فيبقى ممنوعاً حقاً: **إطفاء الرقابة الأبوية عن بُعد** — ميزة سلامة اعتمد عليها المراجع والتصنيف العمري.

**ومع ذلك، `PanelClient` لا يقرأ شيئاً.** ليس لأن القراءة ممنوعة، بل لأن عميلاً يمكن أن يُؤمَر يجعل كل سؤال لاحق عن هذا التطبيق سؤالاً عمّا قيل له وقتها.

---

## 7 · درس من هذه الجلسة

نشرتُ `PlaybackQoE.php` على الإنتاج **قبل** أن أُحمّل الصنف ولو مرّة. كان فيه خطأ قاتل (`$pollingInterval` ساكنة بينما Filament 5 تعرّفها غير ساكنة)، وFilament تكتشف اللوحات تلقائياً — فكانت الصفحة الرئيسية ستفشل عند دخول المالك.

القاعدة موجودة في هذا المشروع منذ شهر: **البوابة لا تُوثَق حتى يُعاد العيب ويُرى وهو يفشل.** خالفتُها.

**من الآن: كل ملف PHP يُحمَّل بـ`php artisan tinker` قبل أن يلمس مجلّداً تكتشفه Filament.**
