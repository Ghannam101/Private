# تشخيص عطلين مؤكَّدين على الجهاز — التنزيل في الخلفية + بطء التقديم

_تشخيص فقط. لم يُعدَّل أي ملف مصدري. كل الأسطر مذكورة بصيغة `file:line` مقابل الحالة الحالية للمستودع (2026-07-28)._

---

# العطل 1 — التنزيل يتجمّد عند 1% بعد إغلاق التطبيق

## الخلاصة في سطر واحد
التقدّم **لا يُحفظ على القرص إطلاقًا** أثناء التنزيل (`updateProgress` في `BlankTV/Downloads.swift:431-439` يعدّل الذاكرة فقط ولا يستدعي `persist()`)، و`reconcileOnLaunch` (`Downloads.swift:94-109`) يقلب أي عنصر `.downloading` إلى `.paused` اعتمادًا على `taskDescription` — وهو مفتاح غير موثّق للبقاء بعد إعادة تشغيل العملية — ثم **لا يوجد أي مسار يُعيد تشغيل عنصر `.paused` تلقائيًا**؛ فيبقى الصف مجمّدًا عند آخر رقم كُتب، وهو 1%.

## فحص كل نقطة، بالتأكيد أو النفي

### 1) إعدادات الجلسة — `Downloads.swift:61-81`
| البند | السطر | الحكم |
|---|---|---|
| `URLSessionConfiguration.background(withIdentifier: "com.blanktv.player.downloads")` | 62 | ✅ صحيح |
| `sessionSendsLaunchEvents = true` | 63 | ✅ صحيح |
| `isDiscretionary = false` | 64 | ✅ صحيح |
| `httpMaximumConnectionsPerHost = 6` | 78 | ✅ غير ضار |
| `waitsForConnectivity = true` | 79 | ⚠️ **بلا أثر** |
| `allowsCellularAccess/Expensive/Constrained = !downloadWifiOnly` | 70-73 | ⚠️ **خطر حقيقي، مشروط** |

**هل `waitsForConnectivity = true` مع قيد Wi‑Fi يُوقف المهمة إلى الأبد على بيانات الجوّال؟**
الجواب الدقيق: `waitsForConnectivity` نفسها **مُهمَلة في الجلسات الخلفية** — Apple توثّق أن الجلسة الخلفية تنتظر الاتصال دائمًا بغضّ النظر عن هذه الخاصية. فالسطر 79 لا يضيف شيئًا ولا يسبّب شيئًا.
لكن الخطر موجود ومصدره الأسطر 70-73: إذا كان `downloadWifiOnly` مفعَّلًا والجهاز على بيانات الجوّال، فإن المهمة الخلفية **لا تفشل** — بل تنتظر بصمت، بلا مهلة، بلا خطأ، وبلا أي مؤشّر في الواجهة. الشريط يتجمّد عند مكانه بالضبط. هذا سيناريو "1% ثم توقّف" حرفيًا.
القيمة الافتراضية `false` (`Core.swift:883-885` — `ud.bool` يُرجِع `false` عند عدم الضبط)، والمفتاح موجود في الإعدادات (`SettingsView.swift:475-476`). **يجب سؤال المالك: هل فعّل "التحميل على Wi‑Fi فقط"؟**

### 2) مسار "Turbo" المتوازي — `Downloads.swift:194-198, 248-355`
- **مبني فعلًا خارج الجلسة الخلفية**: `URLSessionConfiguration.default` في السطر 250، داخل `Task { … }` في السطر 195. و`Task` يُجمَّد مع تجميد التطبيق، ومهام `URLSession` العادية تموت عند تعليق العملية. **إذن Turbo لا يمكنه النجاة من الخلفية إطلاقًا.** مؤكَّد.
- **الافتراضي؟** `false` (`Core.swift:878-880`). لكن هنا مفاجأة تستحق الانتباه:
  **مفتاح Turbo كان له زرّ في الإعدادات القديمة، وحُذف الزر ولم تُنظَّف القيمة.** الالتزام `afbf308` ("Adopt Settings V2…") حذف:
  ```
  toggleRow(icon: "bolt.fill", … title: L("downloads.turbo"), desc: L("downloads.turbo.desc"), isOn: $turboOn)
      .onChange(of: turboOn) { _, v in Store.shared.turboDownloads = v }
  ```
  أي مستخدم فعّل Turbo في نسخة سابقة ما زال يحمل `s8k.turboDownloads == true` في `UserDefaults`، و**لا توجد اليوم أي واجهة لإطفائه**. عطل خفيّ وغير قابل للإصلاح من جهة المستخدم.
- **هل هو تفسير 1% تحديدًا؟** غالبًا لا: Turbo يحدّث التقدّم عند اكتمال كل مقطع فقط (`Downloads.swift:301-311`)، فيقفز 0% → 33% → 66%، ولا يعرض 1%. الدقّة الناعمة (1%) تأتي من `didWriteData` أي من **المسار الخلفي**. لذلك: Turbo خطر مؤكَّد يجب إغلاقه، لكنه على الأرجح ليس سبب ما رآه المالك.

### 3) `reconcileOnLaunch()` — `Downloads.swift:94-109` — **نعم، يمكنه تعليم مهمة حيّة بأنها `.paused`**
مجموعة "الحيّ" تُبنى من `taskDescription` (السطر 97). `taskDescription` خاصية داخل العملية، ومفتاح إعادة الربط الموثّق من Apple هو `taskIdentifier`. إذا عادت `nil` بعد إعادة تشغيل العملية:
- `live` تصبح `{""}` → كل عنصر `.downloading` يُقلب إلى `.paused` (101-104) **بينما النقل ما زال حيًّا في `nsurlsessiond`**؛
- `pump()` (106) يشغّل عناصر `.queued` فقط، و`.paused` ليس منها → لا شيء يستأنف؛
- ثم تصل استدعاءات المفوّض بـ `id == ""` (548, 559) → `updateProgress`/`finish` لا يجدان الفهرس، **والفيلم المكتمل يُنقل إلى `dl_..mp4`** (571) ويضيع.
وحتى لو نجت `taskDescription`: **لا يوجد استئناف تلقائي على الإطلاق**. أي انقطاع = صفّ مجمّد إلى الأبد حتى يضغط المستخدم يدويًا (`resume`، 412-418).

### 4) المفوّض والتقدّم — `Downloads.swift:545-552` و`431-439`
- `urlSession(_:downloadTask:didWriteData:…)` **موصول بشكل صحيح** ✅.
- لكن `updateProgress` (431-439) **يعدّل `items` في الذاكرة فقط ولا يستدعي `persist()`**. البحث الكامل: `persist()` تُستدعى من `enqueue` (174)، `setState` (423)، `finish` (450)، `fail` (458)، `reconcileOnLaunch` (105) — أي عند **تغيّر الحالة فقط**.
  النتيجة: طوال عمر التنزيل، الملف `downloads.json` يقول `receivedBytes = 0`. وبعد أي إنهاء للعملية، يُقرأ الصف عند قيمته القديمة، ثم `reconcile` يكتب `.paused` + `persist()` **فيُثبِّت الرقم القديم كحقيقة**. هذا هو "عالق عند 1%" الذي يراه المالك. **مؤكَّد ولا يحتاج جهازًا لإثباته.**

### 5) `handleEventsForBackgroundURLSession` — `BlankTVApp.swift:85-92`
الشكل صحيح ✅: يخزّن المُكمِّل، ويُفرِغ مُكمِّلًا عالقًا سابقًا (90)، و`urlSessionDidFinishEvents` يستدعيه ويصفّره (`Downloads.swift:593-598`).
**لكن فيه ثغرة حقيقية**: هذا هو المكان **الوحيد** الذي يلمس `DownloadService.shared` عند الإقلاع. البحث الكامل يعطي: `BlankTVApp.swift:90-91`، `Services.swift:345` (حذف الحساب)، و`Downloads.swift:637/723` (مُهيّئات خاصيتين داخل `View`). أي أنّ **الإقلاع العادي (المستخدم يفتح التطبيق) لا يُعيد إنشاء الجلسة الخلفية أبدًا** حتى يفتح صفحة تفاصيل فيلم أو شاشة التنزيلات. قبل ذلك: لا مفوّض، لا تسليم لأحداث مكتملة، ولا `reconcile`، ولا `pump`.

### 6) فحص الانحدار — تغيير Keychain اليوم — **بريء، بشكل قاطع**
- **لا يوجد أي مسار تنزيل يقرأ `Store.m3uURL`.** رابط التنزيل يُحلّ **مرّة واحدة** عند الإضافة (`Downloads.swift:140/151` → `BasePlayerVM.remoteURL`، `PlayerEngine.swift:152-164`) ويُخزَّن كنص داخل العنصر (`remoteURL`، `Downloads.swift:31`)، وإعادة التشغيل تستخدم `it.remoteURL` (190). لا Keychain ولا `Store` في مسار الإقلاع.
- `remoteURL` يقرأ `Keychain.serverCredentials()` (`Core.swift:1273-1284`) — Keychain مباشرة، بلا قفل، وفي المقدّمة فقط.
- `NSLock` في `Store.m3uURL` (`Core.swift:986-1031`) غير عودي لكنه **لا يُعاد الدخول إليه** (الـ getter/setter ينادي `Keychain.shared.m3uURL`، كائن آخر)، ويُمسك لأجزاء من الميلي‑ثانية حول استدعاء `SecItem`. لا احتمال Deadlock.
- `Keychain.upgradeAccessibilityIfNeeded()` (`Core.swift:751-758`، مُستدعى من `Services.swift:18`) عملية لمرّة واحدة خلف علم في `UserDefaults`، 8 قراءات/كتابات Keychain متزامنة على الخيط الرئيسي = ميلي‑ثوانٍ.
- والأهم: `kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly` (`Core.swift:781`) **يُحسِّن** حالة الإقلاع الخلفي (كان `WhenUnlocked` يعني قراءة `nil` على جهاز مقفل).
**الحكم: تغيير اليوم لم يساهم في العطل، لا سلبًا ولا في الطريق.**

## قائمة الإصلاح مرتّبة (الأثر مقابل الخطر)

| # | الإصلاح | الأثر المتوقّع | الخطر |
|---|---|---|---|
| **F1** | حفظ التقدّم: استدعاء `persist()` من `updateProgress` (`Downloads.swift:431-439`) مخنوقًا (كل ~2 ثانية أو كل 1%)، وحفظ إجباري عند `scenePhase == .background` | النسبة تصبح حقيقية بعد أي إعادة تشغيل. **هذا وحده يُنهي عرض "1%" الوهمي** | ضئيل جدًا (اخنق الكتابة على القرص) |
| **F2** | التوقّف عن الاعتماد على `taskDescription`: خزّن `taskIdentifier` داخل `DownloadItem` وأعِد الربط به في `reconcileOnLaunch` والمفوّض (مع `taskDescription` كاحتياطي) | يقتل صنفًا كاملًا: مهمّة حيّة تُعلَّم "متوقفة"، وملف مكتمل يُحفظ باسم `dl_..mp4` ويضيع | منخفض (يحتاج ترحيلًا للعناصر القديمة) |
| **F3** | استئناف تلقائي عند الإقلاع: أضف علم `pausedByUser` إلى `DownloadItem`؛ وكل ما أوقفه `reconcile` (لا المستخدم) يُعاد إلى `.queued` ثم `pump()` | التنزيل يُكمِل نفسه، وهو ما يتوقّعه المالك حرفيًا | منخفض |
| **F4** | لمس `DownloadService.shared` في `application(_:didFinishLaunchingWithOptions:)` | إعادة ربط الجلسة في كل إقلاع، لا فقط عند فتح شاشة التنزيلات | معدوم |
| **F5** | Turbo: إمّا حذف المسار كليًا، أو ترحيل لمرّة واحدة يكتب `Store.shared.turboDownloads = false` لتصفير القيم العالقة من النسخ القديمة | إزالة عطل غير مرئي ولا يمكن للمستخدم إطفاؤه | معدوم (المسار غير قابل للتفعيل اليوم) |
| **F6** | Wi‑Fi‑only: عند تفعيله وكون المسار خلويًا، اعرض حالة صريحة "في انتظار Wi‑Fi" (`NWPathMonitor`) بدل شريط مجمّد | لا انتظار صامت بعد اليوم | منخفض |
| **F7** | سؤالان للمالك يغلقان الملف: (أ) هل "التحميل على Wi‑Fi فقط" مفعّل؟ (ب) هل أغلق التطبيق بالسحب من مبدّل التطبيقات أم خرج منه فقط؟ | يفصل بين F6 وبين سلوك النظام | — |

## "هذا أقصى ما يمكن"
سطر واحد صادق: **إذا سحب المستخدم التطبيق من مبدّل التطبيقات (Force‑Quit)، فإن iOS يُلغي النقل الخلفي ولا يُعيد تشغيل التطبيق تلقائيًا — هذه سياسة نظام لا يصلحها كود.** الجواب الصحيح الوحيد هناك هو الاستئناف التلقائي عند الفتح التالي (F3). أمّا كل ما تبقّى أعلاه فهو عيب في الكود وقابل للإصلاح بالكامل.

---

# العطل 2 — التقديم في الأفلام والمسلسلات ما زال بطيئًا

## الخلاصة في سطر واحد
كل فيلم وكل حلقة تعمل على **VLC** لا على AVPlayer، والسحب على الشريط يُصدر **قفزة شبكية حقيقية كل 250 مللي‑ثانية** (`VLCPlayer.swift:397-402`) بينما قفزة الهبوط في `endScrub` لا تُنفَّذ إلا بعد انتهاء القفزة الجارية — أي أن المستخدم ينتظر **قفزتين شبكيتين كاملتين** بعد رفع إصبعه؛ وزرّا ±10 على VLC بلا تجميع ولا تراكم (`VLCPlayer.swift:366-369`) بعكس AVPlayer.

## المحرّك الذي يشغّل فيلم Xtream فعلًا — مؤكَّد
`PlayerEngineSelector.initialKind` (`PlayerEngine.swift:757-768`) → التفضيل الافتراضي `"auto"` (`Core.swift:869-871`) → `EngineDecisionCache` → `StreamRouter.defaultEngine` (`StreamRouter.swift:45-49`) الذي يُرجِع `.av` **فقط** إذا احتوى الرابط `.m3u8` (`StreamRouter.swift:31`).
وروابط Xtream: `…/movie/user/pass/<id>.<ext>` و`…/series/user/pass/<id>.<ext>` (`Core.swift:1277-1284`) — أي `mkv`/`mp4`/`avi`، **ولا تكون HLS أبدًا**. البثّ المباشر وحده `.m3u8` (`Core.swift:1273-1276`).
**الحكم: 100% من محتوى VOD عند المالك يعمل على VLC.** والملف المُنزَّل محليًا أيضًا يُوجَّه إلى VLC (`StreamRouter.swift:47`).

## هل `:input-fast-seek` خيار حقيقي في libVLC؟ — **نعم، وهو ليس مُهمَلًا — لكنه لا يشتري ما نظنّه**
- `Podfile:7` → `pod 'MobileVLCKit', '~> 3.6.0'` = libVLC **3.0.x**.
- في `src/libvlc-module.c` (فرع 3.0.x): `add_bool( "input-fast-seek", false, "Fast seek", "Favor speed over precision while seeking", false )` — خيار **أساسي** في فئة الإدخال.
- في `src/input/input.c`: يُقرأ في `StartTitle()` كـ `priv->b_fast_seek`، ثم يُمرَّر **معكوسًا** كوسيط الدقّة إلى `demux_Control( …, DEMUX_SET_POSITION, f_pos, !b_fast_seek )` و`DEMUX_SET_TIME`.
- إذن: الاسم صحيح، والخيار مُطبَّق فعلًا (`VLCPlayer.swift:287`). **لكن ما يوفّره هو عمل المُفكِّك (demuxer) فقط**: يمنعه من المشي للأمام من الإطار المفتاحي إلى الإطار المطلوب بالضبط. **لا يوفّر بايتًا واحدًا من تكلفة الشبكة.** المكسب المتوقّع: عشرات المللي‑ثوانٍ. لهذا لم يشعر المالك بأي فرق. أبقِه — لكنه ليس الإصلاح.

## ماذا يفعل libVLC فعليًا عند `player.position = x` على مصدر HTTP VOD؟
`libvlc_media_player_set_position` → `input_Control(INPUT_CONTROL_SET_POSITION)` = **طلب يُصفّ في طابور خيط الإدخال**، لا عملية فورية. الطابور **يدمج** الطلبات المتتالية من النوع نفسه (`ControlGetReducedIndexLocked` في `input.c` يتخطّى كل `SET_POSITION`/`SET_TIME` عدا الأخير) — لكن **الطلب الجاري تنفيذه يجب أن ينتهي أولًا**. والتنفيذ يعني:
1. المُفكِّك يطلب إزاحة جديدة من طبقة التدفّق؛
2. وحدة `access/http` تُصدر **طلب GET جديدًا برأس `Range: bytes=…`** — وغالبًا اتصال TCP/TLS جديد لأن الاتصال السابق كان في منتصف جسم الاستجابة؛
3. ثم يجب ملء `network-caching` — الآن **1000 مللي‑ثانية** (`VLCPlayer.swift:282`) — من بيانات التدفّق **قبل** عرض أول إطار؛
4. ثم فكّ الترميز من الإطار المفتاحي السابق.

**الأرضية الواقعية لملف MKV بحجم 2GB عن بُعد على خط منزلي:**
RTT إلى اللوحة (30–150ms) + زمن أول بايت من لوحة Xtream محمّلة (100–500ms) + 1000ms تخزين مؤقت + فكّ ترميز ≈ **0.8 – 2.5 ثانية لكل قفزة**، وأسوأ على خطّ مخنوق.

**بصراحة تامّة: "فوري" غير قابل للتحقيق لأي VOD عن بُعد — في هذا التطبيق ولا في VLC نفسه ولا في Smarters ولا TiviMate ولا Infuse.** الفوريّة موجودة في حالة واحدة فقط: ملف محلي/مُنزَّل. لذلك تحت ~700-800ms، العمل المتبقّي الصادق هو **التغذية الراجعة** (مؤشّر + الوقت الهدف) لا مزيد من السرعة.

## هل زرّا ±10 والشريط يسلكان مسارين مختلفين؟ — نعم، وبتكلفتين مختلفتين
**الشريط (scrub):** `PlayerView.swift:945-947` → `VLCPlayer.swift:397-402` (خنق 4Hz) ثم `endScrub` (403-408).
سحبة بطول ثانيتين = حتى **8 قفزات شبكية حقيقية**، وقفزة الهبوط تنتظر انتهاء القفزة الجارية → المستخدم يدفع ثمن قفزتين كاملتين بعد رفع إصبعه.

**زرّا ±10 والنقر المزدوج:** `PlayerView.swift:889/904` و`680-686` → `VLCPlayer.swift:366-369` → `player.jumpForward/Backward`، والذي يُحسب من `player.time` — **قيمة قديمة أثناء قفزة جارية** — **بلا تجميع وبلا تراكم**.
للمقارنة: AVPlayer يملك الاثنين (`lastRequestedTime` + `chaseSeek`، `PlayerEngine.swift:557-565` و`599-626`). **VLCPlayerVM لا يملك أيًّا منهما** — وهو المحرّك الذي يشغّل كل الأفلام.
النتيجة: ثلاث نقرات سريعة = **ثلاث قفزات شبكية كاملة**، كلٌّ محسوبة من قاعدة قديمة، بينما شارة النقر المزدوج تَعِد بـ "+30 ثانية".

**أيّ الاثنين يتفاعل معه المالك على الأرجح؟** زرّا ±10 والنقر المزدوج: أرخص إيماءة للتكرار، وأكثر موضع تتناقض فيه النتيجة مع ما تقوله الواجهة (الشارة تقول 30 والمُشغِّل يقفز 10). ثم الشريط في المرتبة الثانية.

## هل بقي شيء متزامن على الخيط الرئيسي في مسار القفز؟
لا شيء **يحجب على الشبكة**: ضبط `position` و`jumpForward` يكتفيان بصفّ طلب تحكّم. لكن:
- `beginScrub` (`VLCPlayer.swift:389-396`) يوقف التشغيل على الخيط الرئيسي، وكل استدعاءات المفوّض على الخيط الرئيسي (`VLCPlayer.swift:578`)؛
- `endScrub` (403-407) يضبط الموضع ثم يستدعي `player.play()` في **نفس دورة الـ runloop** — طلبَا تحكّم متتاليان، والثاني يعيد تشغيل المفكّك على مخزن مؤقّت فارغ.
التكلفة الحقيقية ليست المعالج على الخيط الرئيسي — بل **قفزات شبكية لا داعي لإصدارها أصلًا**.

## فجوة التغذية الراجعة (وهي على الأرجح نصف الشكوى)
`buffering` تُضبط فقط من حالة `.buffering` القادمة من VLC (`VLCPlayer.swift:581-582`) وتُمسح فور تقدّم الزمن (`619-621`)، والواجهة تؤخّر إظهار المؤشّر 0.3 ثانية وتُبقيه 1.2 ثانية (`PlayerView.swift:1108-1130`).
أثناء السحب **المُشغِّل متوقّف** (`VLCPlayer.swift:394`) فالزمن لا يتقدّم وقد لا تصل حالة `.buffering` أصلًا → **صورة مجمّدة بلا أي إشارة أن شيئًا يحدث**. مهما كان الزمن الحقيقي، هذا ما يجعله يبدو معطوبًا.

## هل الأفضل تحويل VOD إلى AVPlayer؟
**لا، ليس كتحويل شامل.** AVPlayer يتفوّق فعلًا في القفز على MP4 التقدّمي (`preferredForwardBufferDuration`، تسامح محدود 0.6s، و`chaseSeek` منفَّذ مسبقًا) — لكن مكتبة المالك Xtream وهي `.mkv`/`.avi`/`.ts`:
- **MKV غير مدعوم في AVFoundation إطلاقًا**، و**AVI كذلك**، وTS الخام مدعوم جزئيًا فقط؛
- صوت DTS/AC3/EAC3 داخل MKV، وترجمات ASS/SRT المدمجة، وبعض أنماط H.265 — كلها تسقط.
التحويل الشامل سيكسر عناوين أكثر ممّا يُسرّع.
**النسخة المدافَع عنها**: توجيه VOD إلى AVPlayer **فقط عندما تكون اللاحقة `.mp4`/`.m4v`/`.mov`** — والتصنيف موجود أصلًا (`StreamRouter.swift:35`) — مع إبقاء VLC لكل ما عداه، ومع شبكة الأمان الموجودة (failover + `EngineDecisionCache`). الخطر: صنف جديد من الأعطال على mp4 بترميز صوتي غريب، مُخفَّف بالـ failover القائم.

## قائمة الإصلاح مرتّبة (الأثر مقابل الخطر)

| # | الإصلاح | الأثر المتوقّع | الخطر |
|---|---|---|---|
| **1** | **إيقاف قفزات المعاينة الشبكية أثناء السحب** (`VLCPlayer.swift:397-402`): أثناء السحب يُحدَّث الوقت المعروض فقط، وتُصدَر **قفزة واحدة** في `endScrub`. أبقِ المعاينة الحيّة للملفات المحلية فقط (`StreamRouter.classify(...).isLocalFile`) حيث هي مجانية | يحذف **قفزة شبكية كاملة (0.8–2.5s)** من زمن الهبوط المُحسّ، ويلغي حتى 8 طلبات Range مهدورة لكل سحبة | لا معاينة حيّة أثناء السحب — وهو ما يحدث عمليًا اليوم (صورة مجمّدة) |
| **2** | **إعطاء VLC ما يملكه AVPlayer**: `lastRequestedTime` + تجميع/مطاردة في `skip` (`VLCPlayer.swift:366-369`) | ثلاث نقرات = **قفزة واحدة** تهبط حيث تقول الشارة. أكبر مكسب محسوس على أكثر زرّ استخدامًا | منخفض |
| **3** | **حالة قفز صريحة يملكها التطبيق**: `seeking = true` لحظة إصدار القفزة، وتُمسح عند أول نبضة زمن متقدّمة (`VLCPlayer.swift:619`)، مع إظهار المؤشّر + الوقت الهدف **فورًا** (بلا تأخير 0.3s للقفزات التي يبدأها المستخدم) | لا يغيّر السرعة إطلاقًا، ويغيّر الإحساس كثيرًا | معدوم |
| **4** | **خفض `network-caching` لـ VOD إلى ~600-700ms** (`VLCPlayer.swift:282`) كتجربة **مع قياس**، لا كتخمين | ~300-400ms أقل من **كل** قفزة | متوسط — تقطيع على الخطوط الضعيفة. اجعله إعدادًا، لا رقمًا مفروضًا |
| **5** | **توجيه `.mp4`/`.m4v`/`.mov` فقط إلى AVPlayer** (`StreamRouter.swift:45-49`) | قفز شبه فوري لذلك الجزء من المكتبة | متوسط، مُخفَّف بالـ failover القائم |
| **6** | إبقاء `:input-fast-seek` (`VLCPlayer.swift:287`) | مكسب صغير حقيقي | معدوم — لكن **لا تُقدَّم كإصلاح** |
| **7** | إبراز "نزّل ثم شاهد": الملف المُنزَّل يقفز فورًا لأنه لا يمسّ الشبكة | الحلّ الوحيد الذي يعطي "فوريًا" حقيقيًا | معدوم |

## "هذا أقصى ما يمكن"
سطر صريح: مع ملف MKV بحجم 2GB عبر HTTP، تستطيع البنود 1-4 أن تجعل القفزة **تُحسّ في حدود 0.6-1.2 ثانية**، ولا تستطيع أي شفرة أن تجعلها فورية — لأن العائق فيزياء الشبكة (RTT + طلب Range جديد + ملء المخزن المؤقّت)، لا الكود. **الفوريّة الحقيقية موجودة في مسار واحد فقط: الملف المُنزَّل.** أي وعد بقفز فوري لمحتوى بعيد هو إمّا ملف محلي أو ادّعاء.

---

## الترابط بين العطلين
العطل 2 يجعل العطل 1 أهم ممّا يبدو: **الملف المُنزَّل هو الحلّ الوحيد الذي يعطي قفزًا فوريًا حقيقيًا** — فإصلاح التنزيل الخلفي ليس ميزة جانبية، بل هو نصف الجواب على شكوى التقديم.
