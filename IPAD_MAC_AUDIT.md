# تدقيق iPad / Mac — Blank Prime

_تدقيق تخطيط (layout QA) لكل شاشة وكل حالة على نافذة كبيرة أو قابلة لتغيير الحجم._
_التاريخ: 2026-07-28 · الفرع `main` · آخر commit وقت التدقيق: `498499b`._
_لم يُعدَّل أي ملف مصدري. كل الأرقام محسوبة من الكود الفعلي ومن أبعاد الأجهزة الحقيقية في `DEVICE_MATRIX.md` — لا تخمين._

---

## 0. الحكم في سطر واحد

**لا. التطبيق ليس جاهزًا لأن يُراجَع على iPad.** دعم الـ iPad هنا **غير مكتمل بنيويًا، وليس مجرد مجموعة أخطاء**: نظام `S8KMetrics` صحيح ومدروس لكنه (أ) غير مُركَّب على شاشات ما قبل تسجيل الدخول إطلاقًا، و(ب) غير مُعتمَد في أغلب الصفحات — `HomeView` وحدها تستهلك 5 أعضاء من أصل ~14، و`PlayerView` لا تقرأه إطلاقًا. النتيجة: عمود بعرض هاتف داخل نافذة 1366pt، وأزرار بعرض 1326–1960pt، وغلاف بطل بنسبة 3.57:1 مكبَّر 4×.

**عدد نتائج BROKEN: 6.** (WRONG: 17 · AWKWARD/اجتهادي: 12)

---

## 1. ما الذي يدّعيه هذا الـ binary بالضبط

| الإعداد | القيمة | المصدر |
|---|---|---|
| `TARGETED_DEVICE_FAMILY` | `"1,2"` | `BlankTV.xcodeproj/project.pbxproj:311` (Debug), `:341` (Release) |
| `SUPPORTS_MACCATALYST` | **غير موجود** → الافتراضي `NO` | — |
| `SUPPORTS_MAC_DESIGNED_FOR_IPHONE_IPAD` | **غير موجود** → الافتراضي `YES` | — |
| `SUPPORTS_XR` | **غير موجود** → الافتراضي `YES` | — |
| `UIRequiresFullScreen` | **غير موجود** | `BlankTV/Info.plist` |
| `UISupportedInterfaceOrientations~ipad` | الاتجاهات الأربعة كلها (بما فيها UpsideDown) | `BlankTV/Info.plist:24-30` |
| `IPHONEOS_DEPLOYMENT_TARGET` | `17.0` | `project.pbxproj:301/331` |

**بعبارة صريحة: هذا الـ binary يُعرَض على أربع منصّات** — iPhone، وiPad، و**Mac بمعالج Apple silicon** ("Designed for iPad")، و**Apple Vision Pro** ("Designed for iPad") — ما لم يُلغِ المالك التوفّر يدويًا من App Store Connect. ليس Catalyst.
وغياب `UIRequiresFullScreen` يعني أنّ Split View وSlide Over وStage Manager **فعّالة**، وأنّ عرض النافذة **متّصل** (continuous) وليس جدول قيم.

**ثلاث من هذه المنصات الأربع لم تُفتَح ولو مرة واحدة.**

---

## 2. مرجع الأحجام المحسوبة (تُستخدم في كل بند أدناه)

| النافذة | `cls` | `gutter` | `contentMaxWidth` | `safeTop/Bottom` | `bottomClearance` | `heroHeight` |
|---|---|---|---|---|---|---|
| 1366×1024 — 12.9" أفقي | `regularXL` | 32 | 1040 | 24 / 20 | 100 | 584 |
| 1024×1366 — 12.9" رأسي | `regularLarge` | 28 | 900 | 24 / 20 | 100 | 584 |
| 834×1194 — Pro 11" رأسي | `regularLarge` | 28 | 900 | 24 / 20 | 100 | 584 |
| 744×1133 — mini رأسي | `regularLarge` | 28 | 900 | 24 / 20 | 100 | 584 |
| 678×1024 — نصف 12.9" | `regularMedium` | 24 | 760 (**عاطل**) | 24 / 20 | 100 | 584 |
| 592×1194 — نصف 11" | `regularMedium` | 24 | 760 (**عاطل**) | 24 / 20 | 100 | 456 |
| 561.5×744 — نصف mini (غير صحيح!) | `compactWide` | 20 | ∞ | 24 / 20 | 100 | 391.4 |
| 320×1000 — Slide Over رأسي | `compactNarrow` | 16 | ∞ | 24 / 20 | 100 | 616 |
| 2000×1200 — Mac | `regularXL` | 32 | 1040 | 0 / 0 | 80 | 560 |

`heroHeight` بعد commit `498499b` = `min(width·aspect + safeTop, heroMaxHeight, ceiling, 0.8h)`؛ على كل iPad **القيد المُلزِم هو `heroMaxHeight` = 560 + safeTop**، أي أنّ الارتفاع ثابت 584 في الاتجاهين ولا يتفاعل مع عرض النافذة إطلاقًا. هذا بالضبط جذر النتيجة B1.

**فروع `S8KDeviceClass` الستّة كلها قابلة للوصول** (تحقّقت من كل عرض في `DEVICE_MATRIX.md` §6b) — لا يوجد فرع ميّت. لكن **قيم `regularMedium` ميّتة**: `contentMaxWidth = 760` لا يمكن أن يُلزِم أبدًا لأنّ الصنف معرَّف بـ `w < 720`؛ و`readableMaxWidth = 640` لا يُلزم أيضًا (أوسع لوح ½ هو 678 → 678 − 48 = 630 < 640). أي أنّ **ألواح Split View النصفية على 11" و12.9" لا تحصل على أي حدّ عرض على الإطلاق** — غير ضارّ اليوم، لكنه كود ميّت يُوهم بأن الحالة مُعالَجة (`DesignSystem.swift:132`, `:134`, `:77-86`).

---

## 3. BROKEN

### B1 — غلاف البطل (hero) في الصفحة الرئيسية: نسبة 2.44:1 → 3.57:1، ويعرض 26%→19% من الملصق مكبَّرًا 2.7×→4×
`HomeView.swift:362` · `HomeView.swift:62-64` · `HomeView.swift:119` · `DesignSystem.swift:163`

`backdropURL` يسقط إلى `posterURL` / `coverURL` (`HomeView.swift:62-63`) — وهو **الحالة الغالبة** في كتالوج IPTV، وهو ما تفرضه قاعدة [[metadata-agnostic-design]] نفسها. و`S8KImage` افتراضه `contentMode: .fill`.

- **12.9" أفقي (1366×1024):** صندوق الرسم = 1366 × (584 − 24) = **1366×560، أي 2.44:1**. ملصق 500×750 يملأ بمعامل `max(1366/500, 560/750) = 2.732` → يُرسَم 1366×2049 ويُقصّ إلى 560 ⇒ **27% فقط من الملصق ظاهر**، مكبَّرًا 2.73×.
- **Mac بعرض 2000 (safeTop = 0):** الصندوق = **2000×560، أي 3.57:1**. المعامل 4.0 ⇒ **18.7% ظاهر**، مكبَّرًا 4×.
- للمقارنة، iPhone 15 Pro (393×852): المعامل 0.786 ⇒ **87% ظاهر**. العطل **حصريّ لـ iPad/Mac**: محور القصّ ينقلب من أفقي إلى رأسي لحظة أن تصير النافذة عريضة الشكل.
- حتى backdrop حقيقي 16:9 يفقد نصف ارتفاعه على Mac (المعامل 1.5625 ⇒ 49.8% ظاهر).
- **تفاقُم:** `maxPixel: 1400` في `HomeView.swift:362` — على 12.9" العرض 1366pt @2x = **2732px**، أي تكبير **1.95×** فوق دقّة فكّ الترميز؛ وعلى Mac 2000pt @2x = 4000px ⇒ **2.86×**. و`S8KImageCache` مفهرَس بالـ URL فقط، فرفع السطر 362 وحده **لن يعمل**: يجب رفع الـ prefetch في `HomeView.swift:119` أيضًا.

**السبب الجذري:** `heroMaxHeight` (`DesignSystem.swift:163`) يُثبّت الارتفاع عند 560 بينما العرض يجري full-bleed بلا حدّ، فتتدهور النسبة بلا نهاية كلما اتّسعت النافذة. `ideal` كان 789 عند 1366 و1120 عند 2000 — وكلاهما أُهمِل.

**الإصلاح الأدنى** (في `heroCard`، عند `HomeView.swift:362`، مع رفع الـ prefetch):
```swift
.frame(maxWidth: metrics.cls.isCompact ? .infinity : (height - metrics.safeTop) * 16.0/9.0)
// و 119/362: maxPixel: max(1400, metrics.size.width * 2)
```

---

### B2 — أول قسم في الصفحة الرئيسية يقع تحت الشريط المثبَّت ولا يمكن الوصول إليه، متى كان البطل فارغًا
`HomeView.swift:1117-1131` · `HomeView.swift:789-829` · `HomeView.swift:107-114`

`heroSection` هو `@ViewBuilder` بشرط `if !vm.heroItems.isEmpty` → لا يُرسِم **شيئًا** عند الفراغ. و`mainScroll` يحمل `.ignoresSafeArea(edges: .top)`، فيبدأ الطفل التالي عند `y = 0` **الفيزيائي**، أي تحت `S8KPinnedPageBar` (`HomeView.swift:726`). لا يوجد فرع `else` يحجز `metrics.topBarReserve`.

`heroItems` يكون فارغًا حين يخلو `movies` و`series` معًا — أي **اشتراك قنوات فقط**، أو فشل جلب VOD مع نجاح القنوات (وعندها `contentFailed` = false فلا يظهر حتى شريط الخطأ).

- **12.9" (أي اتجاه):** الشريط المحجوب = `safeTop` 24 + ارتفاع `homeTopBar` (8 + 48 + 10 = 66) = **90pt**. كتلة `SectionHeader` = 21 + 7 + 3 + 12 = 43pt ⇒ **عنوان «يبث الآن» كاملًا + أعلى 47pt من كل `ChannelChip`** تحت الشريط.
- **ليست قابلة للتمرير إلى داخل الرؤية** — هي عند إزاحة 0 من الـ ScrollView.
- Mac 2000pt: الشريط 66pt (يبتلع الترويسة كاملة).

**الإصلاح:** أضِف الفرع الناقص عند `HomeView.swift:1131`:
```swift
} else { Color.clear.frame(height: metrics.topBarReserve) }
```

---

### B3 — بوّابة التفعيل (أول شاشة يراها مُراجع Apple على جهاز غير مُفعَّل): بلا أي حدّ عرض، وأزرار بعرض 1326–1960pt
`ActivationView.swift:170` · `:174` · `:200` · `:216` · `:233` · `:252` · `:269` · و`:98` / `:145` للشقيقتين

`ActivationRequiredView` تُرسَم **خارج `S8KMetricsRoot`** (المُركَّب فقط حول `tabView` في `BlankTVApp.swift:242`؛ و`ActivationGate` فوقه) — فلا تستطيع قراءة metrics أصلًا، ولا تضع أي حدّ.

كل عنصر تحكّم هو `.frame(maxWidth: .infinity)` + `.padding(.horizontal, 20)`:
- **12.9" أفقي (1366):** كل زر = 1366 − 40 = **1326pt عرضًا**، بداخله نصّ 18pt.
- **11" أفقي (1194):** **1154pt**.
- **Mac 2000pt:** **1960pt** — كبسولة ليمونية واحدة تمتدّ عبر الشاشة.
- **الحشو العلوي:** الـ `ScrollView` (`:169`) يحترم الـ safe area (24 على iPad أفقي)، ثم `:269` يضيف **70** ⇒ الشعار يبدأ عند y = 94. على iPhone 15 Pro: 59 + 70 = **129pt** من السواد قبل أول بكسل.
- التعليق على `:269` صحيح نصفه فقط: نعم `metrics.safeTop` سيكون 0 خارج الجذر، لكن الـ ScrollView **قد طبّق الـ safe area بالفعل**، فالـ 70 حشو مضاعف.
- `MaintenanceView` (`:98`) و`UpdateRequiredView` (`:145`) بنفس العلّة: نصّ الرسالة (المُورَّد من الخادم) يمتدّ **1294pt** على 1366، و**1928pt** على Mac.

**الإصلاح:** على الـ VStack عند `:170` (وكذلك `:98` و`:145`):
```swift
.frame(maxWidth: 440).frame(maxWidth: .infinity)   // 480 للرسائل
```
وغيّر `:269` إلى `.padding(.top, S8KSpace.xxl)`.

---

### B4 — «الفئات المقفلة» (الرقابة الأبوية): اسم الفئة ومفتاحها يبعدان ~1200pt على مسافة صفّ 44pt
`SettingsView.swift:1285` · `:1287` · `:1323` · `:1371`

يُفتَح عبر الإعدادات → الرقابة الأبوية (`SettingsView.swift:184` `fullScreenCover`) → `step = .lockedCats`، فيأخذ **عرض النافذة كاملًا**، ولا شيء مُقيَّد.

- صفّ الفئة: `.padding(20)` على القائمة (`:1323`) + `.padding(.horizontal, 16)` على الصف (`:1371`) ⇒ محتوى الصفّ = 1366 − 40 − 32 = **1294pt**، رمز القفل والاسم في أقصى جهة و`Toggle` (`:1367`) في الجهة المقابلة ⇒ **~1200pt فراغ بين اسم الفئة ومفتاحها**، وارتفاع الصفّ 44pt. على Mac 2000pt الفجوة **1928pt**.
- هذه ليست مسألة جماليّة: خطر تبديل مفتاح الصفّ الخطأ عالٍ جدًا، وهي ميزة رقابة أبوية.
- رقاقات النوع (`:1287`، كلٌّ `.frame(maxWidth: .infinity)`، spacing 8): (1366 − 32 − 16)/3 = **كبسولات بعرض 439.3pt** لكلمة بعرض ~60pt.

**الإصلاح:** لُفّ الـ `VStack(spacing: 0)` عند `:1285` بـ `.frame(maxWidth: 640).frame(maxWidth: .infinity)`.

---

### B5 — نتائج البحث في الصفحة الرئيسية: 1256pt فراغ بين المصغَّرة وعنوانها، في كل صف
`HomeView.swift:614` · `:636-654`

`HomeSearchResults` تحمل `@Environment(\.s8kMetrics)` لكنها تستعمله للحشو فقط؛ الـ `LazyVStack` (`:614`) بلا أي حدّ عرض، وهي **overlay على كامل الشاشة** (`:776-784`).

- **12.9" أفقي:** عرض الصفّ بعد `.padding(.horizontal, 20)` = 1326. التخطيط: مصغَّرة 46pt + 12 + VStack + 12 + chevron ~12 ⇒ الـ VStack بعرض **1244pt**، ونصّاه يحملان `.frame(maxWidth: .infinity, alignment: .trailing)` (`:645`, `:647`). المصغَّرة عند x = 20…66، وحافّة العنوان اليمنى عند x = 1322 ⇒ **~1256pt فراغ بين الصورة وعنوانها**، في كل صف. و`Divider().padding(.leading, 74)` (`:618`) يمتدّ 1292pt.
- **Mac 2000pt:** الفجوة **~1890pt**.
- البحث مسار أساسي يفتحه المُراجع فورًا.

**الإصلاح** (على الـ `LazyVStack` عند `:614`):
```swift
.frame(maxWidth: metrics.readableMaxWidth).frame(maxWidth: .infinity)
```
النمط نفسه في `AlertsView` (`HomeView.swift:1526-1592`, فجوة ~1258pt) — انظر W7.

---

### B6 — قيمة metrics الافتراضية هاتف مُثبَّت في الكود، وكل شاشات ما قبل الدخول ترسم خارج جذر النظام
`DesignSystem.swift:199` · `BlankTVApp.swift:242`

```swift
static let defaultValue = S8KMetrics(size: CGSize(width: 393, height: 852),
                                     hClass: .compact, vClass: .regular)
```
`static let` — **يُقيَّم مرّة واحدة، ولا يُعاد اشتقاقه عند تغيير حجم النافذة أبدًا.** وهو ما تحصل عليه كل شاشة خارج `S8KMetricsRoot`: `SplashView`، `ActivationGate`، `ActivationRequiredView`، `MaintenanceView`، `UpdateRequiredView`، `GatewayView`، `SubscriptionsGateView`.

على نافذة Mac بعرض 2000pt تُبلِّغ هذه القيمة: `cls == .compactRegular`، `contentMaxWidth == .infinity`، `readableMaxWidth == .infinity`، `safeTop == 0`، `size == 393×852`. **بلا أي إشارة عند الترجمة.** هذا هو السبب المباشر الذي اضطرّ B3 إلى تثبيت الرقم 70 يدويًا، وهو فخّ لكل تعديل مستقبلي: أول `metrics.` يُضاف إلى أي شاشة بوّابة سيحصل صامتًا على أرقام هاتف.

**الإصلاح (الأصحّ):** لُفّ الـ ZStack في `ActivationView.swift:31` بـ `S8KMetricsRoot` — أو، كحدّ أدنى، اجعل `defaultValue` خاصية محسوبة تعيد `S8KMetrics(size: s8kWindowSize(), hClass: nil, vClass: nil)` حتى يكون العرض حقيقيًا على الأقل.

---

## 4. WRONG

### W1 — عمود بعرض 1040pt داخل نافذة Mac بعرض 2000pt = 48% حواف سوداء، تعرض 7 ملصقات بحجم هاتف
`HomeView.swift:814-815` · `DesignSystem.swift:132`

الحدّ **مُتمركز** بشكل صحيح (كل سلاسل `.frame(maxWidth: cap).frame(maxWidth: .infinity)` في المشروع تستعمل `.center` الافتراضي — لا شيء منبوذ إلى حافّة). المشكلة في **القيمة**:
- 1366 أفقي: العمود x ∈ [163, 1203] ⇒ **163pt سواد ميّت على كل جانب (24% من النافذة)**. شريط الـ rail = 1000pt، وخطوة `ContentCard` = 118 + 12 = 130 ⇒ **7 ملصقات ظاهرة**.
- Mac 2000pt: العمود x ∈ [480, 1520] ⇒ **480pt سواد على كل جانب (48%)**. ولا يزال **7 ملصقات** بعرض 118pt. مجموع حبر الملصقات = 898pt = 45% من النافذة.

نافذة 2000pt تعرض **المحتوى نفسه** الذي تعرضه نافذة 1040pt، مع 960pt من العدم. هذا أعلى إشارة «غير مكتمل على iPad» في التطبيق كله.

**الإصلاح (سطر واحد، `DesignSystem.swift:132`):**
```swift
var contentMaxWidth: CGFloat { cls.isCompact ? .infinity : min(1400, max(760, size.width * 0.86)) }
```

### W2 — `contentMaxWidth` دالّة درجيّة: سحب نافذة Mac 1pt عبر 1100 يقفز بالخلاصة 140pt
`DesignSystem.swift:132` + `:77-86`
- نافذة 1100pt: العمود 1040، الحواف (1100 − 1040)/2 = **30pt**.
- نافذة 1099pt: العمود 900، الحواف **99.5pt**.
نقطة واحدة من السحب تُزيح كل حافّة محتوى **69.5pt** وتقفز كل ملصق في كل rail. نوافذ Mac تُسحَب بحرّية، فهذا مرئي مباشرة. نفس إصلاح W1.

### W3 — عناوين الأقسام تتناوب بين اليسار واليمين نزولًا في الصفحة: تأرجح 860–1000pt على iPad
`HomeView.swift:1382 / 1404 / 1425` مقابل `:505-513 / 1204-1221 / 1235-1256` · `DesignSystem.swift:1183`

`SectionHeader` عبارة عن `VStack(alignment: .leading)` ⇒ العنوان وخطّه السفلي 32×3 في **أقصى اليسار الفيزيائي**. بينما ترويسة `RankRail` (`:505-510`) و`railHeader` (`:1204-1221`) تضع `Spacer(minLength: 0)` **أولًا** ⇒ العنوان في **أقصى اليمين**.

ترتيب القراءة نزولًا: متابعة المشاهدة **يمين** ← الأعلى تقييمًا (أفلام) **يمين** ← الأعلى تقييمًا (مسلسلات) **يمين** ← أُضيف حديثًا (أفلام) **يسار** ← أُضيف حديثًا (مسلسلات) **يسار** ← يبث الآن **يسار**.
- 12.9" رأسي: نهاية العناوين اليمينية x = 942 مقابل بداية اليسارية x = **82** ⇒ 860pt.
- 12.9" أفقي: x = **1183** مقابل x = **183** ⇒ **1000pt**.
- Mac 2000: 1500 مقابل 500.

على هاتف 393pt التأرجح 353pt ويُقرأ كإهمال؛ عند 1000pt يُقرأ كتطبيقين مُخيَّطين معًا.

**الإصلاح** (`DesignSystem.swift:1183-1185`): اجعل `SectionHeader` يتبع الاصطلاح نفسه — `VStack(alignment: s8kTextAlign)` + `if s8kIsRTL { Spacer() }` قبل `Text(title)`.

### W4 — الهيكل العظمي (skeleton) عناوينه يمينية والحقيقي يساري: قفزة متحرّكة 930pt عند كل تحميل
`HomeView.swift:856-859`
كتلة عنوان `skeletonRail` هي `.frame(maxWidth: .infinity, alignment: .trailing)` (`:859`) داخل حدّ `contentMaxWidth` نفسه، بينما تحلّ محلّها أقسام مبنية على `SectionHeader` (**يسارية**). ومع `.animation(...value: showSkeleton)` (`:732`) تطير كتلة 150×18 من x≈1113 إلى x≈183 على 12.9" أفقي — **930pt**، عند كل تحميل بارد وكل تبديل حساب.
**الإصلاح:** `:859` → `.frame(maxWidth: .infinity, alignment: s8kFrameAlign)`.

### W5 — `HomeView` لا تستعمل أدوات المرآة (`s8kIsRTL` / `s8kFrameAlign` / `s8kTextAlign`) إطلاقًا
`HomeView.swift:383-455`, `1343-1373`, `505-513`, `643-649`, `1249`
التطبيق يفرض `layoutDirection = .leftToRight` عالميًا (`BlankTVApp.swift:172`) ويعكس يدويًا — **و`HomeView` صفر إشارات إلى أي من الأدوات** (grep). مواقع `.trailing` الحرفية: 383، 389/391، 407، 434، 445، 455، 1343/1368/1373، 505، 1204، 643/645/647؛ و`chevron.left` عائد للخلف عند 649 و1249.
بالإنجليزية على نافذة 1366pt: عنوان البطل وتقييمه وزرّ التشغيل 52pt كلها مثبّتة على الحافّة اليمنى (x ≈ 1183) بينما `SectionHeader` عند x ≈ 183 — انفصال 1000pt لن يقرأه أي مستخدم LTR كقصد، مع أسهم «عرض الكل» تشير في الاتجاه الخطأ.

### W6 — شبكة `AllHistoryView` تصغُر خلاياها كلما كبرت النافذة: 12 عمودًا عند 2000pt
`HomeView.swift:1603` · `:1630-1633` · `:1645`
`GridItem(.adaptive(minimum: 150), spacing: 12)`, `.padding(20)`، معروضة كـ `fullScreenCover` بلا أي حدّ:
| النافذة | المتاح | الأعمدة | عرض الخليّة |
|---|---|---|---|
| iPhone 15 Pro 393 | 353 | **2** | **170.5pt** |
| iPad Pro 12.9 أفقي 1366 | 1326 | **8** | **155.25pt** |
| Mac 2000 | 1960 | **12** | **152.33pt** |
الهاتف يحصل على **أكبر** الخلايا، ونافذة Mac بعرض 2000pt على أصغرها بـ 12 عمودًا. وارتفاع المصغَّرة مثبَّت 96pt (`:1645`) ⇒ النسبة تتأرجح 1.78:1 → 1.62:1 → 1.59:1 عشوائيًا، وقصّ `.fill` يتغيّر معها.
**الإصلاح:** اجعل `cols` محسوبة: `.adaptive(minimum: metrics.cls.isCompact ? 150 : 220)` + `.frame(maxWidth: metrics.contentMaxWidth).frame(maxWidth: .infinity)`.

### W7 — `AlertsView`: بطاقات تنبيه بعرض 1326pt، الأيقونة ونصّها في طرفين متقابلين
`HomeView.swift:1526-1557` · `:1572-1592`
معروضة عبر `.fullScreenCover(item: $router.homeSheet)` (`BlankTVApp.swift:279`) ⇒ عرض النافذة كاملًا، بلا حدّ. 12.9" أفقي: البطاقة 1326pt، الأيقونة عند x = 36…72 وحافّة العنوان عند x = 1330 ⇒ **~1258pt فجوة**. Mac: ~1892pt.
**الإصلاح:** بعد `.padding(20)` عند `:1556` → `.frame(maxWidth: metrics.readableMaxWidth).frame(maxWidth: .infinity)`.

### W8 — أزرار الدعم بعرض 495pt، وزرّ التجديد بعرض 1000pt
`HomeView.swift:1447-1461` · `:1464` · `:1476-1494`
12.9" أفقي: (1040 − 40 − 10)/2 = **495pt** لكل زرّ واتساب/تلغرام بارتفاع 43pt ⇒ نسبة 11.5:1. رأسي: 425pt. زرّ التجديد الاختياري صفّ واحد بعرض **1000pt**.
**الإصلاح** (`:1476-1477`): `.frame(maxWidth: metrics.formMaxWidth).frame(maxWidth: .infinity)`.

### W9 — بانر الخطأ بعرض 1000pt، وهو الكتلة الوحيدة المتمركزة في صفحة كلها محاذاة للحافّة
`HomeView.swift:882-907` · `:899`
1000pt على 12.9" أفقي (860 رأسي، 1000 على Mac) لأيقونة 30pt ونصّين وكبسولة ~120pt.
**الإصلاح** (`:899`): `.frame(maxWidth: metrics.readableMaxWidth)` ثم `.frame(maxWidth: .infinity)`.

### W10 — `ParentalControlView.menu` بلا حدّ عرض داخل `fullScreenCover`
`SettingsView.swift:1136` · `:1160` · `:1162` · `:1178`
صفوف `actionCard` = **1326pt** عند 1366 و**1960pt** عند 2000، بأيقونة 42pt في طرف وchevron 13pt في الطرف الآخر. و`GoldButton` (`:1162`) بعرض 1326pt. و`Text(L("pc.enable_hint"))` (`:1160`) سطر واحد بعرض **1326pt** (عرض Apple المقروء ≈ 672).
**الإصلاح** (`:1136`): `.frame(maxWidth: metrics.readableMaxWidth).frame(maxWidth: .infinity)` — و`metrics` متاح أصلًا عند `:1071`.

### W11 — `AccountSwitcherView`: 9 أعمدة، وبطاقات المستخدم منبوذة في الزاوية العليا اليسرى
`SettingsView.swift:601` · `:621` · `:625`
`GridItem(.adaptive(minimum: 118, maximum: 168), spacing: 20)` + `.padding(.horizontal, 24)` داخل `fullScreenCover`:
- **1366pt:** المتاح 1318 ⇒ **9 أعمدة** بعرض **128.7pt**.
- **2000pt (Mac):** **14 عمودًا** بعرض **120.9pt**.
المستخدم الحقيقي لديه 1–3 حسابات، و`LazyVGrid` يملأ من الحافّة البادئة ⇒ **3 بطاقات 98pt في الزاوية العليا اليسرى و~1000pt سواد إلى اليمين** — منتقي حسابات بنمط Netflix غير متمركز. والترويسة (`:609-617`) تفاقمه: زرّ الإغلاق x=20، العنوان x≈1250.
**الإصلاح:** `.frame(maxWidth: 720).frame(maxWidth: .infinity)` بعد الـ padding ⇒ يقيّد عند 5 أعمدة بعرض 118.4pt بالضبط ويتمركز.

### W12 — `SetScaffold` يحجز شريط التبويب العائم **داخل** form sheet على iPad
`SettingsView.swift:340` (المعروض من `:394`)
`EngineStatsView` تُعرض بـ `.sheet` ⇒ على iPad **form sheet بمقاس 540×620**، لا شريط تبويب فيه. لكنّ SwiftUI ينشر القيم البيئية المخصّصة داخل محتوى الـ sheet، فيبقى `metrics` واصفًا **النافذة 1366×1024** (`cls == .regularXL`).
`Color.clear.frame(height: metrics.bottomClearance)` = 68 + 12 + 20 = **100pt تمرير ميّت** في ورقة ارتفاعها 620 = **16% من الورقة**. والتسريب نفسه يجعل `readableMaxWidth` = 640 > 540 فالحدّ عند `:342` **لا يفعل شيئًا**.
**الإصلاح:** أضِف `var inSheet: Bool = false` إلى `SetScaffold` واستعمل `inSheet ? metrics.safeBottom + S8KSpace.xl : metrics.bottomClearance`.

### W13 — `LoginView` بلا أي وسيلة إغلاق مرئية، وهي دائمًا sheet
`AuthViews.swift:123-276` (تُعرض من `SettingsView.swift:633`)
على iPad/Mac هي form sheet 540×620 **بلا `NavigationStack`، بلا toolbar، بلا زرّ إغلاق، وبلا `presentationDragIndicator`**. جسم الورقة كلّه `ScrollView` (`:136`) فتتنافس إيماءة السحب-للإغلاق مع التمرير؛ **وعلى Mac لا يوجد سحب أصلًا** (Esc يعمل، لكن لا شيء على الشاشة يقول ذلك).
**الإصلاح:** لُفّ الجسم بـ `NavigationStack { … .toolbar { ToolbarItem(placement: .topBarLeading) { Button(L("common.cancel")) { dismiss() } } } }` داخل `LoginView` نفسها.

### W14 — `AboutView` هي النافذة الوحيدة بلا `ScrollView`
`SettingsView.swift:881` · `:904`
`VStack` ثابت مع `Spacer()` و`.padding(32)` في form sheet 540×620 (≈564pt مفيد). عند الحجم الافتراضي ≈410pt فتمرّ؛ عند Larger Text (AX3+) يتجاوز إخلاء المسؤولية المطلوب من Apple (قاعدة 4.3) حدود الإطار **ويُقصّ بلا أي وسيلة تمرير**. كل شقيقاتها في هذه الملفات تحمل تعليق `SCROLLABLE:` — هذه فاتت.
**الإصلاح:** لُفّ الـ VStack بـ `ScrollView` + `.defaultScrollAnchor(.center)` + `.scrollBounceBehavior(.basedOnSize)`.

### W15 — `HistoryGrid`: ارتفاع صورة مثبَّت 150pt داخل عمود 168–206pt ⇒ الملصقات تُرسَم كشرائط أفقية
`ContentViews.swift:1906` · `:1912`
هذا **بالضبط** العطل الذي تقول تعليقات `MoviePosterCell` (`:1972-1975`) و`SeriesGrid` (`:2471-2474`) إنه أُصلِح — ولم يُصلَح هنا. مع `hSize == .regular` (أي لوح 592–719pt، وكل استعمال لـ `HistoryGrid` في تخطيط الهاتف على iPad):
- لوح 592pt: 3 أعمدة، الخليّة **177.3 × 150** ⇒ نسبة 1.18:1.
- لوح 678pt (نصف 12.9"): 3 أعمدة، الخليّة **206 × 150** ⇒ **1.37:1** — شريط أفقي لملصق رأسي 2:3.
**الإصلاح:** استبدل `.frame(height: 150)` بصندوق `Color.clear.frame(maxWidth:.infinity).aspectRatio(2.0/3.0, contentMode: .fit).overlay { … }` كما في `MoviePosterCell`.

### W16 — صفحات التفاصيل: لوحة الرسم بنسبة 3.03:1 وسطور نصّ بطول 976pt
`ContentViews.swift:2551` / `:2592` / `:2567` (فيلم) و`:2802` / `:2843` / `:2822` (مسلسل)
`canvasHeight = max(180, min(h·0.44, h − 300))` مأخوذ من **ارتفاع** النافذة فقط، بينما اللوحة `.frame(maxWidth: .infinity)`:
| النافذة | canvasHeight | نسبة اللوحة |
|---|---|---|
| 1024×1366 (12.9 رأسي) | 601 | 1.70:1 |
| 1366×1024 (12.9 أفقي) | 450.6 | **3.03:1** |
| 744×1133 (mini) | 498.5 | 1.49:1 |
| Mac 2000×1200 | 528 | **3.79:1** |
| Mac 1600×500 | 200 | **8:1** |
مع سقوط `backdropURL` إلى `posterURL`، يعني هذا شريحة أفقية بنسبة 3:1 من وسط ملصق 2:3 — والقاعدة الحاكمة للمشروع تقول إنّ الكتالوج قد لا يملك backdrop أصلًا.
وثانيًا: الـ plinth مُقيَّد بـ `metrics.contentMaxWidth` (`:2567`, `:2822`) بينما اللوحة full-bleed ⇒ الملخّص وطاقم العمل يمتدّان **1040 − 2×32 = 976pt** لكل سطر على iPad أفقي و**844pt** رأسيًا (Apple ≈ 672).
**الإصلاح:** استعمل `metrics.readableMaxWidth` لكتل النصّ داخل `plinthContent` (`:2636-2657`, `:2870+`)، وقيّد عرض اللوحة على الأصناف `regular*` بـ `canvasHeight * 16.0/9.0` متمركزًا.

### W17 — طبقة تحكّم المشغّل بلا أي حدّ عرض، وأزرار النقل بلا إطار لمس أصلًا
`PlayerView.swift:829` · `:874-875` · `:1012` · `:1026-1031` · `:1032-1058`
`controlsOverlay` كلّه `.padding(.horizontal, 20)` على عرض النافذة، بلا `maxWidth` في أي مستوى:
- **1366pt:** شريط الترجيع (Slider) بعرض **1326pt**؛ و6 رقاقات `actionChip` كلٌّ `.frame(maxWidth: .infinity)` ⇒ **221pt لكل رقاقة** حول أيقونة 48pt (173pt فراغ حول كل أيقونة)؛ وشريط العنوان العلوي يفرّق العنوان عن أزرار AirPlay/PiP/القفل/التدوير بـ 1326pt.
- **Mac 2000pt:** 1960pt للـ Slider و**326.7pt** لكل رقاقة.
- `ctrlBtn` (`:1026-1031`) **بلا أي `.frame`** ⇒ مساحة اللمس = حجم الرمز نفسه (22–28pt)، وهي أزرار ±10 ثوانٍ والحلقة السابقة/التالية وتبديل القناة. على Mac لا يوجد هامش لمس يعوّض.
- `iconCircle` (`:1018-1025`) = 38×38 (تحت حدّ Apple 44pt).
- `.padding(.top, isLandscape ? 12 : 50)` (`:875`): الرقم 50 رقم شقّ هاتف؛ على iPad رأسي المجموع 24 + 50 = 74pt.
**الإصلاح:** لُفّ الـ `VStack` عند `:844` بـ `.frame(maxWidth: 900).frame(maxWidth: .infinity)`، وأضِف `.frame(minWidth: 44, minHeight: 44).contentShape(Rectangle())` داخل `ctrlBtn` و`iconCircle`.

### W18 — مناطق إيماءات المشغّل: نصفان بعرض 683pt، بلا منطقة ميتة في الوسط
`PlayerView.swift:371-376` · `:569-586` · `:655-676`
`HStack { gestureZone(isVolume: true); gestureZone(isVolume: false) }` يقسم **كامل** عرض المشغّل نصفين متساويين:
| العرض | نصف المنطقة |
|---|---|
| iPhone 393 | 196.5pt |
| iPad 12.9 أفقي 1366 | **683pt** |
| Mac 2000 | **1000pt** |
- نقرة مزدوجة على بُعد 1pt يمين المركز (أي في **منتصف الصورة تمامًا** على شاشة 13") تُطلق ترجيعًا +10 ثوانٍ. النمط الصناعي (Netflix/YouTube على iPad) يترك منطقة ميتة وسطى؛ لا توجد هنا.
- وسحبة رأسية في **أي مكان** على النصف الأيسر تغيّر صوت النظام، وعلى النصف الأيمن السطوع — على مساحة 683×1024pt.
**الإصلاح:** أدخِل منطقة وسطى محايدة على الأصناف العريضة، مثلًا `HStack { zone(true); if !metrics.cls.isCompact { Color.clear.frame(width: metrics.size.width * 0.2).contentShape(Rectangle()).onTapGesture { toggleControls() } }; zone(false) }`.

### W19 — `UIScreen.main.brightness` يكذب على Mac ويشير إلى الشاشة الخطأ تحت Stage Manager
`PlayerView.swift:145` · `:614` · `:622`
`@State private var brightness = Double(UIScreen.main.brightness)` تُقرأ **مرّة واحدة عند التهيئة** ولا تُحدَّث أبدًا. وعلى Mac ("Designed for iPad") **الـ setter لا يفعل شيئًا**: النصف الأيمن من الشاشة يعرض HUD سطوع بنسبة تتحرّك ولا تغيّر شيئًا. وتحت Stage Manager / شاشة خارجية، `UIScreen.main` ليست بالضرورة الشاشة التي تعرض النافذة. `UIScreen.main` مُهمَل في iOS 26 ومُدرَج في قائمة الممنوعات في `DEVICE_MATRIX.md` §5.
**الإصلاح:** اقرأ/اكتب عبر `view.window?.windowScene?.screen`، وأخفِ إيماءة السطوع كليًا حين `screen.brightness` غير قابل للضبط.

### W20 — صفر دعم للمؤشّر ولوحة المفاتيح في 20,140 سطرًا
grep عبر `BlankTV/*.swift`: **صفر** نتيجة لـ `.hoverEffect`، `.onHover`، `.keyboardShortcut`، `.onKeyPress`، `.focusable`.
على Mac (وعلى iPad مع Magic Keyboard): لا تغذية راجعة عند تمرير المؤشّر على أيٍّ من ~50 ملصق/رقاقة في الصفحة الرئيسية، ولا Space للتشغيل/الإيقاف، ولا ←/→ للترجيع، ولا ⌘W. `.contextMenu` موجود في ثلاثة مواضع فقط (`HomeView.swift:1289`, `:1658`, `AuthViews.swift:558`) وهو يُترجَم إلى نقرة يمنى، **لكن وضع `editingHistory` لا يُدخَل إلا من تلك القائمة** (`HomeView.swift:1292`) وهو غير قابل للاكتشاف بمؤشّر.
**ليس BROKEN** (لا شيء غير قابل للوصول تمامًا)، لكنه أعلى إشارة «هذا تطبيق هاتف» على Mac. الحدّ الأدنى: `.hoverEffect(.highlight)` على البطاقات، و`.keyboardShortcut(.space, modifiers: [])` للتشغيل/الإيقاف، و`.keyboardShortcut(.escape)` للإغلاق.

### W21 — التنقّل الأساسي عبر `onTapGesture` على حاوية بدل `Button`
`SettingsView.swift:222` (و`:806`)
`:222` هو **المدخل الوحيد** لمبدّل الحسابات (الكبسولة عند `:208-216` مجرّد زينة داخل المساحة الملموسة). على Mac: لا تأثير hover، لا حالة ضغط، غير قابل للتركيز تحت Full Keyboard Access، وVoiceOver يقرؤه نصًّا ساكنًا بلا سمة زرّ.
**الإصلاح:** `Button { showAccounts = true } label: { heroContent }.buttonStyle(S8KButtonStyle())`.

### W22 — أزرار الفهرس والحذف في التنزيلات دون الحدّ الأدنى
`Downloads.swift:787` · `:798`
زرّ سلّة المحذوفات: رمز 16pt بلا إطار ⇒ مستطيل لمس ≈ **18×19pt**. و`ctlBtn` ≈ **30×30pt**. (`DownloadControl` عند `:638` أصلحها بشكل صحيح بـ `.frame(minWidth: 44, minHeight: 44)` داخل الـ label — لكنها لم تُعمَّم.) على Mac حيث لا هامش لمس، هذان هما الإجراءان الأساسي والمُدمِّر في الشاشة.

### W23 — `SearchView`: الترويسة مقيَّدة بـ 640 والشبكة بـ 1040 — حافّتان مختلفتان بفارق 200pt لكل جانب
`ContentViews.swift:3361` مقابل `:3498`
`contentMaxWidth` المحلّي (`:3323`) = `readableMaxWidth` = 640، بينما شبكة النتائج (`:3498`) تستعمل `metrics.contentMaxWidth` = 1040. على 1366pt: حقل البحث عمود 640 متمركز (x ∈ [363, 1003]) والنتائج عمود 1040 (x ∈ [163, 1203]) ⇒ الحقل يبدو مُدسوسًا داخل النتائج. كذلك `Color.clear.frame(height: 100)` (`:3500`) رقم صلب بدل `metrics.bottomClearance`.

---

## 5. AWKWARD / اجتهادي (مُعلَّم صراحةً)

**A1 — كل بطاقة في التطبيق إطار ثابت بحجم هاتف؛ لا شيء يستشير `metrics`** _(اجتهادي: قرار تصميم، لكنه يقرأ كـ«تطبيق هاتف مُكبَّر»)_
`DesignSystem.swift:1794/1821/1828` (`ContentCard` 118×166)، `:1856/1878` (`ChannelChip` 64×64)، `HomeView.swift:538` (ملصق `RankRail` 106×154)، `:1346/1362` (`watchCard` 220×124)، `:1645` (ارتفاع 96).
على iPad Pro 12.9" (264 ppi، @2x ⇒ 1pt ≈ 0.0076 بوصة): شعار قناة **0.48 بوصة**، وملصق فيلم **0.89 × 1.26 بوصة** على لوح 13 بوصة.

**A2 — `metrics.gutter` كود ميّت في `HomeView`**
`HomeView.swift:449/514/526/652/860/868/905/954/1008/1070/1089/1194/1226/1257/1267/1391/1412/1433/1477` كلها `S8KSpace.xl` (20) ثابتة. النظام يقول 24/28/32 على iPad — فيحصل 12.9" على هامش iPhone SE نفسه.

**A3 — `heroTitleSize` مسطَّح للأصناف الـ regular** — `HomeView.swift:330-334`: `regularLarge` و`regularXL` يعودان بـ 32. لوح Split View 592pt ونافذة Mac 2000pt يحصلان على الحجم نفسه. على بطل 1366×560 العنوان = 2.3% من عرض البطاقة؛ على هاتف 393pt = 5.9%. أي أنّ العنوان **أصغر نسبيًا** على 13 بوصة.

**A4 — «عرض الكل» هدف لمس بارتفاع 17pt** — `HomeView.swift:1246-1252`: `Button` بلا `.padding` ولا `.contentShape` ⇒ ارتفاع مساحة الإصابة ≈ 17pt، ويطفو وحده عند x = 183 في عمود 1040pt.

**A5 — حشو علوي مضاعف على الـ safe area** — `SettingsView.swift:618` (`max(56, safeTop + 12)` فوق الـ safe area التي يطبّقها الـ cover ⇒ 80pt على iPad، 56 منها ميّتة) و`:1127` (74pt). و`HomeView.swift:1626` (`.padding(.top, 80)` داخل `EmptyState` متمركز أصلًا ⇒ 136pt من الأعلى على iPad).

**A6 — `.padding(.top, max(50, metrics.safeTop + 8))` في اللوحات الثلاث** — `ContentViews.swift:262`, `:299`, `:1516`, `:1683`, `:2231`: على iPad `safeTop = 24` ⇒ `max(50, 32) = 50`، أي 50pt ثابتة. تتحاذى اللوحات معًا (وهو المقصود)، لكن الرقم رقم شقّ هاتف لا علاقة له بالـ iPad.

**A7 — أعمدة الشبكة تنخفض عندما تتّسع النافذة عند حدّ compact→regular**
`ContentViews.swift:1933/2450` + `DesignSystem.swift:1562` (`.adaptive(minimum: hSize == .regular ? 168 : 116)`):
- لوح mini النصفي 561.5pt (compact): **4 أعمدة × 121.9pt**
- لوح 11" النصفي 592pt (regular): **3 أعمدة × 177.3pt**
سحب الفاصل 30pt يقلّل عدد الملصقات ويكبّرها. مقصود على الأرجح، لكنه انقطاع مرئي.

**A8 — عرض ألواح المتصفّح ثلاثي الألواح مثبَّت أعلى 1185pt** — `ContentViews.swift:217-218`:
`sidebarW = min(230, max(175, w·0.20))`، `channelsW = min(320, max(240, w·0.27))`.
| النافذة | الشريط الجانبي | قائمة القنوات | لوح المشغّل | ارتفاع الفيديو 16:9 |
|---|---|---|---|---|
| 744 (mini رأسي) | 175 | 240 | 327 | **184pt** |
| 810 / 820 / 834 | 175 | 240 | 393 / 403 / 417 | 221 / 227 / 235 |
| 981 (⅔ من 12.9") | 196.2 | 264.9 | 517.9 | 291 |
| 1024 (12.9 رأسي) | 204.8 | 276.5 | 540.7 | 304 |
| 1194 (11" أفقي) | 230 | 320 | 642 | 361 |
| 1366 / 1376 | 230 | 320 | 814 / 824 | 458 / 464 |
| Mac 2000 | 230 | 320 | **1448** | **814** |
فوق 1185pt يذهب **كل** العرض الإضافي إلى لوح المشغّل وحده. على Mac 2000pt يبقى الشريط الجانبي 230pt (11.5% من النافذة) والقائمة 320pt. وعلى 744pt يبتلع اللوحان الأولان 56% من النافذة تاركين فيديو بارتفاع 184pt.
_وحدّ `useSplit(w) = isPad && w >= 720` (`:173`) صحيح فعلًا_: 678 و592 يسقطان إلى تخطيط الهاتف، و981 و1024 يستعملان الألواح — هذا مُعالَج بشكل جيّد.

**A9 — الشريط الجانبي للفئات بلا حقل بحث** — `ContentViews.swift:1499-1533`. على خط IPTV بـ 400 فئة، اللوح الوحيد الذي يفتحه مستخدم iPad هو قائمة تمرير عمياء. تخطيط الهاتف يقدّم `CategoryPickerSheet` ببحث؛ اللوح لا.

**A10 — جدار ملصقات البوّابة: 72% من ارتفاع نافذة iPad أفقية، وثلثه مدفون تحت سواد صُلب**
`GatewayView.swift:24` (`gwCardWRegular = 152`) · `:98-104` · `:302-309`
ارتفاع الجدار = 30 + 3×228 + 2×12 = **738pt**؛ والتدرّج يصل إلى أسود صُلب عند 42% من الارتفاع = 430pt على نافذة 1024. أي أنّ **308pt من الجدار (42% منه) غير مرئية**، ويبقى المرئي ≈ 1.9 صفّ.
و`topInset = max(16, winH − 766)` (`:363-367`): 600 على 12.9" رأسي، 258 أفقي، 367 على mini، 134 على Mac 1440×900 — كلها ضمن المدى، والبطاقة 400pt متمركزة (483pt سواد على كل جانب عند 1366). **البوّابة أسلم شاشة في التطبيق على iPad** — لا شيء منبوذ ولا مشدود، ولا `GeometryReader` جذري.

**A11 — `.containerRelativeFrame(.horizontal)` في دوّار البطل غير مُعاد التثبيت عند التدوير/تغيير الحجم**
`HomeView.swift:288-313`: لا يوجد `.onChange(of: metrics.size)`. الدوران من 1024 إلى 1366 يأخذ الإزاحة من 3×1024 = 3072 إلى شبكة صفحات 1366 ⇒ 2.249 صفحة، أي الاستقرار عند 25% داخل الصفحة 2. `.scrollPosition(id:)` **يُفترض** أن يعيد التثبيت على iOS 17+، لكن لا شيء في الكود يضمنه. تأمين رخيص: `.onChange(of: metrics.size) { _, _ in let c = currentID; currentID = nil; currentID = c }`.

**A12 — `orientationLock = .allButUpsideDown` عند فتح المشغّل** — `PlayerView.swift:73`. على iPad الاتجاه المقلوب اتجاه شرعي كامل (وهو معلن في `Info.plist:27`)؛ فتح المشغّل على iPad مثبَّت مقلوبًا يقلب الواجهة كلها 180°. الإصلاح: `UIDevice.current.userInterfaceIdiom == .pad ? .all : .allButUpsideDown`.

**A13 — الاعتماد على `UIDevice.current.userInterfaceIdiom == .pad`** — `ContentViews.swift:169`, `:1586`, `:2143`, `:3316`, `BlankTVApp.swift:76`, `PlayerView.swift:81`.
هذه **صحيحة اليوم** على Mac (تطبيق Designed-for-iPad يُبلِّغ `.pad`)، لكنها على قائمة الممنوعات في `DEVICE_MATRIX.md` §7 البند 2، وستنهار صامتة لحظة شحن Catalyst أو نسخة visionOS أصلية — و`SUPPORTS_XR` مفعّل افتراضيًا اليوم. استبدلها بـ `metrics.cls >= .regularLarge`.

**A14 — `SetUI.divider()` مُزاح من الجهة الخطأ** — `SettingsView.swift:26`: `.padding(.leading, 60)` بينما كل صفوف الإعدادات تضع `iconTile` **أخيرًا = في أقصى اليمين** (`:55`, `:72`, `:86`, `:96`). مرئي على كل صفحة إعدادات فرعية بالعربية، عند كل عرض.

**A15 — `SettingsView.swift:37` مقابل `:43`** — عنوان المجموعة `.padding(.horizontal, 28)` والبطاقة التي يعنونها `20` ⇒ اختلال محاذاة دائم 8pt.

**A16 — `PINEntryView` جزيرة 280×290 في حقل أسود 1366×1024** — `SettingsView.swift:991` + `:1006`. عدد الأعمدة الثابت 3 **ليس** عطلًا (مقيَّد بـ `.frame(maxWidth: 280)` ⇒ مفاتيح 84×62pt عند كل عرض)؛ العطل أنّ `PINEntryView` تُستعمل صفحة `fullScreenCover` كاملة (`:1092`, `:1097`, `:1101`, `:1105`, `:1111`).

**A17 — `S8KMetrics.regularMedium` قيمه ميّتة** — انظر §2. `contentMaxWidth = 760` مستحيل الإلزام، و`readableMaxWidth = 640` مستحيل الإلزام بعد الهوامش. غير ضارّ، لكنه يُوهم بأنّ ألواح ½ Split View مُعالَجة.

**A18 — `Downloads.swift:810`** — `DownloadService.freeBytes()` (استعلام قرص متزامن) يُستدعى داخل `body` الذي يُعاد بناؤه عند كل `didWriteData`. ليس عطل تخطيط، لكنه توقّف على الخيط الرئيسي طوال فتح شاشة التنزيلات.

**A19 — كود ميّت لا يزال يُشحَن** — `AuthViews.swift:443` (`SubscriptionsGateView`، وسيلة الحذف الوحيدة فيها ضغطة طويلة بلا بديل، `:558`)؛ `AuthViews.swift:279` (`langMenu` غير مستعمل)؛ `SettingsView.swift:115` و`:331` (`hSize` مُعلَن ولا يُقرأ)؛ `HomeView.swift:910-957`, `959-973`, `1057-1076`, `1079-1101`, `1165-1174`, `1176-1198`, `1203-1228`. ملاحظة: `bannerSection` لو أُعيد تفعيله سيرسم بانرًا **1000×110 (9.1:1)** على 12.9" أفقي.

---

## 6. تحليل مباشر لبنود التكليف العشرة

| # | البند | الحكم |
|---|---|---|
| 1 | أصناف الأحجام | الفروع الستّة **كلها قابلة للوصول** وحدودها صحيحة مقابل أعراض `DEVICE_MATRIX.md`. **لا يوجد موضع يفترض أنّ `hSize == .regular` تعني iPad** بشكل ضارّ — كل مواضع الألواح مُقيَّدة بـ `width >= 720` (`ContentViews.swift:173/1588/2147`). العيوب: قيم `regularMedium` ميّتة (A17)، `compactWide` تضمّ 414pt رأسيًا و**956pt** (هاتف أفقي) في صنف واحد بلا أي حدّ عرض، والاعتماد على `userInterfaceIdiom` (A13). |
| 2 | تغيير الحجم أثناء التشغيل | **جيّد بشكل مفاجئ.** `S8KMetricsRoot` (`DesignSystem.swift:214+`) وسبر `GatewayView.winH` و`PlayerEngineView.isLandscape` و`useSplit(geo.size.width)` كلها تحمل `.onChange`. **العيب الوحيد من هذه الفئة هو B6**: `S8KMetricsKey.defaultValue` هو `static let` بقيمة 393×852 لا تُعاد اشتقاقها أبدًا، وهو ما تحصل عليه كل شاشة خارج الجذر. |
| 3 | الأعمدة والشبكات | شبكات الملصقات الرئيسية **سليمة** (5 أعمدة × 195.4pt على 12.9"؛ 9 × 172.8 على Mac 2000، بنسبة 2:3 محفوظة عبر صندوق `Color.clear` + `aspectRatio`). العيوب: `HistoryGrid` (W15، شريط أفقي)، `AllHistoryView` (W6، 12 عمودًا وخلايا أصغر من الهاتف)، `AccountSwitcherView` (W11، 9–14 عمودًا لثلاثة حسابات). |
| 4 | حدود العرض | **كل** سلسلة `.frame(maxWidth: cap).frame(maxWidth: .infinity)` في المشروع تستعمل `.center` الافتراضي ⇒ **لا شيء منبوذ إلى حافّة**. الأعطال هي (أ) القيم نفسها (W1، W2)، و(ب) **غياب** الحدّ في 9 شاشات (B3، B4، B5، W7، W8، W9، W10، W11، W17)، و(ج) اختلال المحاذاة العربية داخل الحدّ (W3، W4، W5، W23). |
| 5 | متصفّح البثّ ثلاثي الألواح | **الأقلّ سوءًا في التطبيق.** النسب صحيحة، والتراجع عند `width < 720` صحيح، والفيديو يحافظ على 16:9. العيوب: `A8` (كل العرض فوق 1185pt يذهب للمشغّل وحده؛ 184pt فيديو على mini)، `A9` (لا بحث في الشريط الجانبي)، `A6` (50pt ثابتة). لوح المشغّل `VStack` غير قابل للتمرير — على نافذة Mac عريضة وقصيرة سينكمش الفيديو (`.aspectRatio(.fit)` يضيّق ولا يقصّ)، فلا فقدان محتوى. |
| 6 | صفحات التفاصيل والمشغّل | W16 (لوحة 3.03:1 وسطور 976pt)، W17 (طبقة تحكّم بلا حدّ، `ctrlBtn` بلا إطار لمس)، W18 (نصفا إيماءة 683pt بلا منطقة ميتة)، W19 (`UIScreen.main.brightness`). |
| 7 | البوّابة | **سليمة**. البطاقة 400pt متمركزة، `topInset` مشتقّ من ارتفاع النافذة ومناعِة للوحة المفاتيح، الـ marquee متّصل حتى 2000pt (`reps = 6` ⇒ 5892pt إجمالي مقابل قاعدة 984pt). العيب الوحيد اجتهادي: A10. |
| 8 | خاصّ بـ Mac | W20 (صفر hover/keyboard)، W21 (`onTapGesture` بدل `Button`)، W19 (`UIScreen`)، W13 (sheet بلا إغلاق مرئي)، W22 + W17 (أهداف 18–30pt بلا هامش لمس). لا يوجد `sizeRestrictions`/`minimumSize` في أي مكان ⇒ النافذة تُسحَب إلى ما دون أي حدّ. **لا شيء غير قابل للوصول تمامًا** بلا لمس: `.contextMenu` يُترجَم إلى نقرة يمنى، و`.onMove`/`.onDelete` في `CategoryReorderView` تعملان مع `editMode` نشط (مقابض السحب وأزرار الحذف الحمراء ظاهرة). |
| 9 | الاتجاه | `Info.plist` يعلن الاتجاهات الأربعة لـ iPad. `AppDelegate.orientationLock` (`BlankTVApp.swift:75-76`) يعطي `.all` على iPad/Mac. العيوب: A12 (المشغّل يجبر `.allButUpsideDown`)، A11 (عدم إعادة تثبيت الدوّار)، وB1 يتفاقم في الأفقي (2.44:1 مقابل 1.83:1). |
| 10 | ما سيبدو غير مكتمل لمُراجع | بالترتيب: B1 (البطل الضبابي 4×)، W1 (48% حواف سوداء و7 ملصقات بحجم هاتف)، B3 (زرّ 1326pt في أول شاشة على جهاز غير مُفعَّل)، B5/W7 (1256pt فراغ في كل صفّ)، A1 (شعار قناة بحجم 0.48 بوصة على شاشة 13 بوصة)، W3 (عناوين تتناوب يمينًا ويسارًا بفارق 1000pt). |

---

## 7. الحدّ الأدنى القابل للشحن (قائمة الإصلاح، بالترتيب)

هذه هي القائمة التي تنقل التطبيق من «غير مكتمل على iPad» إلى «قابل للمراجعة». ثمانية تغييرات، ستّة منها سطر واحد.

1. **`DesignSystem.swift:132`** — اجعل `contentMaxWidth` تتبع النافذة بدل الدرجات: يصلح W1 وW2 معًا.
2. **`HomeView.swift:362` + `:119`** — قيّد عرض رسم البطل على الأصناف الـ regular إلى `(height − safeTop) × 16/9`، وارفع `maxPixel` إلى `max(1400, size.width * 2)` **في الموضعين**: يصلح B1.
3. **`HomeView.swift:1131`** — أضِف الفرع `else { Color.clear.frame(height: metrics.topBarReserve) }`: يصلح B2.
4. **`ActivationView.swift:170/98/145` + `:269`** — حدّ 440/480 متمركز، واحذف الـ 70: يصلح B3.
5. **`SettingsView.swift:1285` و`:1136` و`:621`، `HomeView.swift:614` و`:1556` و`:899` و`:1477`** — سبعة أسطر `.frame(maxWidth: …).frame(maxWidth: .infinity)`: يصلح B4، B5، W7، W8، W9، W10، W11.
6. **`ContentViews.swift:1906/1912`** — استبدل الارتفاع الثابت 150 بصندوق 2:3: يصلح W15.
7. **`PlayerView.swift:844` + `:1026-1031` + `:1018-1025`** — حدّ 900pt على طبقة التحكّم، و`minWidth/minHeight: 44` داخل `ctrlBtn` و`iconCircle`: يصلح شطر W17.
8. **`DesignSystem.swift:199` أو `ActivationView.swift:31`** — أخرِج القيمة الافتراضية من كونها هاتفًا مثبَّتًا: يصلح B6 ويغلق فئة العطل كاملة.

**بعدها مباشرة (قبل التقديم للمراجعة، وليس بالضرورة قبل الاختبار على الجهاز):** W3/W4/W5 (اتّجاه المحاذاة)، W20 (`.hoverEffect` + اختصاري Space وEsc)، A1/A2 (تكبير البطاقات والهوامش على الأصناف الـ regular).

**الاختبارات الأربعة التي يجب أن تُجرى أوّلًا على الجهاز** (بعد الإصلاحات): (أ) iPad Pro 12.9" أفقي ملء الشاشة، (ب) iPad Pro 12.9" رأسي، (ج) Slide Over بعرض 320pt، (د) نافذة Mac تُسحَب من ~800pt إلى ~2000pt أثناء التشغيل.

---

_انتهى التقرير. لم يُعدَّل أي ملف مصدري._
